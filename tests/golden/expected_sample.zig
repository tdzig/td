//! GENERATED by td TL code generator from "sample.tl".
//! Do not edit by hand; regenerate with td-gen.

const std = @import("std");
const td = @import("td");
const Writer = td.tl.Writer;
const Reader = td.tl.Reader;
const TlError = td.TlError;
const vector_constructor_id = td.tl.vector_constructor_id;

pub const boolFalse = struct {
    pub const constructor_id: u32 = 0xbc799737;

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        _ = self;
        _ = w;
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        _ = allocator;
        _ = r;
        return .{};
    }
};

pub const boolTrue = struct {
    pub const constructor_id: u32 = 0x997275b5;

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        _ = self;
        _ = w;
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        _ = allocator;
        _ = r;
        return .{};
    }
};

pub const photoSize = struct {
    pub const constructor_id: u32 = 0x0e17e23c;
    type_: []const u8,
    w: i32,
    h: i32,
    size: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeString(self.type_);
        try w.writeInt(self.w);
        try w.writeInt(self.h);
        try w.writeInt(self.size);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.type_ = try r.readString();
        self.w = try r.readInt();
        self.h = try r.readInt();
        self.size = try r.readInt();
        _ = allocator;
        return self;
    }
};

pub const geoPoint = struct {
    pub const constructor_id: u32 = 0x2042cdd9;
    long_: f64,
    lat: f64,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeDouble(self.long_);
        try w.writeDouble(self.lat);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.long_ = try r.readDouble();
        self.lat = try r.readDouble();
        _ = allocator;
        return self;
    }
};

pub const userProfilePhoto = struct {
    pub const constructor_id: u32 = 0x82d1f706;
    has_video: bool = false,
    photo_id: i64,
    stripped_thumb: ?[]const u8 = null,
    dc_id: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.has_video) flags |= 1 << 0;
        if (self.stripped_thumb != null) flags |= 1 << 1;
        try w.writeUInt(flags);
        try w.writeLong(self.photo_id);
        if (self.stripped_thumb) |v| {
            try w.writeBytes(v);
        }
        try w.writeInt(self.dc_id);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.has_video = (flags >> 0) & 1 != 0;
        self.photo_id = try r.readLong();
        if ((flags >> 1) & 1 != 0) {
            self.stripped_thumb = try r.readBytes();
        } else {
            self.stripped_thumb = null;
        }
        self.dc_id = try r.readInt();
        _ = allocator;
        return self;
    }
};

pub const user = struct {
    pub const constructor_id: u32 = 0xd10d979a;
    self: bool = false,
    id: i64,
    first_name: ?[]const u8 = null,
    last_name: ?[]const u8 = null,
    photo: ?UserProfilePhoto = null,
    tags: [][]i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.self) flags |= 1 << 10;
        if (self.first_name != null) flags |= 1 << 1;
        if (self.last_name != null) flags |= 1 << 2;
        if (self.photo != null) flags |= 1 << 5;
        try w.writeUInt(flags);
        try w.writeLong(self.id);
        if (self.first_name) |v| {
            try w.writeString(v);
        }
        if (self.last_name) |v| {
            try w.writeString(v);
        }
        if (self.photo) |v| {
            try v.serialize(w);
        }
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.tags.len);
        for (self.tags) |elem_1| {
            try w.writeUInt(vector_constructor_id);
            try w.writeVectorLength(elem_1.len);
            for (elem_1) |elem_2| {
                try w.writeInt(elem_2);
            }
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.self = (flags >> 10) & 1 != 0;
        self.id = try r.readLong();
        if ((flags >> 1) & 1 != 0) {
            self.first_name = try r.readString();
        } else {
            self.first_name = null;
        }
        if ((flags >> 2) & 1 != 0) {
            self.last_name = try r.readString();
        } else {
            self.last_name = null;
        }
        if ((flags >> 5) & 1 != 0) {
            self.photo = try UserProfilePhoto.deserialize(allocator, r);
        } else {
            self.photo = null;
        }
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_3 = try r.readVectorLength();
        const vec_4 = allocator.alloc([]i32, len_3) catch return error.OutOfMemory;
        for (vec_4) |*elem_5| {
            if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
            const len_6 = try r.readVectorLength();
            const vec_7 = allocator.alloc(i32, len_6) catch return error.OutOfMemory;
            for (vec_7) |*elem_8| {
                elem_8.* = try r.readInt();
            }
            elem_5.* = vec_7;
        }
        self.tags = vec_4;
        return self;
    }
};

pub const Bool = union(enum) {
    boolFalse: boolFalse,
    boolTrue: boolTrue,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            boolFalse.constructor_id => return .{ .boolFalse = try boolFalse.deserialize(allocator, r) },
            boolTrue.constructor_id => return .{ .boolTrue = try boolTrue.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const PhotoSize = union(enum) {
    photoSize: photoSize,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            photoSize.constructor_id => return .{ .photoSize = try photoSize.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const GeoPoint = union(enum) {
    geoPoint: geoPoint,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            geoPoint.constructor_id => return .{ .geoPoint = try geoPoint.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const UserProfilePhoto = union(enum) {
    userProfilePhoto: userProfilePhoto,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            userProfilePhoto.constructor_id => return .{ .userProfilePhoto = try userProfilePhoto.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const User = union(enum) {
    user: user,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            user.constructor_id => return .{ .user = try user.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const users = struct {
pub const getUsers = struct {
    pub const constructor_id: u32 = 0x0d91a548;
    pub const Result = []User;
    id: []i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeConstructorId(constructor_id);
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.id.len);
        for (self.id) |elem_9| {
            try w.writeInt(elem_9);
        }
    }
};

};

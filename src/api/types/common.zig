//! GENERATED by td TL code generator from "api.tl".
//! Do not edit by hand; regenerate with td-gen.

const std = @import("std");
const td = @import("../../root.zig");
const Writer = td.tl.Writer;
const Reader = td.tl.Reader;
const TlError = td.TlError;
const vector_constructor_id = td.tl.vector_constructor_id;

const api = @import("../mod.zig");

pub const boolFalse = struct {
    pub const constructor_id: u32 = 0xbc799737;

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        _ = self;
        _ = w;
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        _ = allocator;
        _ = r;
        return .{};
    }
};

pub const boolTrue = struct {
    pub const constructor_id: u32 = 0x997275b5;

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        _ = self;
        _ = w;
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        _ = allocator;
        _ = r;
        return .{};
    }
};

pub const true_ = struct {
    pub const constructor_id: u32 = 0x3fedd339;

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        _ = self;
        _ = w;
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        _ = allocator;
        _ = r;
        return .{};
    }
};

pub const error_ = struct {
    pub const constructor_id: u32 = 0xc4b9f9bb;
    code: i32,
    text: []const u8,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeInt(self.code);
        try w.writeString(self.text);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.code = try r.readInt();
        self.text = try r.readString();
        _ = allocator;
        return self;
    }
};

pub const null_ = struct {
    pub const constructor_id: u32 = 0x56730bcc;

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        _ = self;
        _ = w;
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        _ = allocator;
        _ = r;
        return .{};
    }
};

pub const receivedNotifyMessage = struct {
    pub const constructor_id: u32 = 0xa384b779;
    id: i32,
    flags: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeInt(self.id);
        try w.writeInt(self.flags);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.id = try r.readInt();
        self.flags = try r.readInt();
        _ = allocator;
        return self;
    }
};

pub const dataJSON = struct {
    pub const constructor_id: u32 = 0x7d748d04;
    data: []const u8,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeString(self.data);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.data = try r.readString();
        _ = allocator;
        return self;
    }
};

pub const inputClientProxy = struct {
    pub const constructor_id: u32 = 0x75588b3f;
    address: []const u8,
    port: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeString(self.address);
        try w.writeInt(self.port);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.address = try r.readString();
        self.port = try r.readInt();
        _ = allocator;
        return self;
    }
};

pub const inputAppEvent = struct {
    pub const constructor_id: u32 = 0x1d1b1245;
    time: f64,
    type_: []const u8,
    peer: i64,
    data: *api.JSONValue,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeDouble(self.time);
        try w.writeString(self.type_);
        try w.writeLong(self.peer);
        try self.data.serialize(w);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.time = try r.readDouble();
        self.type_ = try r.readString();
        self.peer = try r.readLong();
                const ptr_1 = allocator.create(api.JSONValue) catch return error.OutOfMemory;
        ptr_1.* = try api.JSONValue.deserialize(allocator, r);
        self.data = ptr_1;
        return self;
    }
};

pub const jsonObjectValue = struct {
    pub const constructor_id: u32 = 0xc0de1bd9;
    key: []const u8,
    value: *api.JSONValue,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeString(self.key);
        try self.value.serialize(w);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.key = try r.readString();
                const ptr_2 = allocator.create(api.JSONValue) catch return error.OutOfMemory;
        ptr_2.* = try api.JSONValue.deserialize(allocator, r);
        self.value = ptr_2;
        return self;
    }
};

pub const jsonNull = struct {
    pub const constructor_id: u32 = 0x3f6d7b68;

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        _ = self;
        _ = w;
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        _ = allocator;
        _ = r;
        return .{};
    }
};

pub const jsonBool = struct {
    pub const constructor_id: u32 = 0xc7345e6a;
    value: bool,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeBool(self.value);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.value = try r.readBool();
        _ = allocator;
        return self;
    }
};

pub const jsonNumber = struct {
    pub const constructor_id: u32 = 0x2be0dfa4;
    value: f64,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeDouble(self.value);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.value = try r.readDouble();
        _ = allocator;
        return self;
    }
};

pub const jsonString = struct {
    pub const constructor_id: u32 = 0xb71e767a;
    value: []const u8,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeString(self.value);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.value = try r.readString();
        _ = allocator;
        return self;
    }
};

pub const jsonArray = struct {
    pub const constructor_id: u32 = 0xf7444763;
    value: []*api.JSONValue,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.value.len);
        for (self.value) |elem_3| {
            try elem_3.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_4 = try r.readVectorLength();
        const vec_5 = allocator.alloc(*api.JSONValue, len_4) catch return error.OutOfMemory;
        for (vec_5) |*elem_6| {
                        const ptr_7 = allocator.create(api.JSONValue) catch return error.OutOfMemory;
            ptr_7.* = try api.JSONValue.deserialize(allocator, r);
            elem_6.* = ptr_7;
        }
        self.value = vec_5;
        return self;
    }
};

pub const jsonObject = struct {
    pub const constructor_id: u32 = 0x99c1d49d;
    value: []*api.JSONObjectValue,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.value.len);
        for (self.value) |elem_8| {
            try elem_8.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_9 = try r.readVectorLength();
        const vec_10 = allocator.alloc(*api.JSONObjectValue, len_9) catch return error.OutOfMemory;
        for (vec_10) |*elem_11| {
                        const ptr_12 = allocator.create(api.JSONObjectValue) catch return error.OutOfMemory;
            ptr_12.* = try api.JSONObjectValue.deserialize(allocator, r);
            elem_11.* = ptr_12;
        }
        self.value = vec_10;
        return self;
    }
};

pub const codeSettings = struct {
    pub const constructor_id: u32 = 0xad253d78;
    allow_flashcall: bool = false,
    current_number: bool = false,
    allow_app_hash: bool = false,
    allow_missed_call: bool = false,
    allow_firebase: bool = false,
    unknown_number: bool = false,
    logout_tokens: ?[][]const u8 = null,
    token: ?[]const u8 = null,
    app_sandbox: ?bool = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.allow_flashcall) flags |= 1 << 0;
        if (self.current_number) flags |= 1 << 1;
        if (self.allow_app_hash) flags |= 1 << 4;
        if (self.allow_missed_call) flags |= 1 << 5;
        if (self.allow_firebase) flags |= 1 << 7;
        if (self.unknown_number) flags |= 1 << 9;
        if (self.logout_tokens != null) flags |= 1 << 6;
        if (self.token != null) flags |= 1 << 8;
        if (self.app_sandbox != null) flags |= 1 << 8;
        try w.writeUInt(flags);
        if (self.logout_tokens) |v| {
            try w.writeUInt(vector_constructor_id);
            try w.writeVectorLength(v.len);
            for (v) |elem_13| {
                try w.writeBytes(elem_13);
            }
        }
        if (self.token) |v| {
            try w.writeString(v);
        }
        if (self.app_sandbox) |v| {
            try w.writeBool(v);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.allow_flashcall = (flags >> 0) & 1 != 0;
        self.current_number = (flags >> 1) & 1 != 0;
        self.allow_app_hash = (flags >> 4) & 1 != 0;
        self.allow_missed_call = (flags >> 5) & 1 != 0;
        self.allow_firebase = (flags >> 7) & 1 != 0;
        self.unknown_number = (flags >> 9) & 1 != 0;
        if ((flags >> 6) & 1 != 0) {
            if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
            const len_14 = try r.readVectorLength();
            const vec_15 = allocator.alloc([]const u8, len_14) catch return error.OutOfMemory;
            for (vec_15) |*elem_16| {
                elem_16.* = try r.readBytes();
            }
            self.logout_tokens = vec_15;
        } else {
            self.logout_tokens = null;
        }
        if ((flags >> 8) & 1 != 0) {
            self.token = try r.readString();
        } else {
            self.token = null;
        }
        if ((flags >> 8) & 1 != 0) {
            self.app_sandbox = try r.readBool();
        } else {
            self.app_sandbox = null;
        }
        return self;
    }
};

pub const restrictionReason = struct {
    pub const constructor_id: u32 = 0xd072acb4;
    platform: []const u8,
    reason: []const u8,
    text: []const u8,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeString(self.platform);
        try w.writeString(self.reason);
        try w.writeString(self.text);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.platform = try r.readString();
        self.reason = try r.readString();
        self.text = try r.readString();
        _ = allocator;
        return self;
    }
};

pub const textWithEntities = struct {
    pub const constructor_id: u32 = 0x751f3146;
    text: []const u8,
    entities: []api.MessageEntity,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeString(self.text);
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.entities.len);
        for (self.entities) |elem_17| {
            try elem_17.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.text = try r.readString();
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_18 = try r.readVectorLength();
        const vec_19 = allocator.alloc(api.MessageEntity, len_18) catch return error.OutOfMemory;
        for (vec_19) |*elem_20| {
            elem_20.* = try api.MessageEntity.deserialize(allocator, r);
        }
        self.entities = vec_19;
        return self;
    }
};

pub const factCheck = struct {
    pub const constructor_id: u32 = 0xb89bfccf;
    need_check: bool = false,
    country: ?[]const u8 = null,
    text: ?api.TextWithEntities = null,
    hash: i64,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.need_check) flags |= 1 << 0;
        if (self.country != null) flags |= 1 << 1;
        if (self.text != null) flags |= 1 << 1;
        try w.writeUInt(flags);
        if (self.country) |v| {
            try w.writeString(v);
        }
        if (self.text) |v| {
            try v.serialize(w);
        }
        try w.writeLong(self.hash);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.need_check = (flags >> 0) & 1 != 0;
        if ((flags >> 1) & 1 != 0) {
            self.country = try r.readString();
        } else {
            self.country = null;
        }
        if ((flags >> 1) & 1 != 0) {
            self.text = try api.TextWithEntities.deserialize(allocator, r);
        } else {
            self.text = null;
        }
        self.hash = try r.readLong();
        return self;
    }
};

pub const Bool = union(enum) {
    boolFalse: api.boolFalse,
    boolTrue: api.boolTrue,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.boolFalse.constructor_id => return .{ .boolFalse = try api.boolFalse.deserialize(allocator, r) },
            api.boolTrue.constructor_id => return .{ .boolTrue = try api.boolTrue.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const True = union(enum) {
    true_: api.true_,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.true_.constructor_id => return .{ .true_ = try api.true_.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const Error = union(enum) {
    error_: api.error_,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.error_.constructor_id => return .{ .error_ = try api.error_.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const Null = union(enum) {
    null_: api.null_,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.null_.constructor_id => return .{ .null_ = try api.null_.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const ReceivedNotifyMessage = union(enum) {
    receivedNotifyMessage: api.receivedNotifyMessage,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.receivedNotifyMessage.constructor_id => return .{ .receivedNotifyMessage = try api.receivedNotifyMessage.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const DataJSON = union(enum) {
    dataJSON: api.dataJSON,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.dataJSON.constructor_id => return .{ .dataJSON = try api.dataJSON.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const InputClientProxy = union(enum) {
    inputClientProxy: api.inputClientProxy,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.inputClientProxy.constructor_id => return .{ .inputClientProxy = try api.inputClientProxy.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const InputAppEvent = union(enum) {
    inputAppEvent: api.inputAppEvent,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.inputAppEvent.constructor_id => return .{ .inputAppEvent = try api.inputAppEvent.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const JSONObjectValue = union(enum) {
    jsonObjectValue: api.jsonObjectValue,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.jsonObjectValue.constructor_id => return .{ .jsonObjectValue = try api.jsonObjectValue.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const JSONValue = union(enum) {
    jsonNull: api.jsonNull,
    jsonBool: api.jsonBool,
    jsonNumber: api.jsonNumber,
    jsonString: api.jsonString,
    jsonArray: api.jsonArray,
    jsonObject: api.jsonObject,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.jsonNull.constructor_id => return .{ .jsonNull = try api.jsonNull.deserialize(allocator, r) },
            api.jsonBool.constructor_id => return .{ .jsonBool = try api.jsonBool.deserialize(allocator, r) },
            api.jsonNumber.constructor_id => return .{ .jsonNumber = try api.jsonNumber.deserialize(allocator, r) },
            api.jsonString.constructor_id => return .{ .jsonString = try api.jsonString.deserialize(allocator, r) },
            api.jsonArray.constructor_id => return .{ .jsonArray = try api.jsonArray.deserialize(allocator, r) },
            api.jsonObject.constructor_id => return .{ .jsonObject = try api.jsonObject.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const CodeSettings = union(enum) {
    codeSettings: api.codeSettings,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.codeSettings.constructor_id => return .{ .codeSettings = try api.codeSettings.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const RestrictionReason = union(enum) {
    restrictionReason: api.restrictionReason,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.restrictionReason.constructor_id => return .{ .restrictionReason = try api.restrictionReason.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const TextWithEntities = union(enum) {
    textWithEntities: api.textWithEntities,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.textWithEntities.constructor_id => return .{ .textWithEntities = try api.textWithEntities.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const FactCheck = union(enum) {
    factCheck: api.factCheck,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.factCheck.constructor_id => return .{ .factCheck = try api.factCheck.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};


//! GENERATED by td TL code generator from "api.tl".
//! Do not edit by hand; regenerate with td-gen.

const std = @import("std");
const td = @import("../../root.zig");
const Writer = td.tl.Writer;
const Reader = td.tl.Reader;
const TlError = td.TlError;
const vector_constructor_id = td.tl.vector_constructor_id;

const api = @import("../mod.zig");

pub const ephemeralMessage = struct {
    pub const constructor_id: u32 = 0xdd27bee9;
    out: bool = false,
    welcome_template: bool = false,
    invert_media: bool = false,
    noforwards: bool = false,
    id: i32,
    from_id: api.Peer,
    peer_id: ?api.Peer = null,
    receiver_id: i64,
    top_msg_id: ?i32 = null,
    date: i32,
    message: []const u8,
    entities: ?[]api.MessageEntity = null,
    media: ?*api.MessageMedia = null,
    reply_markup: ?api.ReplyMarkup = null,
    reply_to: ?api.MessageReplyHeader = null,
    rich_message: ?api.RichMessage = null,
    chat_instance: ?i64 = null,
    anchor_msg_id: ?i32 = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.out) flags |= 1 << 0;
        if (self.welcome_template) flags |= 1 << 5;
        if (self.invert_media) flags |= 1 << 7;
        if (self.noforwards) flags |= 1 << 12;
        if (self.peer_id != null) flags |= 1 << 9;
        if (self.top_msg_id != null) flags |= 1 << 1;
        if (self.entities != null) flags |= 1 << 2;
        if (self.media != null) flags |= 1 << 3;
        if (self.reply_markup != null) flags |= 1 << 4;
        if (self.reply_to != null) flags |= 1 << 6;
        if (self.rich_message != null) flags |= 1 << 8;
        if (self.chat_instance != null) flags |= 1 << 10;
        if (self.anchor_msg_id != null) flags |= 1 << 11;
        try w.writeUInt(flags);
        try w.writeInt(self.id);
        try self.from_id.serialize(w);
        if (self.peer_id) |v| {
            try v.serialize(w);
        }
        try w.writeLong(self.receiver_id);
        if (self.top_msg_id) |v| {
            try w.writeInt(v);
        }
        try w.writeInt(self.date);
        try w.writeString(self.message);
        if (self.entities) |v| {
            try w.writeUInt(vector_constructor_id);
            try w.writeVectorLength(v.len);
            for (v) |elem_2595| {
                try elem_2595.serialize(w);
            }
        }
        if (self.media) |v| {
            try v.serialize(w);
        }
        if (self.reply_markup) |v| {
            try v.serialize(w);
        }
        if (self.reply_to) |v| {
            try v.serialize(w);
        }
        if (self.rich_message) |v| {
            try v.serialize(w);
        }
        if (self.chat_instance) |v| {
            try w.writeLong(v);
        }
        if (self.anchor_msg_id) |v| {
            try w.writeInt(v);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.out = (flags >> 0) & 1 != 0;
        self.welcome_template = (flags >> 5) & 1 != 0;
        self.invert_media = (flags >> 7) & 1 != 0;
        self.noforwards = (flags >> 12) & 1 != 0;
        self.id = try r.readInt();
        self.from_id = try api.Peer.deserialize(allocator, r);
        if ((flags >> 9) & 1 != 0) {
            self.peer_id = try api.Peer.deserialize(allocator, r);
        } else {
            self.peer_id = null;
        }
        self.receiver_id = try r.readLong();
        if ((flags >> 1) & 1 != 0) {
            self.top_msg_id = try r.readInt();
        } else {
            self.top_msg_id = null;
        }
        self.date = try r.readInt();
        self.message = try r.readString();
        if ((flags >> 2) & 1 != 0) {
            if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
            const len_2596 = try r.readVectorLength();
            const vec_2597 = allocator.alloc(api.MessageEntity, len_2596) catch return error.OutOfMemory;
            for (vec_2597) |*elem_2598| {
                elem_2598.* = try api.MessageEntity.deserialize(allocator, r);
            }
            self.entities = vec_2597;
        } else {
            self.entities = null;
        }
        if ((flags >> 3) & 1 != 0) {
                        const ptr_2599 = allocator.create(api.MessageMedia) catch return error.OutOfMemory;
            ptr_2599.* = try api.MessageMedia.deserialize(allocator, r);
            self.media = ptr_2599;
        } else {
            self.media = null;
        }
        if ((flags >> 4) & 1 != 0) {
            self.reply_markup = try api.ReplyMarkup.deserialize(allocator, r);
        } else {
            self.reply_markup = null;
        }
        if ((flags >> 6) & 1 != 0) {
            self.reply_to = try api.MessageReplyHeader.deserialize(allocator, r);
        } else {
            self.reply_to = null;
        }
        if ((flags >> 8) & 1 != 0) {
            self.rich_message = try api.RichMessage.deserialize(allocator, r);
        } else {
            self.rich_message = null;
        }
        if ((flags >> 10) & 1 != 0) {
            self.chat_instance = try r.readLong();
        } else {
            self.chat_instance = null;
        }
        if ((flags >> 11) & 1 != 0) {
            self.anchor_msg_id = try r.readInt();
        } else {
            self.anchor_msg_id = null;
        }
        return self;
    }
};

pub const welcomeMessagesNotModified = struct {
    pub const constructor_id: u32 = 0x59ffdb31;

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        _ = self;
        _ = w;
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        _ = allocator;
        _ = r;
        return .{};
    }
};

pub const welcomeMessages = struct {
    pub const constructor_id: u32 = 0x104fc872;
    hash: i64,
    messages: []api.EphemeralMessage,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeLong(self.hash);
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.messages.len);
        for (self.messages) |elem_2600| {
            try elem_2600.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.hash = try r.readLong();
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_2601 = try r.readVectorLength();
        const vec_2602 = allocator.alloc(api.EphemeralMessage, len_2601) catch return error.OutOfMemory;
        for (vec_2602) |*elem_2603| {
            elem_2603.* = try api.EphemeralMessage.deserialize(allocator, r);
        }
        self.messages = vec_2602;
        return self;
    }
};

pub const EphemeralMessage = union(enum) {
    ephemeralMessage: api.ephemeralMessage,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.ephemeralMessage.constructor_id => return .{ .ephemeralMessage = try api.ephemeralMessage.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const WelcomeMessages = union(enum) {
    welcomeMessagesNotModified: api.ephemeral.welcomeMessagesNotModified,
    welcomeMessages: api.ephemeral.welcomeMessages,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.ephemeral.welcomeMessagesNotModified.constructor_id => return .{ .welcomeMessagesNotModified = try api.ephemeral.welcomeMessagesNotModified.deserialize(allocator, r) },
            api.ephemeral.welcomeMessages.constructor_id => return .{ .welcomeMessages = try api.ephemeral.welcomeMessages.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};


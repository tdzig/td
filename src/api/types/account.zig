//! GENERATED by td TL code generator from "api.tl".
//! Do not edit by hand; regenerate with td-gen.

const std = @import("std");
const td = @import("../../root.zig");
const Writer = td.tl.Writer;
const Reader = td.tl.Reader;
const TlError = td.TlError;
const vector_constructor_id = td.tl.vector_constructor_id;

const api = @import("../mod.zig");

pub const privacyRules = struct {
    pub const constructor_id: u32 = 0x50a04e45;
    rules: []api.PrivacyRule,
    chats: []api.Chat,
    users: []api.User,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.rules.len);
        for (self.rules) |elem_1411| {
            try elem_1411.serialize(w);
        }
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.chats.len);
        for (self.chats) |elem_1412| {
            try elem_1412.serialize(w);
        }
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.users.len);
        for (self.users) |elem_1413| {
            try elem_1413.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_1414 = try r.readVectorLength();
        const vec_1415 = allocator.alloc(api.PrivacyRule, len_1414) catch return error.OutOfMemory;
        for (vec_1415) |*elem_1416| {
            elem_1416.* = try api.PrivacyRule.deserialize(allocator, r);
        }
        self.rules = vec_1415;
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_1417 = try r.readVectorLength();
        const vec_1418 = allocator.alloc(api.Chat, len_1417) catch return error.OutOfMemory;
        for (vec_1418) |*elem_1419| {
            elem_1419.* = try api.Chat.deserialize(allocator, r);
        }
        self.chats = vec_1418;
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_1420 = try r.readVectorLength();
        const vec_1421 = allocator.alloc(api.User, len_1420) catch return error.OutOfMemory;
        for (vec_1421) |*elem_1422| {
            elem_1422.* = try api.User.deserialize(allocator, r);
        }
        self.users = vec_1421;
        return self;
    }
};

pub const accountDaysTTL = struct {
    pub const constructor_id: u32 = 0xb8d0afdf;
    days: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeInt(self.days);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.days = try r.readInt();
        _ = allocator;
        return self;
    }
};

pub const authorizations = struct {
    pub const constructor_id: u32 = 0x4bff8ea0;
    authorization_ttl_days: i32,
    authorizations: []api.Authorization,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeInt(self.authorization_ttl_days);
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.authorizations.len);
        for (self.authorizations) |elem_1423| {
            try elem_1423.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.authorization_ttl_days = try r.readInt();
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_1424 = try r.readVectorLength();
        const vec_1425 = allocator.alloc(api.Authorization, len_1424) catch return error.OutOfMemory;
        for (vec_1425) |*elem_1426| {
            elem_1426.* = try api.Authorization.deserialize(allocator, r);
        }
        self.authorizations = vec_1425;
        return self;
    }
};

pub const password = struct {
    pub const constructor_id: u32 = 0x957b50fb;
    has_recovery: bool = false,
    has_secure_values: bool = false,
    has_password: bool = false,
    current_algo: ?api.PasswordKdfAlgo = null,
    srp_B: ?[]const u8 = null,
    srp_id: ?i64 = null,
    hint: ?[]const u8 = null,
    email_unconfirmed_pattern: ?[]const u8 = null,
    new_algo: api.PasswordKdfAlgo,
    new_secure_algo: api.SecurePasswordKdfAlgo,
    secure_random: []const u8,
    pending_reset_date: ?i32 = null,
    login_email_pattern: ?[]const u8 = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.has_recovery) flags |= 1 << 0;
        if (self.has_secure_values) flags |= 1 << 1;
        if (self.has_password) flags |= 1 << 2;
        if (self.current_algo != null) flags |= 1 << 2;
        if (self.srp_B != null) flags |= 1 << 2;
        if (self.srp_id != null) flags |= 1 << 2;
        if (self.hint != null) flags |= 1 << 3;
        if (self.email_unconfirmed_pattern != null) flags |= 1 << 4;
        if (self.pending_reset_date != null) flags |= 1 << 5;
        if (self.login_email_pattern != null) flags |= 1 << 6;
        try w.writeUInt(flags);
        if (self.current_algo) |v| {
            try v.serialize(w);
        }
        if (self.srp_B) |v| {
            try w.writeBytes(v);
        }
        if (self.srp_id) |v| {
            try w.writeLong(v);
        }
        if (self.hint) |v| {
            try w.writeString(v);
        }
        if (self.email_unconfirmed_pattern) |v| {
            try w.writeString(v);
        }
        try self.new_algo.serialize(w);
        try self.new_secure_algo.serialize(w);
        try w.writeBytes(self.secure_random);
        if (self.pending_reset_date) |v| {
            try w.writeInt(v);
        }
        if (self.login_email_pattern) |v| {
            try w.writeString(v);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.has_recovery = (flags >> 0) & 1 != 0;
        self.has_secure_values = (flags >> 1) & 1 != 0;
        self.has_password = (flags >> 2) & 1 != 0;
        if ((flags >> 2) & 1 != 0) {
            self.current_algo = try api.PasswordKdfAlgo.deserialize(allocator, r);
        } else {
            self.current_algo = null;
        }
        if ((flags >> 2) & 1 != 0) {
            self.srp_B = try r.readBytes();
        } else {
            self.srp_B = null;
        }
        if ((flags >> 2) & 1 != 0) {
            self.srp_id = try r.readLong();
        } else {
            self.srp_id = null;
        }
        if ((flags >> 3) & 1 != 0) {
            self.hint = try r.readString();
        } else {
            self.hint = null;
        }
        if ((flags >> 4) & 1 != 0) {
            self.email_unconfirmed_pattern = try r.readString();
        } else {
            self.email_unconfirmed_pattern = null;
        }
        self.new_algo = try api.PasswordKdfAlgo.deserialize(allocator, r);
        self.new_secure_algo = try api.SecurePasswordKdfAlgo.deserialize(allocator, r);
        self.secure_random = try r.readBytes();
        if ((flags >> 5) & 1 != 0) {
            self.pending_reset_date = try r.readInt();
        } else {
            self.pending_reset_date = null;
        }
        if ((flags >> 6) & 1 != 0) {
            self.login_email_pattern = try r.readString();
        } else {
            self.login_email_pattern = null;
        }
        return self;
    }
};

pub const passwordSettings = struct {
    pub const constructor_id: u32 = 0x9a5c33e5;
    email: ?[]const u8 = null,
    secure_settings: ?api.SecureSecretSettings = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.email != null) flags |= 1 << 0;
        if (self.secure_settings != null) flags |= 1 << 1;
        try w.writeUInt(flags);
        if (self.email) |v| {
            try w.writeString(v);
        }
        if (self.secure_settings) |v| {
            try v.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        if ((flags >> 0) & 1 != 0) {
            self.email = try r.readString();
        } else {
            self.email = null;
        }
        if ((flags >> 1) & 1 != 0) {
            self.secure_settings = try api.SecureSecretSettings.deserialize(allocator, r);
        } else {
            self.secure_settings = null;
        }
        return self;
    }
};

pub const passwordInputSettings = struct {
    pub const constructor_id: u32 = 0xc23727c9;
    new_algo: ?api.PasswordKdfAlgo = null,
    new_password_hash: ?[]const u8 = null,
    hint: ?[]const u8 = null,
    email: ?[]const u8 = null,
    new_secure_settings: ?api.SecureSecretSettings = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.new_algo != null) flags |= 1 << 0;
        if (self.new_password_hash != null) flags |= 1 << 0;
        if (self.hint != null) flags |= 1 << 0;
        if (self.email != null) flags |= 1 << 1;
        if (self.new_secure_settings != null) flags |= 1 << 2;
        try w.writeUInt(flags);
        if (self.new_algo) |v| {
            try v.serialize(w);
        }
        if (self.new_password_hash) |v| {
            try w.writeBytes(v);
        }
        if (self.hint) |v| {
            try w.writeString(v);
        }
        if (self.email) |v| {
            try w.writeString(v);
        }
        if (self.new_secure_settings) |v| {
            try v.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        if ((flags >> 0) & 1 != 0) {
            self.new_algo = try api.PasswordKdfAlgo.deserialize(allocator, r);
        } else {
            self.new_algo = null;
        }
        if ((flags >> 0) & 1 != 0) {
            self.new_password_hash = try r.readBytes();
        } else {
            self.new_password_hash = null;
        }
        if ((flags >> 0) & 1 != 0) {
            self.hint = try r.readString();
        } else {
            self.hint = null;
        }
        if ((flags >> 1) & 1 != 0) {
            self.email = try r.readString();
        } else {
            self.email = null;
        }
        if ((flags >> 2) & 1 != 0) {
            self.new_secure_settings = try api.SecureSecretSettings.deserialize(allocator, r);
        } else {
            self.new_secure_settings = null;
        }
        return self;
    }
};

pub const tmpPassword = struct {
    pub const constructor_id: u32 = 0xdb64fd34;
    tmp_password: []const u8,
    valid_until: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeBytes(self.tmp_password);
        try w.writeInt(self.valid_until);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.tmp_password = try r.readBytes();
        self.valid_until = try r.readInt();
        _ = allocator;
        return self;
    }
};

pub const webAuthorizations = struct {
    pub const constructor_id: u32 = 0xed56c9fc;
    authorizations: []api.WebAuthorization,
    users: []api.User,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.authorizations.len);
        for (self.authorizations) |elem_1427| {
            try elem_1427.serialize(w);
        }
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.users.len);
        for (self.users) |elem_1428| {
            try elem_1428.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_1429 = try r.readVectorLength();
        const vec_1430 = allocator.alloc(api.WebAuthorization, len_1429) catch return error.OutOfMemory;
        for (vec_1430) |*elem_1431| {
            elem_1431.* = try api.WebAuthorization.deserialize(allocator, r);
        }
        self.authorizations = vec_1430;
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_1432 = try r.readVectorLength();
        const vec_1433 = allocator.alloc(api.User, len_1432) catch return error.OutOfMemory;
        for (vec_1433) |*elem_1434| {
            elem_1434.* = try api.User.deserialize(allocator, r);
        }
        self.users = vec_1433;
        return self;
    }
};

pub const authorizationForm = struct {
    pub const constructor_id: u32 = 0xad2e1cd8;
    required_types: []*api.SecureRequiredType,
    values: []api.SecureValue,
    errors: []api.SecureValueError,
    users: []api.User,
    privacy_policy_url: ?[]const u8 = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.privacy_policy_url != null) flags |= 1 << 0;
        try w.writeUInt(flags);
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.required_types.len);
        for (self.required_types) |elem_1435| {
            try elem_1435.serialize(w);
        }
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.values.len);
        for (self.values) |elem_1436| {
            try elem_1436.serialize(w);
        }
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.errors.len);
        for (self.errors) |elem_1437| {
            try elem_1437.serialize(w);
        }
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.users.len);
        for (self.users) |elem_1438| {
            try elem_1438.serialize(w);
        }
        if (self.privacy_policy_url) |v| {
            try w.writeString(v);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_1439 = try r.readVectorLength();
        const vec_1440 = allocator.alloc(*api.SecureRequiredType, len_1439) catch return error.OutOfMemory;
        for (vec_1440) |*elem_1441| {
                        const ptr_1442 = allocator.create(api.SecureRequiredType) catch return error.OutOfMemory;
            ptr_1442.* = try api.SecureRequiredType.deserialize(allocator, r);
            elem_1441.* = ptr_1442;
        }
        self.required_types = vec_1440;
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_1443 = try r.readVectorLength();
        const vec_1444 = allocator.alloc(api.SecureValue, len_1443) catch return error.OutOfMemory;
        for (vec_1444) |*elem_1445| {
            elem_1445.* = try api.SecureValue.deserialize(allocator, r);
        }
        self.values = vec_1444;
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_1446 = try r.readVectorLength();
        const vec_1447 = allocator.alloc(api.SecureValueError, len_1446) catch return error.OutOfMemory;
        for (vec_1447) |*elem_1448| {
            elem_1448.* = try api.SecureValueError.deserialize(allocator, r);
        }
        self.errors = vec_1447;
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_1449 = try r.readVectorLength();
        const vec_1450 = allocator.alloc(api.User, len_1449) catch return error.OutOfMemory;
        for (vec_1450) |*elem_1451| {
            elem_1451.* = try api.User.deserialize(allocator, r);
        }
        self.users = vec_1450;
        if ((flags >> 0) & 1 != 0) {
            self.privacy_policy_url = try r.readString();
        } else {
            self.privacy_policy_url = null;
        }
        return self;
    }
};

pub const sentEmailCode = struct {
    pub const constructor_id: u32 = 0x811f854f;
    email_pattern: []const u8,
    length: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeString(self.email_pattern);
        try w.writeInt(self.length);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.email_pattern = try r.readString();
        self.length = try r.readInt();
        _ = allocator;
        return self;
    }
};

pub const takeout = struct {
    pub const constructor_id: u32 = 0x4dba4501;
    id: i64,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeLong(self.id);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.id = try r.readLong();
        _ = allocator;
        return self;
    }
};

pub const wallPapersNotModified = struct {
    pub const constructor_id: u32 = 0x1c199183;

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        _ = self;
        _ = w;
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        _ = allocator;
        _ = r;
        return .{};
    }
};

pub const wallPapers = struct {
    pub const constructor_id: u32 = 0xcdc3858c;
    hash: i64,
    wallpapers: []api.WallPaper,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeLong(self.hash);
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.wallpapers.len);
        for (self.wallpapers) |elem_1452| {
            try elem_1452.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.hash = try r.readLong();
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_1453 = try r.readVectorLength();
        const vec_1454 = allocator.alloc(api.WallPaper, len_1453) catch return error.OutOfMemory;
        for (vec_1454) |*elem_1455| {
            elem_1455.* = try api.WallPaper.deserialize(allocator, r);
        }
        self.wallpapers = vec_1454;
        return self;
    }
};

pub const autoDownloadSettings = struct {
    pub const constructor_id: u32 = 0xbaa57628;
    disabled: bool = false,
    video_preload_large: bool = false,
    audio_preload_next: bool = false,
    phonecalls_less_data: bool = false,
    stories_preload: bool = false,
    photo_size_max: i32,
    video_size_max: i64,
    file_size_max: i64,
    video_upload_maxbitrate: i32,
    small_queue_active_operations_max: i32,
    large_queue_active_operations_max: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.disabled) flags |= 1 << 0;
        if (self.video_preload_large) flags |= 1 << 1;
        if (self.audio_preload_next) flags |= 1 << 2;
        if (self.phonecalls_less_data) flags |= 1 << 3;
        if (self.stories_preload) flags |= 1 << 4;
        try w.writeUInt(flags);
        try w.writeInt(self.photo_size_max);
        try w.writeLong(self.video_size_max);
        try w.writeLong(self.file_size_max);
        try w.writeInt(self.video_upload_maxbitrate);
        try w.writeInt(self.small_queue_active_operations_max);
        try w.writeInt(self.large_queue_active_operations_max);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.disabled = (flags >> 0) & 1 != 0;
        self.video_preload_large = (flags >> 1) & 1 != 0;
        self.audio_preload_next = (flags >> 2) & 1 != 0;
        self.phonecalls_less_data = (flags >> 3) & 1 != 0;
        self.stories_preload = (flags >> 4) & 1 != 0;
        self.photo_size_max = try r.readInt();
        self.video_size_max = try r.readLong();
        self.file_size_max = try r.readLong();
        self.video_upload_maxbitrate = try r.readInt();
        self.small_queue_active_operations_max = try r.readInt();
        self.large_queue_active_operations_max = try r.readInt();
        _ = allocator;
        return self;
    }
};

pub const autoDownloadSettings_ = struct {
    pub const constructor_id: u32 = 0x63cacf26;
    low: api.AutoDownloadSettings,
    medium: api.AutoDownloadSettings,
    high: api.AutoDownloadSettings,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try self.low.serialize(w);
        try self.medium.serialize(w);
        try self.high.serialize(w);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.low = try api.AutoDownloadSettings.deserialize(allocator, r);
        self.medium = try api.AutoDownloadSettings.deserialize(allocator, r);
        self.high = try api.AutoDownloadSettings.deserialize(allocator, r);
        return self;
    }
};

pub const themesNotModified = struct {
    pub const constructor_id: u32 = 0xf41eb622;

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        _ = self;
        _ = w;
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        _ = allocator;
        _ = r;
        return .{};
    }
};

pub const themes = struct {
    pub const constructor_id: u32 = 0x9a3d8c6d;
    hash: i64,
    themes: []api.Theme,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeLong(self.hash);
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.themes.len);
        for (self.themes) |elem_1456| {
            try elem_1456.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.hash = try r.readLong();
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_1457 = try r.readVectorLength();
        const vec_1458 = allocator.alloc(api.Theme, len_1457) catch return error.OutOfMemory;
        for (vec_1458) |*elem_1459| {
            elem_1459.* = try api.Theme.deserialize(allocator, r);
        }
        self.themes = vec_1458;
        return self;
    }
};

pub const contentSettings = struct {
    pub const constructor_id: u32 = 0x57e28221;
    sensitive_enabled: bool = false,
    sensitive_can_change: bool = false,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.sensitive_enabled) flags |= 1 << 0;
        if (self.sensitive_can_change) flags |= 1 << 1;
        try w.writeUInt(flags);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.sensitive_enabled = (flags >> 0) & 1 != 0;
        self.sensitive_can_change = (flags >> 1) & 1 != 0;
        _ = allocator;
        return self;
    }
};

pub const resetPasswordFailedWait = struct {
    pub const constructor_id: u32 = 0xe3779861;
    retry_date: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeInt(self.retry_date);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.retry_date = try r.readInt();
        _ = allocator;
        return self;
    }
};

pub const resetPasswordRequestedWait = struct {
    pub const constructor_id: u32 = 0xe9effc7d;
    until_date: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeInt(self.until_date);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.until_date = try r.readInt();
        _ = allocator;
        return self;
    }
};

pub const resetPasswordOk = struct {
    pub const constructor_id: u32 = 0xe926d63e;

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        _ = self;
        _ = w;
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        _ = allocator;
        _ = r;
        return .{};
    }
};

pub const chatThemesNotModified = struct {
    pub const constructor_id: u32 = 0xe011e1c4;

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        _ = self;
        _ = w;
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        _ = allocator;
        _ = r;
        return .{};
    }
};

pub const chatThemes = struct {
    pub const constructor_id: u32 = 0xbe098173;
    hash: i64,
    themes: []api.ChatTheme,
    chats: []api.Chat,
    users: []api.User,
    next_offset: ?[]const u8 = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.next_offset != null) flags |= 1 << 0;
        try w.writeUInt(flags);
        try w.writeLong(self.hash);
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.themes.len);
        for (self.themes) |elem_1460| {
            try elem_1460.serialize(w);
        }
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.chats.len);
        for (self.chats) |elem_1461| {
            try elem_1461.serialize(w);
        }
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.users.len);
        for (self.users) |elem_1462| {
            try elem_1462.serialize(w);
        }
        if (self.next_offset) |v| {
            try w.writeString(v);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.hash = try r.readLong();
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_1463 = try r.readVectorLength();
        const vec_1464 = allocator.alloc(api.ChatTheme, len_1463) catch return error.OutOfMemory;
        for (vec_1464) |*elem_1465| {
            elem_1465.* = try api.ChatTheme.deserialize(allocator, r);
        }
        self.themes = vec_1464;
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_1466 = try r.readVectorLength();
        const vec_1467 = allocator.alloc(api.Chat, len_1466) catch return error.OutOfMemory;
        for (vec_1467) |*elem_1468| {
            elem_1468.* = try api.Chat.deserialize(allocator, r);
        }
        self.chats = vec_1467;
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_1469 = try r.readVectorLength();
        const vec_1470 = allocator.alloc(api.User, len_1469) catch return error.OutOfMemory;
        for (vec_1470) |*elem_1471| {
            elem_1471.* = try api.User.deserialize(allocator, r);
        }
        self.users = vec_1470;
        if ((flags >> 0) & 1 != 0) {
            self.next_offset = try r.readString();
        } else {
            self.next_offset = null;
        }
        return self;
    }
};

pub const savedRingtonesNotModified = struct {
    pub const constructor_id: u32 = 0xfbf6e8b1;

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        _ = self;
        _ = w;
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        _ = allocator;
        _ = r;
        return .{};
    }
};

pub const savedRingtones = struct {
    pub const constructor_id: u32 = 0xc1e92cc5;
    hash: i64,
    ringtones: []api.Document,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeLong(self.hash);
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.ringtones.len);
        for (self.ringtones) |elem_1472| {
            try elem_1472.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.hash = try r.readLong();
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_1473 = try r.readVectorLength();
        const vec_1474 = allocator.alloc(api.Document, len_1473) catch return error.OutOfMemory;
        for (vec_1474) |*elem_1475| {
            elem_1475.* = try api.Document.deserialize(allocator, r);
        }
        self.ringtones = vec_1474;
        return self;
    }
};

pub const savedRingtone = struct {
    pub const constructor_id: u32 = 0xb7263f6d;

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        _ = self;
        _ = w;
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        _ = allocator;
        _ = r;
        return .{};
    }
};

pub const savedRingtoneConverted = struct {
    pub const constructor_id: u32 = 0x1f307eb7;
    document: api.Document,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try self.document.serialize(w);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.document = try api.Document.deserialize(allocator, r);
        return self;
    }
};

pub const emojiStatusesNotModified = struct {
    pub const constructor_id: u32 = 0xd08ce645;

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        _ = self;
        _ = w;
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        _ = allocator;
        _ = r;
        return .{};
    }
};

pub const emojiStatuses = struct {
    pub const constructor_id: u32 = 0x90c467d1;
    hash: i64,
    statuses: []api.EmojiStatus,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeLong(self.hash);
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.statuses.len);
        for (self.statuses) |elem_1476| {
            try elem_1476.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.hash = try r.readLong();
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_1477 = try r.readVectorLength();
        const vec_1478 = allocator.alloc(api.EmojiStatus, len_1477) catch return error.OutOfMemory;
        for (vec_1478) |*elem_1479| {
            elem_1479.* = try api.EmojiStatus.deserialize(allocator, r);
        }
        self.statuses = vec_1478;
        return self;
    }
};

pub const emailVerified = struct {
    pub const constructor_id: u32 = 0x2b96cd1b;
    email: []const u8,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeString(self.email);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.email = try r.readString();
        _ = allocator;
        return self;
    }
};

pub const emailVerifiedLogin = struct {
    pub const constructor_id: u32 = 0xe1bb0d61;
    email: []const u8,
    sent_code: api.auth.SentCode,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeString(self.email);
        try self.sent_code.serialize(w);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.email = try r.readString();
        self.sent_code = try api.auth.SentCode.deserialize(allocator, r);
        return self;
    }
};

pub const autoSaveSettings = struct {
    pub const constructor_id: u32 = 0xc84834ce;
    photos: bool = false,
    videos: bool = false,
    video_max_size: ?i64 = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.photos) flags |= 1 << 0;
        if (self.videos) flags |= 1 << 1;
        if (self.video_max_size != null) flags |= 1 << 2;
        try w.writeUInt(flags);
        if (self.video_max_size) |v| {
            try w.writeLong(v);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.photos = (flags >> 0) & 1 != 0;
        self.videos = (flags >> 1) & 1 != 0;
        if ((flags >> 2) & 1 != 0) {
            self.video_max_size = try r.readLong();
        } else {
            self.video_max_size = null;
        }
        _ = allocator;
        return self;
    }
};

pub const autoSaveException = struct {
    pub const constructor_id: u32 = 0x81602d47;
    peer: api.Peer,
    settings: api.AutoSaveSettings,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try self.peer.serialize(w);
        try self.settings.serialize(w);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.peer = try api.Peer.deserialize(allocator, r);
        self.settings = try api.AutoSaveSettings.deserialize(allocator, r);
        return self;
    }
};

pub const autoSaveSettings_ = struct {
    pub const constructor_id: u32 = 0x4c3e069d;
    users_settings: api.AutoSaveSettings,
    chats_settings: api.AutoSaveSettings,
    broadcasts_settings: api.AutoSaveSettings,
    exceptions: []api.AutoSaveException,
    chats: []api.Chat,
    users: []api.User,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try self.users_settings.serialize(w);
        try self.chats_settings.serialize(w);
        try self.broadcasts_settings.serialize(w);
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.exceptions.len);
        for (self.exceptions) |elem_1480| {
            try elem_1480.serialize(w);
        }
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.chats.len);
        for (self.chats) |elem_1481| {
            try elem_1481.serialize(w);
        }
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.users.len);
        for (self.users) |elem_1482| {
            try elem_1482.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.users_settings = try api.AutoSaveSettings.deserialize(allocator, r);
        self.chats_settings = try api.AutoSaveSettings.deserialize(allocator, r);
        self.broadcasts_settings = try api.AutoSaveSettings.deserialize(allocator, r);
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_1483 = try r.readVectorLength();
        const vec_1484 = allocator.alloc(api.AutoSaveException, len_1483) catch return error.OutOfMemory;
        for (vec_1484) |*elem_1485| {
            elem_1485.* = try api.AutoSaveException.deserialize(allocator, r);
        }
        self.exceptions = vec_1484;
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_1486 = try r.readVectorLength();
        const vec_1487 = allocator.alloc(api.Chat, len_1486) catch return error.OutOfMemory;
        for (vec_1487) |*elem_1488| {
            elem_1488.* = try api.Chat.deserialize(allocator, r);
        }
        self.chats = vec_1487;
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_1489 = try r.readVectorLength();
        const vec_1490 = allocator.alloc(api.User, len_1489) catch return error.OutOfMemory;
        for (vec_1490) |*elem_1491| {
            elem_1491.* = try api.User.deserialize(allocator, r);
        }
        self.users = vec_1490;
        return self;
    }
};

pub const connectedBots = struct {
    pub const constructor_id: u32 = 0x17d7f87b;
    connected_bots: []api.ConnectedBot,
    users: []api.User,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.connected_bots.len);
        for (self.connected_bots) |elem_1492| {
            try elem_1492.serialize(w);
        }
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.users.len);
        for (self.users) |elem_1493| {
            try elem_1493.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_1494 = try r.readVectorLength();
        const vec_1495 = allocator.alloc(api.ConnectedBot, len_1494) catch return error.OutOfMemory;
        for (vec_1495) |*elem_1496| {
            elem_1496.* = try api.ConnectedBot.deserialize(allocator, r);
        }
        self.connected_bots = vec_1495;
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_1497 = try r.readVectorLength();
        const vec_1498 = allocator.alloc(api.User, len_1497) catch return error.OutOfMemory;
        for (vec_1498) |*elem_1499| {
            elem_1499.* = try api.User.deserialize(allocator, r);
        }
        self.users = vec_1498;
        return self;
    }
};

pub const businessChatLinks = struct {
    pub const constructor_id: u32 = 0xec43a2d1;
    links: []api.BusinessChatLink,
    chats: []api.Chat,
    users: []api.User,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.links.len);
        for (self.links) |elem_1500| {
            try elem_1500.serialize(w);
        }
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.chats.len);
        for (self.chats) |elem_1501| {
            try elem_1501.serialize(w);
        }
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.users.len);
        for (self.users) |elem_1502| {
            try elem_1502.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_1503 = try r.readVectorLength();
        const vec_1504 = allocator.alloc(api.BusinessChatLink, len_1503) catch return error.OutOfMemory;
        for (vec_1504) |*elem_1505| {
            elem_1505.* = try api.BusinessChatLink.deserialize(allocator, r);
        }
        self.links = vec_1504;
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_1506 = try r.readVectorLength();
        const vec_1507 = allocator.alloc(api.Chat, len_1506) catch return error.OutOfMemory;
        for (vec_1507) |*elem_1508| {
            elem_1508.* = try api.Chat.deserialize(allocator, r);
        }
        self.chats = vec_1507;
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_1509 = try r.readVectorLength();
        const vec_1510 = allocator.alloc(api.User, len_1509) catch return error.OutOfMemory;
        for (vec_1510) |*elem_1511| {
            elem_1511.* = try api.User.deserialize(allocator, r);
        }
        self.users = vec_1510;
        return self;
    }
};

pub const resolvedBusinessChatLinks = struct {
    pub const constructor_id: u32 = 0x9a23af21;
    peer: api.Peer,
    message: []const u8,
    entities: ?[]api.MessageEntity = null,
    chats: []api.Chat,
    users: []api.User,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.entities != null) flags |= 1 << 0;
        try w.writeUInt(flags);
        try self.peer.serialize(w);
        try w.writeString(self.message);
        if (self.entities) |v| {
            try w.writeUInt(vector_constructor_id);
            try w.writeVectorLength(v.len);
            for (v) |elem_1512| {
                try elem_1512.serialize(w);
            }
        }
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.chats.len);
        for (self.chats) |elem_1513| {
            try elem_1513.serialize(w);
        }
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.users.len);
        for (self.users) |elem_1514| {
            try elem_1514.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.peer = try api.Peer.deserialize(allocator, r);
        self.message = try r.readString();
        if ((flags >> 0) & 1 != 0) {
            if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
            const len_1515 = try r.readVectorLength();
            const vec_1516 = allocator.alloc(api.MessageEntity, len_1515) catch return error.OutOfMemory;
            for (vec_1516) |*elem_1517| {
                elem_1517.* = try api.MessageEntity.deserialize(allocator, r);
            }
            self.entities = vec_1516;
        } else {
            self.entities = null;
        }
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_1518 = try r.readVectorLength();
        const vec_1519 = allocator.alloc(api.Chat, len_1518) catch return error.OutOfMemory;
        for (vec_1519) |*elem_1520| {
            elem_1520.* = try api.Chat.deserialize(allocator, r);
        }
        self.chats = vec_1519;
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_1521 = try r.readVectorLength();
        const vec_1522 = allocator.alloc(api.User, len_1521) catch return error.OutOfMemory;
        for (vec_1522) |*elem_1523| {
            elem_1523.* = try api.User.deserialize(allocator, r);
        }
        self.users = vec_1522;
        return self;
    }
};

pub const paidMessagesRevenue = struct {
    pub const constructor_id: u32 = 0x1e109708;
    stars_amount: i64,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeLong(self.stars_amount);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.stars_amount = try r.readLong();
        _ = allocator;
        return self;
    }
};

pub const savedMusicIdsNotModified = struct {
    pub const constructor_id: u32 = 0x4fc81d6e;

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        _ = self;
        _ = w;
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        _ = allocator;
        _ = r;
        return .{};
    }
};

pub const savedMusicIds = struct {
    pub const constructor_id: u32 = 0x998d6636;
    ids: []i64,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.ids.len);
        for (self.ids) |elem_1524| {
            try w.writeLong(elem_1524);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_1525 = try r.readVectorLength();
        const vec_1526 = allocator.alloc(i64, len_1525) catch return error.OutOfMemory;
        for (vec_1526) |*elem_1527| {
            elem_1527.* = try r.readLong();
        }
        self.ids = vec_1526;
        return self;
    }
};

pub const passkeys = struct {
    pub const constructor_id: u32 = 0xf8e0aa1c;
    passkeys: []api.Passkey,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.passkeys.len);
        for (self.passkeys) |elem_1528| {
            try elem_1528.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_1529 = try r.readVectorLength();
        const vec_1530 = allocator.alloc(api.Passkey, len_1529) catch return error.OutOfMemory;
        for (vec_1530) |*elem_1531| {
            elem_1531.* = try api.Passkey.deserialize(allocator, r);
        }
        self.passkeys = vec_1530;
        return self;
    }
};

pub const passkeyRegistrationOptions = struct {
    pub const constructor_id: u32 = 0xe16b5ce1;
    options: api.DataJSON,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try self.options.serialize(w);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.options = try api.DataJSON.deserialize(allocator, r);
        return self;
    }
};

pub const webBrowserSettingsNotModified = struct {
    pub const constructor_id: u32 = 0xc31c8f4e;

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        _ = self;
        _ = w;
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        _ = allocator;
        _ = r;
        return .{};
    }
};

pub const webBrowserSettings = struct {
    pub const constructor_id: u32 = 0x79eb8cb3;
    open_external_browser: bool = false,
    display_close_button: bool = false,
    external_exceptions: []api.WebDomainException,
    inapp_exceptions: []api.WebDomainException,
    hash: i64,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.open_external_browser) flags |= 1 << 0;
        if (self.display_close_button) flags |= 1 << 1;
        try w.writeUInt(flags);
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.external_exceptions.len);
        for (self.external_exceptions) |elem_1532| {
            try elem_1532.serialize(w);
        }
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.inapp_exceptions.len);
        for (self.inapp_exceptions) |elem_1533| {
            try elem_1533.serialize(w);
        }
        try w.writeLong(self.hash);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.open_external_browser = (flags >> 0) & 1 != 0;
        self.display_close_button = (flags >> 1) & 1 != 0;
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_1534 = try r.readVectorLength();
        const vec_1535 = allocator.alloc(api.WebDomainException, len_1534) catch return error.OutOfMemory;
        for (vec_1535) |*elem_1536| {
            elem_1536.* = try api.WebDomainException.deserialize(allocator, r);
        }
        self.external_exceptions = vec_1535;
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_1537 = try r.readVectorLength();
        const vec_1538 = allocator.alloc(api.WebDomainException, len_1537) catch return error.OutOfMemory;
        for (vec_1538) |*elem_1539| {
            elem_1539.* = try api.WebDomainException.deserialize(allocator, r);
        }
        self.inapp_exceptions = vec_1538;
        self.hash = try r.readLong();
        return self;
    }
};

pub const PrivacyRules = union(enum) {
    privacyRules: api.account.privacyRules,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.account.privacyRules.constructor_id => return .{ .privacyRules = try api.account.privacyRules.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const AccountDaysTTL = union(enum) {
    accountDaysTTL: api.accountDaysTTL,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.accountDaysTTL.constructor_id => return .{ .accountDaysTTL = try api.accountDaysTTL.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const Authorizations = union(enum) {
    authorizations: api.account.authorizations,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.account.authorizations.constructor_id => return .{ .authorizations = try api.account.authorizations.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const Password = union(enum) {
    password: api.account.password,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.account.password.constructor_id => return .{ .password = try api.account.password.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const PasswordSettings = union(enum) {
    passwordSettings: api.account.passwordSettings,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.account.passwordSettings.constructor_id => return .{ .passwordSettings = try api.account.passwordSettings.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const PasswordInputSettings = union(enum) {
    passwordInputSettings: api.account.passwordInputSettings,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.account.passwordInputSettings.constructor_id => return .{ .passwordInputSettings = try api.account.passwordInputSettings.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const TmpPassword = union(enum) {
    tmpPassword: api.account.tmpPassword,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.account.tmpPassword.constructor_id => return .{ .tmpPassword = try api.account.tmpPassword.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const WebAuthorizations = union(enum) {
    webAuthorizations: api.account.webAuthorizations,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.account.webAuthorizations.constructor_id => return .{ .webAuthorizations = try api.account.webAuthorizations.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const AuthorizationForm = union(enum) {
    authorizationForm: api.account.authorizationForm,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.account.authorizationForm.constructor_id => return .{ .authorizationForm = try api.account.authorizationForm.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const SentEmailCode = union(enum) {
    sentEmailCode: api.account.sentEmailCode,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.account.sentEmailCode.constructor_id => return .{ .sentEmailCode = try api.account.sentEmailCode.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const Takeout = union(enum) {
    takeout: api.account.takeout,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.account.takeout.constructor_id => return .{ .takeout = try api.account.takeout.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const WallPapers = union(enum) {
    wallPapersNotModified: api.account.wallPapersNotModified,
    wallPapers: api.account.wallPapers,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.account.wallPapersNotModified.constructor_id => return .{ .wallPapersNotModified = try api.account.wallPapersNotModified.deserialize(allocator, r) },
            api.account.wallPapers.constructor_id => return .{ .wallPapers = try api.account.wallPapers.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const AutoDownloadSettings = union(enum) {
    autoDownloadSettings: api.autoDownloadSettings,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.autoDownloadSettings.constructor_id => return .{ .autoDownloadSettings = try api.autoDownloadSettings.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const AutoDownloadSettings_ = union(enum) {
    autoDownloadSettings: api.account.autoDownloadSettings,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.account.autoDownloadSettings.constructor_id => return .{ .autoDownloadSettings = try api.account.autoDownloadSettings.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const Themes = union(enum) {
    themesNotModified: api.account.themesNotModified,
    themes: api.account.themes,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.account.themesNotModified.constructor_id => return .{ .themesNotModified = try api.account.themesNotModified.deserialize(allocator, r) },
            api.account.themes.constructor_id => return .{ .themes = try api.account.themes.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const ContentSettings = union(enum) {
    contentSettings: api.account.contentSettings,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.account.contentSettings.constructor_id => return .{ .contentSettings = try api.account.contentSettings.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const ResetPasswordResult = union(enum) {
    resetPasswordFailedWait: api.account.resetPasswordFailedWait,
    resetPasswordRequestedWait: api.account.resetPasswordRequestedWait,
    resetPasswordOk: api.account.resetPasswordOk,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.account.resetPasswordFailedWait.constructor_id => return .{ .resetPasswordFailedWait = try api.account.resetPasswordFailedWait.deserialize(allocator, r) },
            api.account.resetPasswordRequestedWait.constructor_id => return .{ .resetPasswordRequestedWait = try api.account.resetPasswordRequestedWait.deserialize(allocator, r) },
            api.account.resetPasswordOk.constructor_id => return .{ .resetPasswordOk = try api.account.resetPasswordOk.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const ChatThemes = union(enum) {
    chatThemesNotModified: api.account.chatThemesNotModified,
    chatThemes: api.account.chatThemes,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.account.chatThemesNotModified.constructor_id => return .{ .chatThemesNotModified = try api.account.chatThemesNotModified.deserialize(allocator, r) },
            api.account.chatThemes.constructor_id => return .{ .chatThemes = try api.account.chatThemes.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const SavedRingtones = union(enum) {
    savedRingtonesNotModified: api.account.savedRingtonesNotModified,
    savedRingtones: api.account.savedRingtones,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.account.savedRingtonesNotModified.constructor_id => return .{ .savedRingtonesNotModified = try api.account.savedRingtonesNotModified.deserialize(allocator, r) },
            api.account.savedRingtones.constructor_id => return .{ .savedRingtones = try api.account.savedRingtones.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const SavedRingtone = union(enum) {
    savedRingtone: api.account.savedRingtone,
    savedRingtoneConverted: api.account.savedRingtoneConverted,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.account.savedRingtone.constructor_id => return .{ .savedRingtone = try api.account.savedRingtone.deserialize(allocator, r) },
            api.account.savedRingtoneConverted.constructor_id => return .{ .savedRingtoneConverted = try api.account.savedRingtoneConverted.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const EmojiStatuses = union(enum) {
    emojiStatusesNotModified: api.account.emojiStatusesNotModified,
    emojiStatuses: api.account.emojiStatuses,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.account.emojiStatusesNotModified.constructor_id => return .{ .emojiStatusesNotModified = try api.account.emojiStatusesNotModified.deserialize(allocator, r) },
            api.account.emojiStatuses.constructor_id => return .{ .emojiStatuses = try api.account.emojiStatuses.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const EmailVerified = union(enum) {
    emailVerified: api.account.emailVerified,
    emailVerifiedLogin: api.account.emailVerifiedLogin,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.account.emailVerified.constructor_id => return .{ .emailVerified = try api.account.emailVerified.deserialize(allocator, r) },
            api.account.emailVerifiedLogin.constructor_id => return .{ .emailVerifiedLogin = try api.account.emailVerifiedLogin.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const AutoSaveSettings = union(enum) {
    autoSaveSettings: api.autoSaveSettings,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.autoSaveSettings.constructor_id => return .{ .autoSaveSettings = try api.autoSaveSettings.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const AutoSaveException = union(enum) {
    autoSaveException: api.autoSaveException,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.autoSaveException.constructor_id => return .{ .autoSaveException = try api.autoSaveException.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const AutoSaveSettings_ = union(enum) {
    autoSaveSettings: api.account.autoSaveSettings,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.account.autoSaveSettings.constructor_id => return .{ .autoSaveSettings = try api.account.autoSaveSettings.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const ConnectedBots = union(enum) {
    connectedBots: api.account.connectedBots,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.account.connectedBots.constructor_id => return .{ .connectedBots = try api.account.connectedBots.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const BusinessChatLinks = union(enum) {
    businessChatLinks: api.account.businessChatLinks,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.account.businessChatLinks.constructor_id => return .{ .businessChatLinks = try api.account.businessChatLinks.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const ResolvedBusinessChatLinks = union(enum) {
    resolvedBusinessChatLinks: api.account.resolvedBusinessChatLinks,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.account.resolvedBusinessChatLinks.constructor_id => return .{ .resolvedBusinessChatLinks = try api.account.resolvedBusinessChatLinks.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const PaidMessagesRevenue = union(enum) {
    paidMessagesRevenue: api.account.paidMessagesRevenue,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.account.paidMessagesRevenue.constructor_id => return .{ .paidMessagesRevenue = try api.account.paidMessagesRevenue.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const SavedMusicIds = union(enum) {
    savedMusicIdsNotModified: api.account.savedMusicIdsNotModified,
    savedMusicIds: api.account.savedMusicIds,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.account.savedMusicIdsNotModified.constructor_id => return .{ .savedMusicIdsNotModified = try api.account.savedMusicIdsNotModified.deserialize(allocator, r) },
            api.account.savedMusicIds.constructor_id => return .{ .savedMusicIds = try api.account.savedMusicIds.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const Passkeys = union(enum) {
    passkeys: api.account.passkeys,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.account.passkeys.constructor_id => return .{ .passkeys = try api.account.passkeys.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const PasskeyRegistrationOptions = union(enum) {
    passkeyRegistrationOptions: api.account.passkeyRegistrationOptions,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.account.passkeyRegistrationOptions.constructor_id => return .{ .passkeyRegistrationOptions = try api.account.passkeyRegistrationOptions.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const WebBrowserSettings = union(enum) {
    webBrowserSettingsNotModified: api.account.webBrowserSettingsNotModified,
    webBrowserSettings: api.account.webBrowserSettings,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.account.webBrowserSettingsNotModified.constructor_id => return .{ .webBrowserSettingsNotModified = try api.account.webBrowserSettingsNotModified.deserialize(allocator, r) },
            api.account.webBrowserSettings.constructor_id => return .{ .webBrowserSettings = try api.account.webBrowserSettings.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};


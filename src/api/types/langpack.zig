//! GENERATED by td TL code generator from "api.tl".
//! Do not edit by hand; regenerate with td-gen.

const std = @import("std");
const td = @import("../../root.zig");
const Writer = td.tl.Writer;
const Reader = td.tl.Reader;
const TlError = td.TlError;
const vector_constructor_id = td.tl.vector_constructor_id;

const api = @import("../mod.zig");

pub const langPackString = struct {
    pub const constructor_id: u32 = 0xcad181f6;
    key: []const u8,
    value: []const u8,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeString(self.key);
        try w.writeString(self.value);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.key = try r.readString();
        self.value = try r.readString();
        _ = allocator;
        return self;
    }
};

pub const langPackStringPluralized = struct {
    pub const constructor_id: u32 = 0x6c47ac9f;
    key: []const u8,
    zero_value: ?[]const u8 = null,
    one_value: ?[]const u8 = null,
    two_value: ?[]const u8 = null,
    few_value: ?[]const u8 = null,
    many_value: ?[]const u8 = null,
    other_value: []const u8,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.zero_value != null) flags |= 1 << 0;
        if (self.one_value != null) flags |= 1 << 1;
        if (self.two_value != null) flags |= 1 << 2;
        if (self.few_value != null) flags |= 1 << 3;
        if (self.many_value != null) flags |= 1 << 4;
        try w.writeUInt(flags);
        try w.writeString(self.key);
        if (self.zero_value) |v| {
            try w.writeString(v);
        }
        if (self.one_value) |v| {
            try w.writeString(v);
        }
        if (self.two_value) |v| {
            try w.writeString(v);
        }
        if (self.few_value) |v| {
            try w.writeString(v);
        }
        if (self.many_value) |v| {
            try w.writeString(v);
        }
        try w.writeString(self.other_value);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.key = try r.readString();
        if ((flags >> 0) & 1 != 0) {
            self.zero_value = try r.readString();
        } else {
            self.zero_value = null;
        }
        if ((flags >> 1) & 1 != 0) {
            self.one_value = try r.readString();
        } else {
            self.one_value = null;
        }
        if ((flags >> 2) & 1 != 0) {
            self.two_value = try r.readString();
        } else {
            self.two_value = null;
        }
        if ((flags >> 3) & 1 != 0) {
            self.few_value = try r.readString();
        } else {
            self.few_value = null;
        }
        if ((flags >> 4) & 1 != 0) {
            self.many_value = try r.readString();
        } else {
            self.many_value = null;
        }
        self.other_value = try r.readString();
        _ = allocator;
        return self;
    }
};

pub const langPackStringDeleted = struct {
    pub const constructor_id: u32 = 0x2979eeb2;
    key: []const u8,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeString(self.key);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.key = try r.readString();
        _ = allocator;
        return self;
    }
};

pub const langPackDifference = struct {
    pub const constructor_id: u32 = 0xf385c1f6;
    lang_code: []const u8,
    from_version: i32,
    version: i32,
    strings: []api.LangPackString,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeString(self.lang_code);
        try w.writeInt(self.from_version);
        try w.writeInt(self.version);
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.strings.len);
        for (self.strings) |elem_2198| {
            try elem_2198.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.lang_code = try r.readString();
        self.from_version = try r.readInt();
        self.version = try r.readInt();
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_2199 = try r.readVectorLength();
        const vec_2200 = allocator.alloc(api.LangPackString, len_2199) catch return error.OutOfMemory;
        for (vec_2200) |*elem_2201| {
            elem_2201.* = try api.LangPackString.deserialize(allocator, r);
        }
        self.strings = vec_2200;
        return self;
    }
};

pub const langPackLanguage = struct {
    pub const constructor_id: u32 = 0xeeca5ce3;
    official: bool = false,
    rtl: bool = false,
    beta: bool = false,
    name: []const u8,
    native_name: []const u8,
    lang_code: []const u8,
    base_lang_code: ?[]const u8 = null,
    plural_code: []const u8,
    strings_count: i32,
    translated_count: i32,
    translations_url: []const u8,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.official) flags |= 1 << 0;
        if (self.rtl) flags |= 1 << 2;
        if (self.beta) flags |= 1 << 3;
        if (self.base_lang_code != null) flags |= 1 << 1;
        try w.writeUInt(flags);
        try w.writeString(self.name);
        try w.writeString(self.native_name);
        try w.writeString(self.lang_code);
        if (self.base_lang_code) |v| {
            try w.writeString(v);
        }
        try w.writeString(self.plural_code);
        try w.writeInt(self.strings_count);
        try w.writeInt(self.translated_count);
        try w.writeString(self.translations_url);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.official = (flags >> 0) & 1 != 0;
        self.rtl = (flags >> 2) & 1 != 0;
        self.beta = (flags >> 3) & 1 != 0;
        self.name = try r.readString();
        self.native_name = try r.readString();
        self.lang_code = try r.readString();
        if ((flags >> 1) & 1 != 0) {
            self.base_lang_code = try r.readString();
        } else {
            self.base_lang_code = null;
        }
        self.plural_code = try r.readString();
        self.strings_count = try r.readInt();
        self.translated_count = try r.readInt();
        self.translations_url = try r.readString();
        _ = allocator;
        return self;
    }
};

pub const LangPackString = union(enum) {
    langPackString: api.langPackString,
    langPackStringPluralized: api.langPackStringPluralized,
    langPackStringDeleted: api.langPackStringDeleted,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.langPackString.constructor_id => return .{ .langPackString = try api.langPackString.deserialize(allocator, r) },
            api.langPackStringPluralized.constructor_id => return .{ .langPackStringPluralized = try api.langPackStringPluralized.deserialize(allocator, r) },
            api.langPackStringDeleted.constructor_id => return .{ .langPackStringDeleted = try api.langPackStringDeleted.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const LangPackDifference = union(enum) {
    langPackDifference: api.langPackDifference,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.langPackDifference.constructor_id => return .{ .langPackDifference = try api.langPackDifference.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const LangPackLanguage = union(enum) {
    langPackLanguage: api.langPackLanguage,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.langPackLanguage.constructor_id => return .{ .langPackLanguage = try api.langPackLanguage.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};


//! GENERATED by td TL code generator from "api.tl".
//! Do not edit by hand; regenerate with td-gen.

const std = @import("std");
const td = @import("../../root.zig");
const Writer = td.tl.Writer;
const Reader = td.tl.Reader;
const TlError = td.TlError;
const vector_constructor_id = td.tl.vector_constructor_id;

const api = @import("../mod.zig");

pub const exportedChatlistInvite = struct {
    pub const constructor_id: u32 = 0x10e6e3a6;
    filter: api.DialogFilter,
    invite: api.ExportedChatlistInvite,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try self.filter.serialize(w);
        try self.invite.serialize(w);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.filter = try api.DialogFilter.deserialize(allocator, r);
        self.invite = try api.ExportedChatlistInvite.deserialize(allocator, r);
        return self;
    }
};

pub const exportedInvites = struct {
    pub const constructor_id: u32 = 0x10ab6dc7;
    invites: []api.ExportedChatlistInvite,
    chats: []api.Chat,
    users: []api.User,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.invites.len);
        for (self.invites) |elem_2320| {
            try elem_2320.serialize(w);
        }
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.chats.len);
        for (self.chats) |elem_2321| {
            try elem_2321.serialize(w);
        }
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.users.len);
        for (self.users) |elem_2322| {
            try elem_2322.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_2323 = try r.readVectorLength();
        const vec_2324 = allocator.alloc(api.ExportedChatlistInvite, len_2323) catch return error.OutOfMemory;
        for (vec_2324) |*elem_2325| {
            elem_2325.* = try api.ExportedChatlistInvite.deserialize(allocator, r);
        }
        self.invites = vec_2324;
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_2326 = try r.readVectorLength();
        const vec_2327 = allocator.alloc(api.Chat, len_2326) catch return error.OutOfMemory;
        for (vec_2327) |*elem_2328| {
            elem_2328.* = try api.Chat.deserialize(allocator, r);
        }
        self.chats = vec_2327;
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_2329 = try r.readVectorLength();
        const vec_2330 = allocator.alloc(api.User, len_2329) catch return error.OutOfMemory;
        for (vec_2330) |*elem_2331| {
            elem_2331.* = try api.User.deserialize(allocator, r);
        }
        self.users = vec_2330;
        return self;
    }
};

pub const chatlistInviteAlready = struct {
    pub const constructor_id: u32 = 0xfa87f659;
    filter_id: i32,
    missing_peers: []api.Peer,
    already_peers: []api.Peer,
    chats: []api.Chat,
    users: []api.User,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeInt(self.filter_id);
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.missing_peers.len);
        for (self.missing_peers) |elem_2332| {
            try elem_2332.serialize(w);
        }
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.already_peers.len);
        for (self.already_peers) |elem_2333| {
            try elem_2333.serialize(w);
        }
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.chats.len);
        for (self.chats) |elem_2334| {
            try elem_2334.serialize(w);
        }
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.users.len);
        for (self.users) |elem_2335| {
            try elem_2335.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.filter_id = try r.readInt();
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_2336 = try r.readVectorLength();
        const vec_2337 = allocator.alloc(api.Peer, len_2336) catch return error.OutOfMemory;
        for (vec_2337) |*elem_2338| {
            elem_2338.* = try api.Peer.deserialize(allocator, r);
        }
        self.missing_peers = vec_2337;
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_2339 = try r.readVectorLength();
        const vec_2340 = allocator.alloc(api.Peer, len_2339) catch return error.OutOfMemory;
        for (vec_2340) |*elem_2341| {
            elem_2341.* = try api.Peer.deserialize(allocator, r);
        }
        self.already_peers = vec_2340;
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_2342 = try r.readVectorLength();
        const vec_2343 = allocator.alloc(api.Chat, len_2342) catch return error.OutOfMemory;
        for (vec_2343) |*elem_2344| {
            elem_2344.* = try api.Chat.deserialize(allocator, r);
        }
        self.chats = vec_2343;
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_2345 = try r.readVectorLength();
        const vec_2346 = allocator.alloc(api.User, len_2345) catch return error.OutOfMemory;
        for (vec_2346) |*elem_2347| {
            elem_2347.* = try api.User.deserialize(allocator, r);
        }
        self.users = vec_2346;
        return self;
    }
};

pub const chatlistInvite = struct {
    pub const constructor_id: u32 = 0xf10ece2f;
    title_noanimate: bool = false,
    title: api.TextWithEntities,
    emoticon: ?[]const u8 = null,
    peers: []api.Peer,
    chats: []api.Chat,
    users: []api.User,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.title_noanimate) flags |= 1 << 1;
        if (self.emoticon != null) flags |= 1 << 0;
        try w.writeUInt(flags);
        try self.title.serialize(w);
        if (self.emoticon) |v| {
            try w.writeString(v);
        }
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.peers.len);
        for (self.peers) |elem_2348| {
            try elem_2348.serialize(w);
        }
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.chats.len);
        for (self.chats) |elem_2349| {
            try elem_2349.serialize(w);
        }
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.users.len);
        for (self.users) |elem_2350| {
            try elem_2350.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.title_noanimate = (flags >> 1) & 1 != 0;
        self.title = try api.TextWithEntities.deserialize(allocator, r);
        if ((flags >> 0) & 1 != 0) {
            self.emoticon = try r.readString();
        } else {
            self.emoticon = null;
        }
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_2351 = try r.readVectorLength();
        const vec_2352 = allocator.alloc(api.Peer, len_2351) catch return error.OutOfMemory;
        for (vec_2352) |*elem_2353| {
            elem_2353.* = try api.Peer.deserialize(allocator, r);
        }
        self.peers = vec_2352;
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_2354 = try r.readVectorLength();
        const vec_2355 = allocator.alloc(api.Chat, len_2354) catch return error.OutOfMemory;
        for (vec_2355) |*elem_2356| {
            elem_2356.* = try api.Chat.deserialize(allocator, r);
        }
        self.chats = vec_2355;
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_2357 = try r.readVectorLength();
        const vec_2358 = allocator.alloc(api.User, len_2357) catch return error.OutOfMemory;
        for (vec_2358) |*elem_2359| {
            elem_2359.* = try api.User.deserialize(allocator, r);
        }
        self.users = vec_2358;
        return self;
    }
};

pub const chatlistUpdates = struct {
    pub const constructor_id: u32 = 0x93bd878d;
    missing_peers: []api.Peer,
    chats: []api.Chat,
    users: []api.User,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.missing_peers.len);
        for (self.missing_peers) |elem_2360| {
            try elem_2360.serialize(w);
        }
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.chats.len);
        for (self.chats) |elem_2361| {
            try elem_2361.serialize(w);
        }
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.users.len);
        for (self.users) |elem_2362| {
            try elem_2362.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_2363 = try r.readVectorLength();
        const vec_2364 = allocator.alloc(api.Peer, len_2363) catch return error.OutOfMemory;
        for (vec_2364) |*elem_2365| {
            elem_2365.* = try api.Peer.deserialize(allocator, r);
        }
        self.missing_peers = vec_2364;
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_2366 = try r.readVectorLength();
        const vec_2367 = allocator.alloc(api.Chat, len_2366) catch return error.OutOfMemory;
        for (vec_2367) |*elem_2368| {
            elem_2368.* = try api.Chat.deserialize(allocator, r);
        }
        self.chats = vec_2367;
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_2369 = try r.readVectorLength();
        const vec_2370 = allocator.alloc(api.User, len_2369) catch return error.OutOfMemory;
        for (vec_2370) |*elem_2371| {
            elem_2371.* = try api.User.deserialize(allocator, r);
        }
        self.users = vec_2370;
        return self;
    }
};

pub const ExportedChatlistInvite_ = union(enum) {
    exportedChatlistInvite: api.chatlists.exportedChatlistInvite,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.chatlists.exportedChatlistInvite.constructor_id => return .{ .exportedChatlistInvite = try api.chatlists.exportedChatlistInvite.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const ExportedInvites = union(enum) {
    exportedInvites: api.chatlists.exportedInvites,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.chatlists.exportedInvites.constructor_id => return .{ .exportedInvites = try api.chatlists.exportedInvites.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const ChatlistInvite = union(enum) {
    chatlistInviteAlready: api.chatlists.chatlistInviteAlready,
    chatlistInvite: api.chatlists.chatlistInvite,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.chatlists.chatlistInviteAlready.constructor_id => return .{ .chatlistInviteAlready = try api.chatlists.chatlistInviteAlready.deserialize(allocator, r) },
            api.chatlists.chatlistInvite.constructor_id => return .{ .chatlistInvite = try api.chatlists.chatlistInvite.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const ChatlistUpdates = union(enum) {
    chatlistUpdates: api.chatlists.chatlistUpdates,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.chatlists.chatlistUpdates.constructor_id => return .{ .chatlistUpdates = try api.chatlists.chatlistUpdates.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};


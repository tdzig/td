//! GENERATED by td TL code generator from "api.tl".
//! Do not edit by hand; regenerate with td-gen.

const std = @import("std");
const td = @import("../../root.zig");
const Writer = td.tl.Writer;
const Reader = td.tl.Reader;
const TlError = td.TlError;
const vector_constructor_id = td.tl.vector_constructor_id;

const api = @import("../mod.zig");

pub const wallPaper = struct {
    pub const constructor_id: u32 = 0xa437c3ed;
    id: i64,
    creator: bool = false,
    default: bool = false,
    pattern: bool = false,
    dark: bool = false,
    access_hash: i64,
    slug: []const u8,
    document: api.Document,
    settings: ?api.WallPaperSettings = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.creator) flags |= 1 << 0;
        if (self.default) flags |= 1 << 1;
        if (self.pattern) flags |= 1 << 3;
        if (self.dark) flags |= 1 << 4;
        if (self.settings != null) flags |= 1 << 2;
        try w.writeLong(self.id);
        try w.writeUInt(flags);
        try w.writeLong(self.access_hash);
        try w.writeString(self.slug);
        try self.document.serialize(w);
        if (self.settings) |v| {
            try v.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.id = try r.readLong();
        const flags = try r.readUInt();
        self.creator = (flags >> 0) & 1 != 0;
        self.default = (flags >> 1) & 1 != 0;
        self.pattern = (flags >> 3) & 1 != 0;
        self.dark = (flags >> 4) & 1 != 0;
        self.access_hash = try r.readLong();
        self.slug = try r.readString();
        self.document = try api.Document.deserialize(allocator, r);
        if ((flags >> 2) & 1 != 0) {
            self.settings = try api.WallPaperSettings.deserialize(allocator, r);
        } else {
            self.settings = null;
        }
        return self;
    }
};

pub const wallPaperNoFile = struct {
    pub const constructor_id: u32 = 0xe0804116;
    id: i64,
    default: bool = false,
    dark: bool = false,
    settings: ?api.WallPaperSettings = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.default) flags |= 1 << 1;
        if (self.dark) flags |= 1 << 4;
        if (self.settings != null) flags |= 1 << 2;
        try w.writeLong(self.id);
        try w.writeUInt(flags);
        if (self.settings) |v| {
            try v.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.id = try r.readLong();
        const flags = try r.readUInt();
        self.default = (flags >> 1) & 1 != 0;
        self.dark = (flags >> 4) & 1 != 0;
        if ((flags >> 2) & 1 != 0) {
            self.settings = try api.WallPaperSettings.deserialize(allocator, r);
        } else {
            self.settings = null;
        }
        return self;
    }
};

pub const inputWallPaper = struct {
    pub const constructor_id: u32 = 0xe630b979;
    id: i64,
    access_hash: i64,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeLong(self.id);
        try w.writeLong(self.access_hash);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.id = try r.readLong();
        self.access_hash = try r.readLong();
        _ = allocator;
        return self;
    }
};

pub const inputWallPaperSlug = struct {
    pub const constructor_id: u32 = 0x72091c80;
    slug: []const u8,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeString(self.slug);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.slug = try r.readString();
        _ = allocator;
        return self;
    }
};

pub const inputWallPaperNoFile = struct {
    pub const constructor_id: u32 = 0x967a462e;
    id: i64,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeLong(self.id);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.id = try r.readLong();
        _ = allocator;
        return self;
    }
};

pub const wallPaperSettings = struct {
    pub const constructor_id: u32 = 0x372efcd0;
    blur: bool = false,
    motion: bool = false,
    background_color: ?i32 = null,
    second_background_color: ?i32 = null,
    third_background_color: ?i32 = null,
    fourth_background_color: ?i32 = null,
    intensity: ?i32 = null,
    rotation: ?i32 = null,
    emoticon: ?[]const u8 = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.blur) flags |= 1 << 1;
        if (self.motion) flags |= 1 << 2;
        if (self.background_color != null) flags |= 1 << 0;
        if (self.second_background_color != null) flags |= 1 << 4;
        if (self.third_background_color != null) flags |= 1 << 5;
        if (self.fourth_background_color != null) flags |= 1 << 6;
        if (self.intensity != null) flags |= 1 << 3;
        if (self.rotation != null) flags |= 1 << 4;
        if (self.emoticon != null) flags |= 1 << 7;
        try w.writeUInt(flags);
        if (self.background_color) |v| {
            try w.writeInt(v);
        }
        if (self.second_background_color) |v| {
            try w.writeInt(v);
        }
        if (self.third_background_color) |v| {
            try w.writeInt(v);
        }
        if (self.fourth_background_color) |v| {
            try w.writeInt(v);
        }
        if (self.intensity) |v| {
            try w.writeInt(v);
        }
        if (self.rotation) |v| {
            try w.writeInt(v);
        }
        if (self.emoticon) |v| {
            try w.writeString(v);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.blur = (flags >> 1) & 1 != 0;
        self.motion = (flags >> 2) & 1 != 0;
        if ((flags >> 0) & 1 != 0) {
            self.background_color = try r.readInt();
        } else {
            self.background_color = null;
        }
        if ((flags >> 4) & 1 != 0) {
            self.second_background_color = try r.readInt();
        } else {
            self.second_background_color = null;
        }
        if ((flags >> 5) & 1 != 0) {
            self.third_background_color = try r.readInt();
        } else {
            self.third_background_color = null;
        }
        if ((flags >> 6) & 1 != 0) {
            self.fourth_background_color = try r.readInt();
        } else {
            self.fourth_background_color = null;
        }
        if ((flags >> 3) & 1 != 0) {
            self.intensity = try r.readInt();
        } else {
            self.intensity = null;
        }
        if ((flags >> 4) & 1 != 0) {
            self.rotation = try r.readInt();
        } else {
            self.rotation = null;
        }
        if ((flags >> 7) & 1 != 0) {
            self.emoticon = try r.readString();
        } else {
            self.emoticon = null;
        }
        _ = allocator;
        return self;
    }
};

pub const inputTheme = struct {
    pub const constructor_id: u32 = 0x3c5693e9;
    id: i64,
    access_hash: i64,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeLong(self.id);
        try w.writeLong(self.access_hash);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.id = try r.readLong();
        self.access_hash = try r.readLong();
        _ = allocator;
        return self;
    }
};

pub const inputThemeSlug = struct {
    pub const constructor_id: u32 = 0xf5890df1;
    slug: []const u8,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeString(self.slug);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.slug = try r.readString();
        _ = allocator;
        return self;
    }
};

pub const theme = struct {
    pub const constructor_id: u32 = 0xa00e67d6;
    creator: bool = false,
    default: bool = false,
    for_chat: bool = false,
    id: i64,
    access_hash: i64,
    slug: []const u8,
    title: []const u8,
    document: ?api.Document = null,
    settings: ?[]api.ThemeSettings = null,
    emoticon: ?[]const u8 = null,
    installs_count: ?i32 = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.creator) flags |= 1 << 0;
        if (self.default) flags |= 1 << 1;
        if (self.for_chat) flags |= 1 << 5;
        if (self.document != null) flags |= 1 << 2;
        if (self.settings != null) flags |= 1 << 3;
        if (self.emoticon != null) flags |= 1 << 6;
        if (self.installs_count != null) flags |= 1 << 4;
        try w.writeUInt(flags);
        try w.writeLong(self.id);
        try w.writeLong(self.access_hash);
        try w.writeString(self.slug);
        try w.writeString(self.title);
        if (self.document) |v| {
            try v.serialize(w);
        }
        if (self.settings) |v| {
            try w.writeUInt(vector_constructor_id);
            try w.writeVectorLength(v.len);
            for (v) |elem_1071| {
                try elem_1071.serialize(w);
            }
        }
        if (self.emoticon) |v| {
            try w.writeString(v);
        }
        if (self.installs_count) |v| {
            try w.writeInt(v);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.creator = (flags >> 0) & 1 != 0;
        self.default = (flags >> 1) & 1 != 0;
        self.for_chat = (flags >> 5) & 1 != 0;
        self.id = try r.readLong();
        self.access_hash = try r.readLong();
        self.slug = try r.readString();
        self.title = try r.readString();
        if ((flags >> 2) & 1 != 0) {
            self.document = try api.Document.deserialize(allocator, r);
        } else {
            self.document = null;
        }
        if ((flags >> 3) & 1 != 0) {
            if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
            const len_1072 = try r.readVectorLength();
            const vec_1073 = allocator.alloc(api.ThemeSettings, len_1072) catch return error.OutOfMemory;
            for (vec_1073) |*elem_1074| {
                elem_1074.* = try api.ThemeSettings.deserialize(allocator, r);
            }
            self.settings = vec_1073;
        } else {
            self.settings = null;
        }
        if ((flags >> 6) & 1 != 0) {
            self.emoticon = try r.readString();
        } else {
            self.emoticon = null;
        }
        if ((flags >> 4) & 1 != 0) {
            self.installs_count = try r.readInt();
        } else {
            self.installs_count = null;
        }
        return self;
    }
};

pub const baseThemeClassic = struct {
    pub const constructor_id: u32 = 0xc3a12462;

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        _ = self;
        _ = w;
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        _ = allocator;
        _ = r;
        return .{};
    }
};

pub const baseThemeDay = struct {
    pub const constructor_id: u32 = 0xfbd81688;

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        _ = self;
        _ = w;
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        _ = allocator;
        _ = r;
        return .{};
    }
};

pub const baseThemeNight = struct {
    pub const constructor_id: u32 = 0xb7b31ea8;

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        _ = self;
        _ = w;
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        _ = allocator;
        _ = r;
        return .{};
    }
};

pub const baseThemeTinted = struct {
    pub const constructor_id: u32 = 0x6d5f77ee;

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        _ = self;
        _ = w;
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        _ = allocator;
        _ = r;
        return .{};
    }
};

pub const baseThemeArctic = struct {
    pub const constructor_id: u32 = 0x5b11125a;

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        _ = self;
        _ = w;
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        _ = allocator;
        _ = r;
        return .{};
    }
};

pub const inputThemeSettings = struct {
    pub const constructor_id: u32 = 0x8fde504f;
    message_colors_animated: bool = false,
    base_theme: api.BaseTheme,
    accent_color: i32,
    outbox_accent_color: ?i32 = null,
    message_colors: ?[]i32 = null,
    wallpaper: ?api.InputWallPaper = null,
    wallpaper_settings: ?api.WallPaperSettings = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.message_colors_animated) flags |= 1 << 2;
        if (self.outbox_accent_color != null) flags |= 1 << 3;
        if (self.message_colors != null) flags |= 1 << 0;
        if (self.wallpaper != null) flags |= 1 << 1;
        if (self.wallpaper_settings != null) flags |= 1 << 1;
        try w.writeUInt(flags);
        try self.base_theme.serialize(w);
        try w.writeInt(self.accent_color);
        if (self.outbox_accent_color) |v| {
            try w.writeInt(v);
        }
        if (self.message_colors) |v| {
            try w.writeUInt(vector_constructor_id);
            try w.writeVectorLength(v.len);
            for (v) |elem_1075| {
                try w.writeInt(elem_1075);
            }
        }
        if (self.wallpaper) |v| {
            try v.serialize(w);
        }
        if (self.wallpaper_settings) |v| {
            try v.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.message_colors_animated = (flags >> 2) & 1 != 0;
        self.base_theme = try api.BaseTheme.deserialize(allocator, r);
        self.accent_color = try r.readInt();
        if ((flags >> 3) & 1 != 0) {
            self.outbox_accent_color = try r.readInt();
        } else {
            self.outbox_accent_color = null;
        }
        if ((flags >> 0) & 1 != 0) {
            if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
            const len_1076 = try r.readVectorLength();
            const vec_1077 = allocator.alloc(i32, len_1076) catch return error.OutOfMemory;
            for (vec_1077) |*elem_1078| {
                elem_1078.* = try r.readInt();
            }
            self.message_colors = vec_1077;
        } else {
            self.message_colors = null;
        }
        if ((flags >> 1) & 1 != 0) {
            self.wallpaper = try api.InputWallPaper.deserialize(allocator, r);
        } else {
            self.wallpaper = null;
        }
        if ((flags >> 1) & 1 != 0) {
            self.wallpaper_settings = try api.WallPaperSettings.deserialize(allocator, r);
        } else {
            self.wallpaper_settings = null;
        }
        return self;
    }
};

pub const themeSettings = struct {
    pub const constructor_id: u32 = 0xfa58b6d4;
    message_colors_animated: bool = false,
    base_theme: api.BaseTheme,
    accent_color: i32,
    outbox_accent_color: ?i32 = null,
    message_colors: ?[]i32 = null,
    wallpaper: ?api.WallPaper = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.message_colors_animated) flags |= 1 << 2;
        if (self.outbox_accent_color != null) flags |= 1 << 3;
        if (self.message_colors != null) flags |= 1 << 0;
        if (self.wallpaper != null) flags |= 1 << 1;
        try w.writeUInt(flags);
        try self.base_theme.serialize(w);
        try w.writeInt(self.accent_color);
        if (self.outbox_accent_color) |v| {
            try w.writeInt(v);
        }
        if (self.message_colors) |v| {
            try w.writeUInt(vector_constructor_id);
            try w.writeVectorLength(v.len);
            for (v) |elem_1079| {
                try w.writeInt(elem_1079);
            }
        }
        if (self.wallpaper) |v| {
            try v.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.message_colors_animated = (flags >> 2) & 1 != 0;
        self.base_theme = try api.BaseTheme.deserialize(allocator, r);
        self.accent_color = try r.readInt();
        if ((flags >> 3) & 1 != 0) {
            self.outbox_accent_color = try r.readInt();
        } else {
            self.outbox_accent_color = null;
        }
        if ((flags >> 0) & 1 != 0) {
            if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
            const len_1080 = try r.readVectorLength();
            const vec_1081 = allocator.alloc(i32, len_1080) catch return error.OutOfMemory;
            for (vec_1081) |*elem_1082| {
                elem_1082.* = try r.readInt();
            }
            self.message_colors = vec_1081;
        } else {
            self.message_colors = null;
        }
        if ((flags >> 1) & 1 != 0) {
            self.wallpaper = try api.WallPaper.deserialize(allocator, r);
        } else {
            self.wallpaper = null;
        }
        return self;
    }
};

pub const chatTheme = struct {
    pub const constructor_id: u32 = 0xc3dffc04;
    emoticon: []const u8,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeString(self.emoticon);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.emoticon = try r.readString();
        _ = allocator;
        return self;
    }
};

pub const chatThemeUniqueGift = struct {
    pub const constructor_id: u32 = 0x3458f9c8;
    gift: api.StarGift,
    theme_settings: []api.ThemeSettings,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try self.gift.serialize(w);
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.theme_settings.len);
        for (self.theme_settings) |elem_1083| {
            try elem_1083.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.gift = try api.StarGift.deserialize(allocator, r);
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_1084 = try r.readVectorLength();
        const vec_1085 = allocator.alloc(api.ThemeSettings, len_1084) catch return error.OutOfMemory;
        for (vec_1085) |*elem_1086| {
            elem_1086.* = try api.ThemeSettings.deserialize(allocator, r);
        }
        self.theme_settings = vec_1085;
        return self;
    }
};

pub const inputChatThemeEmpty = struct {
    pub const constructor_id: u32 = 0x83268483;

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        _ = self;
        _ = w;
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        _ = allocator;
        _ = r;
        return .{};
    }
};

pub const inputChatTheme = struct {
    pub const constructor_id: u32 = 0xc93de95c;
    emoticon: []const u8,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeString(self.emoticon);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.emoticon = try r.readString();
        _ = allocator;
        return self;
    }
};

pub const inputChatThemeUniqueGift = struct {
    pub const constructor_id: u32 = 0x87e5dfe4;
    slug: []const u8,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeString(self.slug);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.slug = try r.readString();
        _ = allocator;
        return self;
    }
};

pub const WallPaper = union(enum) {
    wallPaper: api.wallPaper,
    wallPaperNoFile: api.wallPaperNoFile,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.wallPaper.constructor_id => return .{ .wallPaper = try api.wallPaper.deserialize(allocator, r) },
            api.wallPaperNoFile.constructor_id => return .{ .wallPaperNoFile = try api.wallPaperNoFile.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const InputWallPaper = union(enum) {
    inputWallPaper: api.inputWallPaper,
    inputWallPaperSlug: api.inputWallPaperSlug,
    inputWallPaperNoFile: api.inputWallPaperNoFile,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.inputWallPaper.constructor_id => return .{ .inputWallPaper = try api.inputWallPaper.deserialize(allocator, r) },
            api.inputWallPaperSlug.constructor_id => return .{ .inputWallPaperSlug = try api.inputWallPaperSlug.deserialize(allocator, r) },
            api.inputWallPaperNoFile.constructor_id => return .{ .inputWallPaperNoFile = try api.inputWallPaperNoFile.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const WallPaperSettings = union(enum) {
    wallPaperSettings: api.wallPaperSettings,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.wallPaperSettings.constructor_id => return .{ .wallPaperSettings = try api.wallPaperSettings.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const InputTheme = union(enum) {
    inputTheme: api.inputTheme,
    inputThemeSlug: api.inputThemeSlug,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.inputTheme.constructor_id => return .{ .inputTheme = try api.inputTheme.deserialize(allocator, r) },
            api.inputThemeSlug.constructor_id => return .{ .inputThemeSlug = try api.inputThemeSlug.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const Theme = union(enum) {
    theme: api.theme,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.theme.constructor_id => return .{ .theme = try api.theme.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const BaseTheme = union(enum) {
    baseThemeClassic: api.baseThemeClassic,
    baseThemeDay: api.baseThemeDay,
    baseThemeNight: api.baseThemeNight,
    baseThemeTinted: api.baseThemeTinted,
    baseThemeArctic: api.baseThemeArctic,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.baseThemeClassic.constructor_id => return .{ .baseThemeClassic = try api.baseThemeClassic.deserialize(allocator, r) },
            api.baseThemeDay.constructor_id => return .{ .baseThemeDay = try api.baseThemeDay.deserialize(allocator, r) },
            api.baseThemeNight.constructor_id => return .{ .baseThemeNight = try api.baseThemeNight.deserialize(allocator, r) },
            api.baseThemeTinted.constructor_id => return .{ .baseThemeTinted = try api.baseThemeTinted.deserialize(allocator, r) },
            api.baseThemeArctic.constructor_id => return .{ .baseThemeArctic = try api.baseThemeArctic.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const InputThemeSettings = union(enum) {
    inputThemeSettings: api.inputThemeSettings,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.inputThemeSettings.constructor_id => return .{ .inputThemeSettings = try api.inputThemeSettings.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const ThemeSettings = union(enum) {
    themeSettings: api.themeSettings,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.themeSettings.constructor_id => return .{ .themeSettings = try api.themeSettings.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const ChatTheme = union(enum) {
    chatTheme: api.chatTheme,
    chatThemeUniqueGift: api.chatThemeUniqueGift,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.chatTheme.constructor_id => return .{ .chatTheme = try api.chatTheme.deserialize(allocator, r) },
            api.chatThemeUniqueGift.constructor_id => return .{ .chatThemeUniqueGift = try api.chatThemeUniqueGift.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const InputChatTheme = union(enum) {
    inputChatThemeEmpty: api.inputChatThemeEmpty,
    inputChatTheme: api.inputChatTheme,
    inputChatThemeUniqueGift: api.inputChatThemeUniqueGift,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.inputChatThemeEmpty.constructor_id => return .{ .inputChatThemeEmpty = try api.inputChatThemeEmpty.deserialize(allocator, r) },
            api.inputChatTheme.constructor_id => return .{ .inputChatTheme = try api.inputChatTheme.deserialize(allocator, r) },
            api.inputChatThemeUniqueGift.constructor_id => return .{ .inputChatThemeUniqueGift = try api.inputChatThemeUniqueGift.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};


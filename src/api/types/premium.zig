//! GENERATED by td TL code generator from "api.tl".
//! Do not edit by hand; regenerate with td-gen.

const std = @import("std");
const td = @import("../../root.zig");
const Writer = td.tl.Writer;
const Reader = td.tl.Reader;
const TlError = td.TlError;
const vector_constructor_id = td.tl.vector_constructor_id;

const api = @import("../mod.zig");

pub const premiumSubscriptionOption = struct {
    pub const constructor_id: u32 = 0x5f2d1df2;
    current: bool = false,
    can_purchase_upgrade: bool = false,
    transaction: ?[]const u8 = null,
    months: i32,
    currency: []const u8,
    amount: i64,
    bot_url: []const u8,
    store_product: ?[]const u8 = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.current) flags |= 1 << 1;
        if (self.can_purchase_upgrade) flags |= 1 << 2;
        if (self.transaction != null) flags |= 1 << 3;
        if (self.store_product != null) flags |= 1 << 0;
        try w.writeUInt(flags);
        if (self.transaction) |v| {
            try w.writeString(v);
        }
        try w.writeInt(self.months);
        try w.writeString(self.currency);
        try w.writeLong(self.amount);
        try w.writeString(self.bot_url);
        if (self.store_product) |v| {
            try w.writeString(v);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.current = (flags >> 1) & 1 != 0;
        self.can_purchase_upgrade = (flags >> 2) & 1 != 0;
        if ((flags >> 3) & 1 != 0) {
            self.transaction = try r.readString();
        } else {
            self.transaction = null;
        }
        self.months = try r.readInt();
        self.currency = try r.readString();
        self.amount = try r.readLong();
        self.bot_url = try r.readString();
        if ((flags >> 0) & 1 != 0) {
            self.store_product = try r.readString();
        } else {
            self.store_product = null;
        }
        _ = allocator;
        return self;
    }
};

pub const premiumGiftCodeOption = struct {
    pub const constructor_id: u32 = 0x257e962b;
    users: i32,
    months: i32,
    store_product: ?[]const u8 = null,
    store_quantity: ?i32 = null,
    currency: []const u8,
    amount: i64,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.store_product != null) flags |= 1 << 0;
        if (self.store_quantity != null) flags |= 1 << 1;
        try w.writeUInt(flags);
        try w.writeInt(self.users);
        try w.writeInt(self.months);
        if (self.store_product) |v| {
            try w.writeString(v);
        }
        if (self.store_quantity) |v| {
            try w.writeInt(v);
        }
        try w.writeString(self.currency);
        try w.writeLong(self.amount);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.users = try r.readInt();
        self.months = try r.readInt();
        if ((flags >> 0) & 1 != 0) {
            self.store_product = try r.readString();
        } else {
            self.store_product = null;
        }
        if ((flags >> 1) & 1 != 0) {
            self.store_quantity = try r.readInt();
        } else {
            self.store_quantity = null;
        }
        self.currency = try r.readString();
        self.amount = try r.readLong();
        _ = allocator;
        return self;
    }
};

pub const boost = struct {
    pub const constructor_id: u32 = 0x4b3e14d6;
    gift: bool = false,
    giveaway: bool = false,
    unclaimed: bool = false,
    id: []const u8,
    user_id: ?i64 = null,
    giveaway_msg_id: ?i32 = null,
    date: i32,
    expires: i32,
    used_gift_slug: ?[]const u8 = null,
    multiplier: ?i32 = null,
    stars: ?i64 = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.gift) flags |= 1 << 1;
        if (self.giveaway) flags |= 1 << 2;
        if (self.unclaimed) flags |= 1 << 3;
        if (self.user_id != null) flags |= 1 << 0;
        if (self.giveaway_msg_id != null) flags |= 1 << 2;
        if (self.used_gift_slug != null) flags |= 1 << 4;
        if (self.multiplier != null) flags |= 1 << 5;
        if (self.stars != null) flags |= 1 << 6;
        try w.writeUInt(flags);
        try w.writeString(self.id);
        if (self.user_id) |v| {
            try w.writeLong(v);
        }
        if (self.giveaway_msg_id) |v| {
            try w.writeInt(v);
        }
        try w.writeInt(self.date);
        try w.writeInt(self.expires);
        if (self.used_gift_slug) |v| {
            try w.writeString(v);
        }
        if (self.multiplier) |v| {
            try w.writeInt(v);
        }
        if (self.stars) |v| {
            try w.writeLong(v);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.gift = (flags >> 1) & 1 != 0;
        self.giveaway = (flags >> 2) & 1 != 0;
        self.unclaimed = (flags >> 3) & 1 != 0;
        self.id = try r.readString();
        if ((flags >> 0) & 1 != 0) {
            self.user_id = try r.readLong();
        } else {
            self.user_id = null;
        }
        if ((flags >> 2) & 1 != 0) {
            self.giveaway_msg_id = try r.readInt();
        } else {
            self.giveaway_msg_id = null;
        }
        self.date = try r.readInt();
        self.expires = try r.readInt();
        if ((flags >> 4) & 1 != 0) {
            self.used_gift_slug = try r.readString();
        } else {
            self.used_gift_slug = null;
        }
        if ((flags >> 5) & 1 != 0) {
            self.multiplier = try r.readInt();
        } else {
            self.multiplier = null;
        }
        if ((flags >> 6) & 1 != 0) {
            self.stars = try r.readLong();
        } else {
            self.stars = null;
        }
        _ = allocator;
        return self;
    }
};

pub const boostsList = struct {
    pub const constructor_id: u32 = 0x86f8613c;
    count: i32,
    boosts: []api.Boost,
    next_offset: ?[]const u8 = null,
    users: []api.User,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.next_offset != null) flags |= 1 << 0;
        try w.writeUInt(flags);
        try w.writeInt(self.count);
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.boosts.len);
        for (self.boosts) |elem_2292| {
            try elem_2292.serialize(w);
        }
        if (self.next_offset) |v| {
            try w.writeString(v);
        }
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.users.len);
        for (self.users) |elem_2293| {
            try elem_2293.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.count = try r.readInt();
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_2294 = try r.readVectorLength();
        const vec_2295 = allocator.alloc(api.Boost, len_2294) catch return error.OutOfMemory;
        for (vec_2295) |*elem_2296| {
            elem_2296.* = try api.Boost.deserialize(allocator, r);
        }
        self.boosts = vec_2295;
        if ((flags >> 0) & 1 != 0) {
            self.next_offset = try r.readString();
        } else {
            self.next_offset = null;
        }
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_2297 = try r.readVectorLength();
        const vec_2298 = allocator.alloc(api.User, len_2297) catch return error.OutOfMemory;
        for (vec_2298) |*elem_2299| {
            elem_2299.* = try api.User.deserialize(allocator, r);
        }
        self.users = vec_2298;
        return self;
    }
};

pub const myBoost = struct {
    pub const constructor_id: u32 = 0xc448415c;
    slot: i32,
    peer: ?api.Peer = null,
    date: i32,
    expires: i32,
    cooldown_until_date: ?i32 = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.peer != null) flags |= 1 << 0;
        if (self.cooldown_until_date != null) flags |= 1 << 1;
        try w.writeUInt(flags);
        try w.writeInt(self.slot);
        if (self.peer) |v| {
            try v.serialize(w);
        }
        try w.writeInt(self.date);
        try w.writeInt(self.expires);
        if (self.cooldown_until_date) |v| {
            try w.writeInt(v);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.slot = try r.readInt();
        if ((flags >> 0) & 1 != 0) {
            self.peer = try api.Peer.deserialize(allocator, r);
        } else {
            self.peer = null;
        }
        self.date = try r.readInt();
        self.expires = try r.readInt();
        if ((flags >> 1) & 1 != 0) {
            self.cooldown_until_date = try r.readInt();
        } else {
            self.cooldown_until_date = null;
        }
        return self;
    }
};

pub const myBoosts = struct {
    pub const constructor_id: u32 = 0x9ae228e2;
    my_boosts: []api.MyBoost,
    chats: []api.Chat,
    users: []api.User,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.my_boosts.len);
        for (self.my_boosts) |elem_2300| {
            try elem_2300.serialize(w);
        }
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.chats.len);
        for (self.chats) |elem_2301| {
            try elem_2301.serialize(w);
        }
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.users.len);
        for (self.users) |elem_2302| {
            try elem_2302.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_2303 = try r.readVectorLength();
        const vec_2304 = allocator.alloc(api.MyBoost, len_2303) catch return error.OutOfMemory;
        for (vec_2304) |*elem_2305| {
            elem_2305.* = try api.MyBoost.deserialize(allocator, r);
        }
        self.my_boosts = vec_2304;
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_2306 = try r.readVectorLength();
        const vec_2307 = allocator.alloc(api.Chat, len_2306) catch return error.OutOfMemory;
        for (vec_2307) |*elem_2308| {
            elem_2308.* = try api.Chat.deserialize(allocator, r);
        }
        self.chats = vec_2307;
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_2309 = try r.readVectorLength();
        const vec_2310 = allocator.alloc(api.User, len_2309) catch return error.OutOfMemory;
        for (vec_2310) |*elem_2311| {
            elem_2311.* = try api.User.deserialize(allocator, r);
        }
        self.users = vec_2310;
        return self;
    }
};

pub const boostsStatus = struct {
    pub const constructor_id: u32 = 0x4959427a;
    my_boost: bool = false,
    level: i32,
    current_level_boosts: i32,
    boosts: i32,
    gift_boosts: ?i32 = null,
    next_level_boosts: ?i32 = null,
    premium_audience: ?api.StatsPercentValue = null,
    boost_url: []const u8,
    prepaid_giveaways: ?[]api.PrepaidGiveaway = null,
    my_boost_slots: ?[]i32 = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.my_boost) flags |= 1 << 2;
        if (self.gift_boosts != null) flags |= 1 << 4;
        if (self.next_level_boosts != null) flags |= 1 << 0;
        if (self.premium_audience != null) flags |= 1 << 1;
        if (self.prepaid_giveaways != null) flags |= 1 << 3;
        if (self.my_boost_slots != null) flags |= 1 << 2;
        try w.writeUInt(flags);
        try w.writeInt(self.level);
        try w.writeInt(self.current_level_boosts);
        try w.writeInt(self.boosts);
        if (self.gift_boosts) |v| {
            try w.writeInt(v);
        }
        if (self.next_level_boosts) |v| {
            try w.writeInt(v);
        }
        if (self.premium_audience) |v| {
            try v.serialize(w);
        }
        try w.writeString(self.boost_url);
        if (self.prepaid_giveaways) |v| {
            try w.writeUInt(vector_constructor_id);
            try w.writeVectorLength(v.len);
            for (v) |elem_2312| {
                try elem_2312.serialize(w);
            }
        }
        if (self.my_boost_slots) |v| {
            try w.writeUInt(vector_constructor_id);
            try w.writeVectorLength(v.len);
            for (v) |elem_2313| {
                try w.writeInt(elem_2313);
            }
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.my_boost = (flags >> 2) & 1 != 0;
        self.level = try r.readInt();
        self.current_level_boosts = try r.readInt();
        self.boosts = try r.readInt();
        if ((flags >> 4) & 1 != 0) {
            self.gift_boosts = try r.readInt();
        } else {
            self.gift_boosts = null;
        }
        if ((flags >> 0) & 1 != 0) {
            self.next_level_boosts = try r.readInt();
        } else {
            self.next_level_boosts = null;
        }
        if ((flags >> 1) & 1 != 0) {
            self.premium_audience = try api.StatsPercentValue.deserialize(allocator, r);
        } else {
            self.premium_audience = null;
        }
        self.boost_url = try r.readString();
        if ((flags >> 3) & 1 != 0) {
            if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
            const len_2314 = try r.readVectorLength();
            const vec_2315 = allocator.alloc(api.PrepaidGiveaway, len_2314) catch return error.OutOfMemory;
            for (vec_2315) |*elem_2316| {
                elem_2316.* = try api.PrepaidGiveaway.deserialize(allocator, r);
            }
            self.prepaid_giveaways = vec_2315;
        } else {
            self.prepaid_giveaways = null;
        }
        if ((flags >> 2) & 1 != 0) {
            if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
            const len_2317 = try r.readVectorLength();
            const vec_2318 = allocator.alloc(i32, len_2317) catch return error.OutOfMemory;
            for (vec_2318) |*elem_2319| {
                elem_2319.* = try r.readInt();
            }
            self.my_boost_slots = vec_2318;
        } else {
            self.my_boost_slots = null;
        }
        return self;
    }
};

pub const PremiumSubscriptionOption = union(enum) {
    premiumSubscriptionOption: api.premiumSubscriptionOption,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.premiumSubscriptionOption.constructor_id => return .{ .premiumSubscriptionOption = try api.premiumSubscriptionOption.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const PremiumGiftCodeOption = union(enum) {
    premiumGiftCodeOption: api.premiumGiftCodeOption,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.premiumGiftCodeOption.constructor_id => return .{ .premiumGiftCodeOption = try api.premiumGiftCodeOption.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const Boost = union(enum) {
    boost: api.boost,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.boost.constructor_id => return .{ .boost = try api.boost.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const BoostsList = union(enum) {
    boostsList: api.premium.boostsList,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.premium.boostsList.constructor_id => return .{ .boostsList = try api.premium.boostsList.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const MyBoost = union(enum) {
    myBoost: api.myBoost,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.myBoost.constructor_id => return .{ .myBoost = try api.myBoost.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const MyBoosts = union(enum) {
    myBoosts: api.premium.myBoosts,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.premium.myBoosts.constructor_id => return .{ .myBoosts = try api.premium.myBoosts.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const BoostsStatus = union(enum) {
    boostsStatus: api.premium.boostsStatus,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.premium.boostsStatus.constructor_id => return .{ .boostsStatus = try api.premium.boostsStatus.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};


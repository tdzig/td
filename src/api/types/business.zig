//! GENERATED by td TL code generator from "api.tl".
//! Do not edit by hand; regenerate with td-gen.

const std = @import("std");
const td = @import("../../root.zig");
const Writer = td.tl.Writer;
const Reader = td.tl.Reader;
const TlError = td.TlError;
const vector_constructor_id = td.tl.vector_constructor_id;

const api = @import("../mod.zig");

pub const businessWeeklyOpen = struct {
    pub const constructor_id: u32 = 0x120b1ab9;
    start_minute: i32,
    end_minute: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeInt(self.start_minute);
        try w.writeInt(self.end_minute);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.start_minute = try r.readInt();
        self.end_minute = try r.readInt();
        _ = allocator;
        return self;
    }
};

pub const businessWorkHours = struct {
    pub const constructor_id: u32 = 0x8c92b098;
    open_now: bool = false,
    timezone_id: []const u8,
    weekly_open: []api.BusinessWeeklyOpen,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.open_now) flags |= 1 << 0;
        try w.writeUInt(flags);
        try w.writeString(self.timezone_id);
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.weekly_open.len);
        for (self.weekly_open) |elem_2523| {
            try elem_2523.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.open_now = (flags >> 0) & 1 != 0;
        self.timezone_id = try r.readString();
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_2524 = try r.readVectorLength();
        const vec_2525 = allocator.alloc(api.BusinessWeeklyOpen, len_2524) catch return error.OutOfMemory;
        for (vec_2525) |*elem_2526| {
            elem_2526.* = try api.BusinessWeeklyOpen.deserialize(allocator, r);
        }
        self.weekly_open = vec_2525;
        return self;
    }
};

pub const businessLocation = struct {
    pub const constructor_id: u32 = 0xac5c1af7;
    geo_point: ?api.GeoPoint = null,
    address: []const u8,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.geo_point != null) flags |= 1 << 0;
        try w.writeUInt(flags);
        if (self.geo_point) |v| {
            try v.serialize(w);
        }
        try w.writeString(self.address);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        if ((flags >> 0) & 1 != 0) {
            self.geo_point = try api.GeoPoint.deserialize(allocator, r);
        } else {
            self.geo_point = null;
        }
        self.address = try r.readString();
        return self;
    }
};

pub const inputBusinessRecipients = struct {
    pub const constructor_id: u32 = 0x6f8b32aa;
    existing_chats: bool = false,
    new_chats: bool = false,
    contacts: bool = false,
    non_contacts: bool = false,
    exclude_selected: bool = false,
    users: ?[]api.InputUser = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.existing_chats) flags |= 1 << 0;
        if (self.new_chats) flags |= 1 << 1;
        if (self.contacts) flags |= 1 << 2;
        if (self.non_contacts) flags |= 1 << 3;
        if (self.exclude_selected) flags |= 1 << 5;
        if (self.users != null) flags |= 1 << 4;
        try w.writeUInt(flags);
        if (self.users) |v| {
            try w.writeUInt(vector_constructor_id);
            try w.writeVectorLength(v.len);
            for (v) |elem_2527| {
                try elem_2527.serialize(w);
            }
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.existing_chats = (flags >> 0) & 1 != 0;
        self.new_chats = (flags >> 1) & 1 != 0;
        self.contacts = (flags >> 2) & 1 != 0;
        self.non_contacts = (flags >> 3) & 1 != 0;
        self.exclude_selected = (flags >> 5) & 1 != 0;
        if ((flags >> 4) & 1 != 0) {
            if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
            const len_2528 = try r.readVectorLength();
            const vec_2529 = allocator.alloc(api.InputUser, len_2528) catch return error.OutOfMemory;
            for (vec_2529) |*elem_2530| {
                elem_2530.* = try api.InputUser.deserialize(allocator, r);
            }
            self.users = vec_2529;
        } else {
            self.users = null;
        }
        return self;
    }
};

pub const businessRecipients = struct {
    pub const constructor_id: u32 = 0x21108ff7;
    existing_chats: bool = false,
    new_chats: bool = false,
    contacts: bool = false,
    non_contacts: bool = false,
    exclude_selected: bool = false,
    users: ?[]i64 = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.existing_chats) flags |= 1 << 0;
        if (self.new_chats) flags |= 1 << 1;
        if (self.contacts) flags |= 1 << 2;
        if (self.non_contacts) flags |= 1 << 3;
        if (self.exclude_selected) flags |= 1 << 5;
        if (self.users != null) flags |= 1 << 4;
        try w.writeUInt(flags);
        if (self.users) |v| {
            try w.writeUInt(vector_constructor_id);
            try w.writeVectorLength(v.len);
            for (v) |elem_2531| {
                try w.writeLong(elem_2531);
            }
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.existing_chats = (flags >> 0) & 1 != 0;
        self.new_chats = (flags >> 1) & 1 != 0;
        self.contacts = (flags >> 2) & 1 != 0;
        self.non_contacts = (flags >> 3) & 1 != 0;
        self.exclude_selected = (flags >> 5) & 1 != 0;
        if ((flags >> 4) & 1 != 0) {
            if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
            const len_2532 = try r.readVectorLength();
            const vec_2533 = allocator.alloc(i64, len_2532) catch return error.OutOfMemory;
            for (vec_2533) |*elem_2534| {
                elem_2534.* = try r.readLong();
            }
            self.users = vec_2533;
        } else {
            self.users = null;
        }
        return self;
    }
};

pub const businessAwayMessageScheduleAlways = struct {
    pub const constructor_id: u32 = 0xc9b9e2b9;

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        _ = self;
        _ = w;
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        _ = allocator;
        _ = r;
        return .{};
    }
};

pub const businessAwayMessageScheduleOutsideWorkHours = struct {
    pub const constructor_id: u32 = 0xc3f2f501;

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        _ = self;
        _ = w;
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        _ = allocator;
        _ = r;
        return .{};
    }
};

pub const businessAwayMessageScheduleCustom = struct {
    pub const constructor_id: u32 = 0xcc4d9ecc;
    start_date: i32,
    end_date: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeInt(self.start_date);
        try w.writeInt(self.end_date);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.start_date = try r.readInt();
        self.end_date = try r.readInt();
        _ = allocator;
        return self;
    }
};

pub const inputBusinessGreetingMessage = struct {
    pub const constructor_id: u32 = 0x0194cb3b;
    shortcut_id: i32,
    recipients: api.InputBusinessRecipients,
    no_activity_days: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeInt(self.shortcut_id);
        try self.recipients.serialize(w);
        try w.writeInt(self.no_activity_days);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.shortcut_id = try r.readInt();
        self.recipients = try api.InputBusinessRecipients.deserialize(allocator, r);
        self.no_activity_days = try r.readInt();
        return self;
    }
};

pub const businessGreetingMessage = struct {
    pub const constructor_id: u32 = 0xe519abab;
    shortcut_id: i32,
    recipients: api.BusinessRecipients,
    no_activity_days: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeInt(self.shortcut_id);
        try self.recipients.serialize(w);
        try w.writeInt(self.no_activity_days);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.shortcut_id = try r.readInt();
        self.recipients = try api.BusinessRecipients.deserialize(allocator, r);
        self.no_activity_days = try r.readInt();
        return self;
    }
};

pub const inputBusinessAwayMessage = struct {
    pub const constructor_id: u32 = 0x832175e0;
    offline_only: bool = false,
    shortcut_id: i32,
    schedule: api.BusinessAwayMessageSchedule,
    recipients: api.InputBusinessRecipients,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.offline_only) flags |= 1 << 0;
        try w.writeUInt(flags);
        try w.writeInt(self.shortcut_id);
        try self.schedule.serialize(w);
        try self.recipients.serialize(w);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.offline_only = (flags >> 0) & 1 != 0;
        self.shortcut_id = try r.readInt();
        self.schedule = try api.BusinessAwayMessageSchedule.deserialize(allocator, r);
        self.recipients = try api.InputBusinessRecipients.deserialize(allocator, r);
        return self;
    }
};

pub const businessAwayMessage = struct {
    pub const constructor_id: u32 = 0xef156a5c;
    offline_only: bool = false,
    shortcut_id: i32,
    schedule: api.BusinessAwayMessageSchedule,
    recipients: api.BusinessRecipients,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.offline_only) flags |= 1 << 0;
        try w.writeUInt(flags);
        try w.writeInt(self.shortcut_id);
        try self.schedule.serialize(w);
        try self.recipients.serialize(w);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.offline_only = (flags >> 0) & 1 != 0;
        self.shortcut_id = try r.readInt();
        self.schedule = try api.BusinessAwayMessageSchedule.deserialize(allocator, r);
        self.recipients = try api.BusinessRecipients.deserialize(allocator, r);
        return self;
    }
};

pub const timezone = struct {
    pub const constructor_id: u32 = 0xff9289f5;
    id: []const u8,
    name: []const u8,
    utc_offset: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeString(self.id);
        try w.writeString(self.name);
        try w.writeInt(self.utc_offset);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.id = try r.readString();
        self.name = try r.readString();
        self.utc_offset = try r.readInt();
        _ = allocator;
        return self;
    }
};

pub const quickReply = struct {
    pub const constructor_id: u32 = 0x0697102b;
    shortcut_id: i32,
    shortcut: []const u8,
    top_message: i32,
    count: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeInt(self.shortcut_id);
        try w.writeString(self.shortcut);
        try w.writeInt(self.top_message);
        try w.writeInt(self.count);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.shortcut_id = try r.readInt();
        self.shortcut = try r.readString();
        self.top_message = try r.readInt();
        self.count = try r.readInt();
        _ = allocator;
        return self;
    }
};

pub const inputQuickReplyShortcut = struct {
    pub const constructor_id: u32 = 0x24596d41;
    shortcut: []const u8,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeString(self.shortcut);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.shortcut = try r.readString();
        _ = allocator;
        return self;
    }
};

pub const inputQuickReplyShortcutId = struct {
    pub const constructor_id: u32 = 0x01190cf1;
    shortcut_id: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeInt(self.shortcut_id);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.shortcut_id = try r.readInt();
        _ = allocator;
        return self;
    }
};

pub const birthday = struct {
    pub const constructor_id: u32 = 0x6c8e1e06;
    day: i32,
    month: i32,
    year: ?i32 = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.year != null) flags |= 1 << 0;
        try w.writeUInt(flags);
        try w.writeInt(self.day);
        try w.writeInt(self.month);
        if (self.year) |v| {
            try w.writeInt(v);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.day = try r.readInt();
        self.month = try r.readInt();
        if ((flags >> 0) & 1 != 0) {
            self.year = try r.readInt();
        } else {
            self.year = null;
        }
        _ = allocator;
        return self;
    }
};

pub const botBusinessConnection = struct {
    pub const constructor_id: u32 = 0x8f34b2f5;
    disabled: bool = false,
    connection_id: []const u8,
    user_id: i64,
    dc_id: i32,
    date: i32,
    rights: ?api.BusinessBotRights = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.disabled) flags |= 1 << 1;
        if (self.rights != null) flags |= 1 << 2;
        try w.writeUInt(flags);
        try w.writeString(self.connection_id);
        try w.writeLong(self.user_id);
        try w.writeInt(self.dc_id);
        try w.writeInt(self.date);
        if (self.rights) |v| {
            try v.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.disabled = (flags >> 1) & 1 != 0;
        self.connection_id = try r.readString();
        self.user_id = try r.readLong();
        self.dc_id = try r.readInt();
        self.date = try r.readInt();
        if ((flags >> 2) & 1 != 0) {
            self.rights = try api.BusinessBotRights.deserialize(allocator, r);
        } else {
            self.rights = null;
        }
        return self;
    }
};

pub const inputBusinessIntro = struct {
    pub const constructor_id: u32 = 0x09c469cd;
    title: []const u8,
    description: []const u8,
    sticker: ?api.InputDocument = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.sticker != null) flags |= 1 << 0;
        try w.writeUInt(flags);
        try w.writeString(self.title);
        try w.writeString(self.description);
        if (self.sticker) |v| {
            try v.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.title = try r.readString();
        self.description = try r.readString();
        if ((flags >> 0) & 1 != 0) {
            self.sticker = try api.InputDocument.deserialize(allocator, r);
        } else {
            self.sticker = null;
        }
        return self;
    }
};

pub const businessIntro = struct {
    pub const constructor_id: u32 = 0x5a0a066d;
    title: []const u8,
    description: []const u8,
    sticker: ?api.Document = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.sticker != null) flags |= 1 << 0;
        try w.writeUInt(flags);
        try w.writeString(self.title);
        try w.writeString(self.description);
        if (self.sticker) |v| {
            try v.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.title = try r.readString();
        self.description = try r.readString();
        if ((flags >> 0) & 1 != 0) {
            self.sticker = try api.Document.deserialize(allocator, r);
        } else {
            self.sticker = null;
        }
        return self;
    }
};

pub const inputBusinessBotRecipients = struct {
    pub const constructor_id: u32 = 0xc4e5921e;
    existing_chats: bool = false,
    new_chats: bool = false,
    contacts: bool = false,
    non_contacts: bool = false,
    exclude_selected: bool = false,
    users: ?[]api.InputUser = null,
    exclude_users: ?[]api.InputUser = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.existing_chats) flags |= 1 << 0;
        if (self.new_chats) flags |= 1 << 1;
        if (self.contacts) flags |= 1 << 2;
        if (self.non_contacts) flags |= 1 << 3;
        if (self.exclude_selected) flags |= 1 << 5;
        if (self.users != null) flags |= 1 << 4;
        if (self.exclude_users != null) flags |= 1 << 6;
        try w.writeUInt(flags);
        if (self.users) |v| {
            try w.writeUInt(vector_constructor_id);
            try w.writeVectorLength(v.len);
            for (v) |elem_2535| {
                try elem_2535.serialize(w);
            }
        }
        if (self.exclude_users) |v| {
            try w.writeUInt(vector_constructor_id);
            try w.writeVectorLength(v.len);
            for (v) |elem_2536| {
                try elem_2536.serialize(w);
            }
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.existing_chats = (flags >> 0) & 1 != 0;
        self.new_chats = (flags >> 1) & 1 != 0;
        self.contacts = (flags >> 2) & 1 != 0;
        self.non_contacts = (flags >> 3) & 1 != 0;
        self.exclude_selected = (flags >> 5) & 1 != 0;
        if ((flags >> 4) & 1 != 0) {
            if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
            const len_2537 = try r.readVectorLength();
            const vec_2538 = allocator.alloc(api.InputUser, len_2537) catch return error.OutOfMemory;
            for (vec_2538) |*elem_2539| {
                elem_2539.* = try api.InputUser.deserialize(allocator, r);
            }
            self.users = vec_2538;
        } else {
            self.users = null;
        }
        if ((flags >> 6) & 1 != 0) {
            if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
            const len_2540 = try r.readVectorLength();
            const vec_2541 = allocator.alloc(api.InputUser, len_2540) catch return error.OutOfMemory;
            for (vec_2541) |*elem_2542| {
                elem_2542.* = try api.InputUser.deserialize(allocator, r);
            }
            self.exclude_users = vec_2541;
        } else {
            self.exclude_users = null;
        }
        return self;
    }
};

pub const businessBotRecipients = struct {
    pub const constructor_id: u32 = 0xb88cf373;
    existing_chats: bool = false,
    new_chats: bool = false,
    contacts: bool = false,
    non_contacts: bool = false,
    exclude_selected: bool = false,
    users: ?[]i64 = null,
    exclude_users: ?[]i64 = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.existing_chats) flags |= 1 << 0;
        if (self.new_chats) flags |= 1 << 1;
        if (self.contacts) flags |= 1 << 2;
        if (self.non_contacts) flags |= 1 << 3;
        if (self.exclude_selected) flags |= 1 << 5;
        if (self.users != null) flags |= 1 << 4;
        if (self.exclude_users != null) flags |= 1 << 6;
        try w.writeUInt(flags);
        if (self.users) |v| {
            try w.writeUInt(vector_constructor_id);
            try w.writeVectorLength(v.len);
            for (v) |elem_2543| {
                try w.writeLong(elem_2543);
            }
        }
        if (self.exclude_users) |v| {
            try w.writeUInt(vector_constructor_id);
            try w.writeVectorLength(v.len);
            for (v) |elem_2544| {
                try w.writeLong(elem_2544);
            }
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.existing_chats = (flags >> 0) & 1 != 0;
        self.new_chats = (flags >> 1) & 1 != 0;
        self.contacts = (flags >> 2) & 1 != 0;
        self.non_contacts = (flags >> 3) & 1 != 0;
        self.exclude_selected = (flags >> 5) & 1 != 0;
        if ((flags >> 4) & 1 != 0) {
            if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
            const len_2545 = try r.readVectorLength();
            const vec_2546 = allocator.alloc(i64, len_2545) catch return error.OutOfMemory;
            for (vec_2546) |*elem_2547| {
                elem_2547.* = try r.readLong();
            }
            self.users = vec_2546;
        } else {
            self.users = null;
        }
        if ((flags >> 6) & 1 != 0) {
            if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
            const len_2548 = try r.readVectorLength();
            const vec_2549 = allocator.alloc(i64, len_2548) catch return error.OutOfMemory;
            for (vec_2549) |*elem_2550| {
                elem_2550.* = try r.readLong();
            }
            self.exclude_users = vec_2549;
        } else {
            self.exclude_users = null;
        }
        return self;
    }
};

pub const inputBusinessChatLink = struct {
    pub const constructor_id: u32 = 0x11679fa7;
    message: []const u8,
    entities: ?[]api.MessageEntity = null,
    title: ?[]const u8 = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.entities != null) flags |= 1 << 0;
        if (self.title != null) flags |= 1 << 1;
        try w.writeUInt(flags);
        try w.writeString(self.message);
        if (self.entities) |v| {
            try w.writeUInt(vector_constructor_id);
            try w.writeVectorLength(v.len);
            for (v) |elem_2551| {
                try elem_2551.serialize(w);
            }
        }
        if (self.title) |v| {
            try w.writeString(v);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.message = try r.readString();
        if ((flags >> 0) & 1 != 0) {
            if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
            const len_2552 = try r.readVectorLength();
            const vec_2553 = allocator.alloc(api.MessageEntity, len_2552) catch return error.OutOfMemory;
            for (vec_2553) |*elem_2554| {
                elem_2554.* = try api.MessageEntity.deserialize(allocator, r);
            }
            self.entities = vec_2553;
        } else {
            self.entities = null;
        }
        if ((flags >> 1) & 1 != 0) {
            self.title = try r.readString();
        } else {
            self.title = null;
        }
        return self;
    }
};

pub const businessChatLink = struct {
    pub const constructor_id: u32 = 0xb4ae666f;
    link: []const u8,
    message: []const u8,
    entities: ?[]api.MessageEntity = null,
    title: ?[]const u8 = null,
    views: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.entities != null) flags |= 1 << 0;
        if (self.title != null) flags |= 1 << 1;
        try w.writeUInt(flags);
        try w.writeString(self.link);
        try w.writeString(self.message);
        if (self.entities) |v| {
            try w.writeUInt(vector_constructor_id);
            try w.writeVectorLength(v.len);
            for (v) |elem_2555| {
                try elem_2555.serialize(w);
            }
        }
        if (self.title) |v| {
            try w.writeString(v);
        }
        try w.writeInt(self.views);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.link = try r.readString();
        self.message = try r.readString();
        if ((flags >> 0) & 1 != 0) {
            if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
            const len_2556 = try r.readVectorLength();
            const vec_2557 = allocator.alloc(api.MessageEntity, len_2556) catch return error.OutOfMemory;
            for (vec_2557) |*elem_2558| {
                elem_2558.* = try api.MessageEntity.deserialize(allocator, r);
            }
            self.entities = vec_2557;
        } else {
            self.entities = null;
        }
        if ((flags >> 1) & 1 != 0) {
            self.title = try r.readString();
        } else {
            self.title = null;
        }
        self.views = try r.readInt();
        return self;
    }
};

pub const businessBotRights = struct {
    pub const constructor_id: u32 = 0xa0624cf7;
    reply: bool = false,
    read_messages: bool = false,
    delete_sent_messages: bool = false,
    delete_received_messages: bool = false,
    edit_name: bool = false,
    edit_bio: bool = false,
    edit_profile_photo: bool = false,
    edit_username: bool = false,
    view_gifts: bool = false,
    sell_gifts: bool = false,
    change_gift_settings: bool = false,
    transfer_and_upgrade_gifts: bool = false,
    transfer_stars: bool = false,
    manage_stories: bool = false,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.reply) flags |= 1 << 0;
        if (self.read_messages) flags |= 1 << 1;
        if (self.delete_sent_messages) flags |= 1 << 2;
        if (self.delete_received_messages) flags |= 1 << 3;
        if (self.edit_name) flags |= 1 << 4;
        if (self.edit_bio) flags |= 1 << 5;
        if (self.edit_profile_photo) flags |= 1 << 6;
        if (self.edit_username) flags |= 1 << 7;
        if (self.view_gifts) flags |= 1 << 8;
        if (self.sell_gifts) flags |= 1 << 9;
        if (self.change_gift_settings) flags |= 1 << 10;
        if (self.transfer_and_upgrade_gifts) flags |= 1 << 11;
        if (self.transfer_stars) flags |= 1 << 12;
        if (self.manage_stories) flags |= 1 << 13;
        try w.writeUInt(flags);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.reply = (flags >> 0) & 1 != 0;
        self.read_messages = (flags >> 1) & 1 != 0;
        self.delete_sent_messages = (flags >> 2) & 1 != 0;
        self.delete_received_messages = (flags >> 3) & 1 != 0;
        self.edit_name = (flags >> 4) & 1 != 0;
        self.edit_bio = (flags >> 5) & 1 != 0;
        self.edit_profile_photo = (flags >> 6) & 1 != 0;
        self.edit_username = (flags >> 7) & 1 != 0;
        self.view_gifts = (flags >> 8) & 1 != 0;
        self.sell_gifts = (flags >> 9) & 1 != 0;
        self.change_gift_settings = (flags >> 10) & 1 != 0;
        self.transfer_and_upgrade_gifts = (flags >> 11) & 1 != 0;
        self.transfer_stars = (flags >> 12) & 1 != 0;
        self.manage_stories = (flags >> 13) & 1 != 0;
        _ = allocator;
        return self;
    }
};

pub const pendingSuggestion = struct {
    pub const constructor_id: u32 = 0xe7e82e12;
    suggestion: []const u8,
    title: api.TextWithEntities,
    description: api.TextWithEntities,
    url: []const u8,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeString(self.suggestion);
        try self.title.serialize(w);
        try self.description.serialize(w);
        try w.writeString(self.url);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.suggestion = try r.readString();
        self.title = try api.TextWithEntities.deserialize(allocator, r);
        self.description = try api.TextWithEntities.deserialize(allocator, r);
        self.url = try r.readString();
        return self;
    }
};

pub const BusinessWeeklyOpen = union(enum) {
    businessWeeklyOpen: api.businessWeeklyOpen,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.businessWeeklyOpen.constructor_id => return .{ .businessWeeklyOpen = try api.businessWeeklyOpen.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const BusinessWorkHours = union(enum) {
    businessWorkHours: api.businessWorkHours,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.businessWorkHours.constructor_id => return .{ .businessWorkHours = try api.businessWorkHours.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const BusinessLocation = union(enum) {
    businessLocation: api.businessLocation,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.businessLocation.constructor_id => return .{ .businessLocation = try api.businessLocation.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const InputBusinessRecipients = union(enum) {
    inputBusinessRecipients: api.inputBusinessRecipients,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.inputBusinessRecipients.constructor_id => return .{ .inputBusinessRecipients = try api.inputBusinessRecipients.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const BusinessRecipients = union(enum) {
    businessRecipients: api.businessRecipients,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.businessRecipients.constructor_id => return .{ .businessRecipients = try api.businessRecipients.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const BusinessAwayMessageSchedule = union(enum) {
    businessAwayMessageScheduleAlways: api.businessAwayMessageScheduleAlways,
    businessAwayMessageScheduleOutsideWorkHours: api.businessAwayMessageScheduleOutsideWorkHours,
    businessAwayMessageScheduleCustom: api.businessAwayMessageScheduleCustom,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.businessAwayMessageScheduleAlways.constructor_id => return .{ .businessAwayMessageScheduleAlways = try api.businessAwayMessageScheduleAlways.deserialize(allocator, r) },
            api.businessAwayMessageScheduleOutsideWorkHours.constructor_id => return .{ .businessAwayMessageScheduleOutsideWorkHours = try api.businessAwayMessageScheduleOutsideWorkHours.deserialize(allocator, r) },
            api.businessAwayMessageScheduleCustom.constructor_id => return .{ .businessAwayMessageScheduleCustom = try api.businessAwayMessageScheduleCustom.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const InputBusinessGreetingMessage = union(enum) {
    inputBusinessGreetingMessage: api.inputBusinessGreetingMessage,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.inputBusinessGreetingMessage.constructor_id => return .{ .inputBusinessGreetingMessage = try api.inputBusinessGreetingMessage.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const BusinessGreetingMessage = union(enum) {
    businessGreetingMessage: api.businessGreetingMessage,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.businessGreetingMessage.constructor_id => return .{ .businessGreetingMessage = try api.businessGreetingMessage.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const InputBusinessAwayMessage = union(enum) {
    inputBusinessAwayMessage: api.inputBusinessAwayMessage,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.inputBusinessAwayMessage.constructor_id => return .{ .inputBusinessAwayMessage = try api.inputBusinessAwayMessage.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const BusinessAwayMessage = union(enum) {
    businessAwayMessage: api.businessAwayMessage,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.businessAwayMessage.constructor_id => return .{ .businessAwayMessage = try api.businessAwayMessage.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const Timezone = union(enum) {
    timezone: api.timezone,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.timezone.constructor_id => return .{ .timezone = try api.timezone.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const QuickReply = union(enum) {
    quickReply: api.quickReply,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.quickReply.constructor_id => return .{ .quickReply = try api.quickReply.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const InputQuickReplyShortcut = union(enum) {
    inputQuickReplyShortcut: api.inputQuickReplyShortcut,
    inputQuickReplyShortcutId: api.inputQuickReplyShortcutId,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.inputQuickReplyShortcut.constructor_id => return .{ .inputQuickReplyShortcut = try api.inputQuickReplyShortcut.deserialize(allocator, r) },
            api.inputQuickReplyShortcutId.constructor_id => return .{ .inputQuickReplyShortcutId = try api.inputQuickReplyShortcutId.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const Birthday = union(enum) {
    birthday: api.birthday,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.birthday.constructor_id => return .{ .birthday = try api.birthday.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const BotBusinessConnection = union(enum) {
    botBusinessConnection: api.botBusinessConnection,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.botBusinessConnection.constructor_id => return .{ .botBusinessConnection = try api.botBusinessConnection.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const InputBusinessIntro = union(enum) {
    inputBusinessIntro: api.inputBusinessIntro,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.inputBusinessIntro.constructor_id => return .{ .inputBusinessIntro = try api.inputBusinessIntro.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const BusinessIntro = union(enum) {
    businessIntro: api.businessIntro,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.businessIntro.constructor_id => return .{ .businessIntro = try api.businessIntro.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const InputBusinessBotRecipients = union(enum) {
    inputBusinessBotRecipients: api.inputBusinessBotRecipients,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.inputBusinessBotRecipients.constructor_id => return .{ .inputBusinessBotRecipients = try api.inputBusinessBotRecipients.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const BusinessBotRecipients = union(enum) {
    businessBotRecipients: api.businessBotRecipients,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.businessBotRecipients.constructor_id => return .{ .businessBotRecipients = try api.businessBotRecipients.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const InputBusinessChatLink = union(enum) {
    inputBusinessChatLink: api.inputBusinessChatLink,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.inputBusinessChatLink.constructor_id => return .{ .inputBusinessChatLink = try api.inputBusinessChatLink.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const BusinessChatLink = union(enum) {
    businessChatLink: api.businessChatLink,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.businessChatLink.constructor_id => return .{ .businessChatLink = try api.businessChatLink.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const BusinessBotRights = union(enum) {
    businessBotRights: api.businessBotRights,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.businessBotRights.constructor_id => return .{ .businessBotRights = try api.businessBotRights.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const PendingSuggestion = union(enum) {
    pendingSuggestion: api.pendingSuggestion,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.pendingSuggestion.constructor_id => return .{ .pendingSuggestion = try api.pendingSuggestion.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};


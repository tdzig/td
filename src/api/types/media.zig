//! GENERATED by td TL code generator from "api.tl".
//! Do not edit by hand; regenerate with td-gen.

const std = @import("std");
const td = @import("../../root.zig");
const Writer = td.tl.Writer;
const Reader = td.tl.Reader;
const TlError = td.TlError;
const vector_constructor_id = td.tl.vector_constructor_id;

const api = @import("../mod.zig");

pub const inputChatPhotoEmpty = struct {
    pub const constructor_id: u32 = 0x1ca48f57;

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        _ = self;
        _ = w;
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        _ = allocator;
        _ = r;
        return .{};
    }
};

pub const inputChatUploadedPhoto = struct {
    pub const constructor_id: u32 = 0xbdcdaec0;
    file: ?api.InputFile = null,
    video: ?api.InputFile = null,
    video_start_ts: ?f64 = null,
    video_emoji_markup: ?api.VideoSize = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.file != null) flags |= 1 << 0;
        if (self.video != null) flags |= 1 << 1;
        if (self.video_start_ts != null) flags |= 1 << 2;
        if (self.video_emoji_markup != null) flags |= 1 << 3;
        try w.writeUInt(flags);
        if (self.file) |v| {
            try v.serialize(w);
        }
        if (self.video) |v| {
            try v.serialize(w);
        }
        if (self.video_start_ts) |v| {
            try w.writeDouble(v);
        }
        if (self.video_emoji_markup) |v| {
            try v.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        if ((flags >> 0) & 1 != 0) {
            self.file = try api.InputFile.deserialize(allocator, r);
        } else {
            self.file = null;
        }
        if ((flags >> 1) & 1 != 0) {
            self.video = try api.InputFile.deserialize(allocator, r);
        } else {
            self.video = null;
        }
        if ((flags >> 2) & 1 != 0) {
            self.video_start_ts = try r.readDouble();
        } else {
            self.video_start_ts = null;
        }
        if ((flags >> 3) & 1 != 0) {
            self.video_emoji_markup = try api.VideoSize.deserialize(allocator, r);
        } else {
            self.video_emoji_markup = null;
        }
        return self;
    }
};

pub const inputChatPhoto = struct {
    pub const constructor_id: u32 = 0x8953ad37;
    id: api.InputPhoto,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try self.id.serialize(w);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.id = try api.InputPhoto.deserialize(allocator, r);
        return self;
    }
};

pub const inputGeoPointEmpty = struct {
    pub const constructor_id: u32 = 0xe4c123d6;

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        _ = self;
        _ = w;
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        _ = allocator;
        _ = r;
        return .{};
    }
};

pub const inputGeoPoint = struct {
    pub const constructor_id: u32 = 0x48222faf;
    lat: f64,
    long_: f64,
    accuracy_radius: ?i32 = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.accuracy_radius != null) flags |= 1 << 0;
        try w.writeUInt(flags);
        try w.writeDouble(self.lat);
        try w.writeDouble(self.long_);
        if (self.accuracy_radius) |v| {
            try w.writeInt(v);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.lat = try r.readDouble();
        self.long_ = try r.readDouble();
        if ((flags >> 0) & 1 != 0) {
            self.accuracy_radius = try r.readInt();
        } else {
            self.accuracy_radius = null;
        }
        _ = allocator;
        return self;
    }
};

pub const inputPhotoEmpty = struct {
    pub const constructor_id: u32 = 0x1cd7bf0d;

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        _ = self;
        _ = w;
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        _ = allocator;
        _ = r;
        return .{};
    }
};

pub const inputPhoto = struct {
    pub const constructor_id: u32 = 0x3bb3b94a;
    id: i64,
    access_hash: i64,
    file_reference: []const u8,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeLong(self.id);
        try w.writeLong(self.access_hash);
        try w.writeBytes(self.file_reference);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.id = try r.readLong();
        self.access_hash = try r.readLong();
        self.file_reference = try r.readBytes();
        _ = allocator;
        return self;
    }
};

pub const photoEmpty = struct {
    pub const constructor_id: u32 = 0x2331b22d;
    id: i64,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeLong(self.id);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.id = try r.readLong();
        _ = allocator;
        return self;
    }
};

pub const photo = struct {
    pub const constructor_id: u32 = 0xfb197a65;
    has_stickers: bool = false,
    id: i64,
    access_hash: i64,
    file_reference: []const u8,
    date: i32,
    sizes: []api.PhotoSize,
    video_sizes: ?[]api.VideoSize = null,
    dc_id: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.has_stickers) flags |= 1 << 0;
        if (self.video_sizes != null) flags |= 1 << 1;
        try w.writeUInt(flags);
        try w.writeLong(self.id);
        try w.writeLong(self.access_hash);
        try w.writeBytes(self.file_reference);
        try w.writeInt(self.date);
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.sizes.len);
        for (self.sizes) |elem_828| {
            try elem_828.serialize(w);
        }
        if (self.video_sizes) |v| {
            try w.writeUInt(vector_constructor_id);
            try w.writeVectorLength(v.len);
            for (v) |elem_829| {
                try elem_829.serialize(w);
            }
        }
        try w.writeInt(self.dc_id);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.has_stickers = (flags >> 0) & 1 != 0;
        self.id = try r.readLong();
        self.access_hash = try r.readLong();
        self.file_reference = try r.readBytes();
        self.date = try r.readInt();
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_830 = try r.readVectorLength();
        const vec_831 = allocator.alloc(api.PhotoSize, len_830) catch return error.OutOfMemory;
        for (vec_831) |*elem_832| {
            elem_832.* = try api.PhotoSize.deserialize(allocator, r);
        }
        self.sizes = vec_831;
        if ((flags >> 1) & 1 != 0) {
            if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
            const len_833 = try r.readVectorLength();
            const vec_834 = allocator.alloc(api.VideoSize, len_833) catch return error.OutOfMemory;
            for (vec_834) |*elem_835| {
                elem_835.* = try api.VideoSize.deserialize(allocator, r);
            }
            self.video_sizes = vec_834;
        } else {
            self.video_sizes = null;
        }
        self.dc_id = try r.readInt();
        return self;
    }
};

pub const geoPointEmpty = struct {
    pub const constructor_id: u32 = 0x1117dd5f;

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        _ = self;
        _ = w;
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        _ = allocator;
        _ = r;
        return .{};
    }
};

pub const geoPoint = struct {
    pub const constructor_id: u32 = 0xb2a2f663;
    long_: f64,
    lat: f64,
    access_hash: i64,
    accuracy_radius: ?i32 = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.accuracy_radius != null) flags |= 1 << 0;
        try w.writeUInt(flags);
        try w.writeDouble(self.long_);
        try w.writeDouble(self.lat);
        try w.writeLong(self.access_hash);
        if (self.accuracy_radius) |v| {
            try w.writeInt(v);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.long_ = try r.readDouble();
        self.lat = try r.readDouble();
        self.access_hash = try r.readLong();
        if ((flags >> 0) & 1 != 0) {
            self.accuracy_radius = try r.readInt();
        } else {
            self.accuracy_radius = null;
        }
        _ = allocator;
        return self;
    }
};

pub const photos_ = struct {
    pub const constructor_id: u32 = 0x8dca6aa5;
    photos: []api.Photo,
    users: []api.User,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.photos.len);
        for (self.photos) |elem_836| {
            try elem_836.serialize(w);
        }
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.users.len);
        for (self.users) |elem_837| {
            try elem_837.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_838 = try r.readVectorLength();
        const vec_839 = allocator.alloc(api.Photo, len_838) catch return error.OutOfMemory;
        for (vec_839) |*elem_840| {
            elem_840.* = try api.Photo.deserialize(allocator, r);
        }
        self.photos = vec_839;
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_841 = try r.readVectorLength();
        const vec_842 = allocator.alloc(api.User, len_841) catch return error.OutOfMemory;
        for (vec_842) |*elem_843| {
            elem_843.* = try api.User.deserialize(allocator, r);
        }
        self.users = vec_842;
        return self;
    }
};

pub const photosSlice = struct {
    pub const constructor_id: u32 = 0x15051f54;
    count: i32,
    photos: []api.Photo,
    users: []api.User,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeInt(self.count);
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.photos.len);
        for (self.photos) |elem_844| {
            try elem_844.serialize(w);
        }
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.users.len);
        for (self.users) |elem_845| {
            try elem_845.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.count = try r.readInt();
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_846 = try r.readVectorLength();
        const vec_847 = allocator.alloc(api.Photo, len_846) catch return error.OutOfMemory;
        for (vec_847) |*elem_848| {
            elem_848.* = try api.Photo.deserialize(allocator, r);
        }
        self.photos = vec_847;
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_849 = try r.readVectorLength();
        const vec_850 = allocator.alloc(api.User, len_849) catch return error.OutOfMemory;
        for (vec_850) |*elem_851| {
            elem_851.* = try api.User.deserialize(allocator, r);
        }
        self.users = vec_850;
        return self;
    }
};

pub const photo_ = struct {
    pub const constructor_id: u32 = 0x20212ca8;
    photo: api.Photo,
    users: []api.User,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try self.photo.serialize(w);
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.users.len);
        for (self.users) |elem_852| {
            try elem_852.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.photo = try api.Photo.deserialize(allocator, r);
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_853 = try r.readVectorLength();
        const vec_854 = allocator.alloc(api.User, len_853) catch return error.OutOfMemory;
        for (vec_854) |*elem_855| {
            elem_855.* = try api.User.deserialize(allocator, r);
        }
        self.users = vec_854;
        return self;
    }
};

pub const inputDocumentEmpty = struct {
    pub const constructor_id: u32 = 0x72f0eaae;

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        _ = self;
        _ = w;
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        _ = allocator;
        _ = r;
        return .{};
    }
};

pub const inputDocument = struct {
    pub const constructor_id: u32 = 0x1abfb575;
    id: i64,
    access_hash: i64,
    file_reference: []const u8,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeLong(self.id);
        try w.writeLong(self.access_hash);
        try w.writeBytes(self.file_reference);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.id = try r.readLong();
        self.access_hash = try r.readLong();
        self.file_reference = try r.readBytes();
        _ = allocator;
        return self;
    }
};

pub const documentEmpty = struct {
    pub const constructor_id: u32 = 0x36f8c871;
    id: i64,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeLong(self.id);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.id = try r.readLong();
        _ = allocator;
        return self;
    }
};

pub const document = struct {
    pub const constructor_id: u32 = 0x8fd4c4d8;
    id: i64,
    access_hash: i64,
    file_reference: []const u8,
    date: i32,
    mime_type: []const u8,
    size: i64,
    thumbs: ?[]api.PhotoSize = null,
    video_thumbs: ?[]api.VideoSize = null,
    dc_id: i32,
    attributes: []api.DocumentAttribute,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.thumbs != null) flags |= 1 << 0;
        if (self.video_thumbs != null) flags |= 1 << 1;
        try w.writeUInt(flags);
        try w.writeLong(self.id);
        try w.writeLong(self.access_hash);
        try w.writeBytes(self.file_reference);
        try w.writeInt(self.date);
        try w.writeString(self.mime_type);
        try w.writeLong(self.size);
        if (self.thumbs) |v| {
            try w.writeUInt(vector_constructor_id);
            try w.writeVectorLength(v.len);
            for (v) |elem_856| {
                try elem_856.serialize(w);
            }
        }
        if (self.video_thumbs) |v| {
            try w.writeUInt(vector_constructor_id);
            try w.writeVectorLength(v.len);
            for (v) |elem_857| {
                try elem_857.serialize(w);
            }
        }
        try w.writeInt(self.dc_id);
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.attributes.len);
        for (self.attributes) |elem_858| {
            try elem_858.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.id = try r.readLong();
        self.access_hash = try r.readLong();
        self.file_reference = try r.readBytes();
        self.date = try r.readInt();
        self.mime_type = try r.readString();
        self.size = try r.readLong();
        if ((flags >> 0) & 1 != 0) {
            if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
            const len_859 = try r.readVectorLength();
            const vec_860 = allocator.alloc(api.PhotoSize, len_859) catch return error.OutOfMemory;
            for (vec_860) |*elem_861| {
                elem_861.* = try api.PhotoSize.deserialize(allocator, r);
            }
            self.thumbs = vec_860;
        } else {
            self.thumbs = null;
        }
        if ((flags >> 1) & 1 != 0) {
            if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
            const len_862 = try r.readVectorLength();
            const vec_863 = allocator.alloc(api.VideoSize, len_862) catch return error.OutOfMemory;
            for (vec_863) |*elem_864| {
                elem_864.* = try api.VideoSize.deserialize(allocator, r);
            }
            self.video_thumbs = vec_863;
        } else {
            self.video_thumbs = null;
        }
        self.dc_id = try r.readInt();
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_865 = try r.readVectorLength();
        const vec_866 = allocator.alloc(api.DocumentAttribute, len_865) catch return error.OutOfMemory;
        for (vec_866) |*elem_867| {
            elem_867.* = try api.DocumentAttribute.deserialize(allocator, r);
        }
        self.attributes = vec_866;
        return self;
    }
};

pub const documentAttributeImageSize = struct {
    pub const constructor_id: u32 = 0x6c37c15c;
    w: i32,
    h: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeInt(self.w);
        try w.writeInt(self.h);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.w = try r.readInt();
        self.h = try r.readInt();
        _ = allocator;
        return self;
    }
};

pub const documentAttributeAnimated = struct {
    pub const constructor_id: u32 = 0x11b58939;

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        _ = self;
        _ = w;
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        _ = allocator;
        _ = r;
        return .{};
    }
};

pub const documentAttributeSticker = struct {
    pub const constructor_id: u32 = 0x6319d612;
    mask: bool = false,
    alt: []const u8,
    stickerset: api.InputStickerSet,
    mask_coords: ?api.MaskCoords = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.mask) flags |= 1 << 1;
        if (self.mask_coords != null) flags |= 1 << 0;
        try w.writeUInt(flags);
        try w.writeString(self.alt);
        try self.stickerset.serialize(w);
        if (self.mask_coords) |v| {
            try v.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.mask = (flags >> 1) & 1 != 0;
        self.alt = try r.readString();
        self.stickerset = try api.InputStickerSet.deserialize(allocator, r);
        if ((flags >> 0) & 1 != 0) {
            self.mask_coords = try api.MaskCoords.deserialize(allocator, r);
        } else {
            self.mask_coords = null;
        }
        return self;
    }
};

pub const documentAttributeVideo = struct {
    pub const constructor_id: u32 = 0x43c57c48;
    round_message: bool = false,
    supports_streaming: bool = false,
    nosound: bool = false,
    duration: f64,
    w: i32,
    h: i32,
    preload_prefix_size: ?i32 = null,
    video_start_ts: ?f64 = null,
    video_codec: ?[]const u8 = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.round_message) flags |= 1 << 0;
        if (self.supports_streaming) flags |= 1 << 1;
        if (self.nosound) flags |= 1 << 3;
        if (self.preload_prefix_size != null) flags |= 1 << 2;
        if (self.video_start_ts != null) flags |= 1 << 4;
        if (self.video_codec != null) flags |= 1 << 5;
        try w.writeUInt(flags);
        try w.writeDouble(self.duration);
        try w.writeInt(self.w);
        try w.writeInt(self.h);
        if (self.preload_prefix_size) |v| {
            try w.writeInt(v);
        }
        if (self.video_start_ts) |v| {
            try w.writeDouble(v);
        }
        if (self.video_codec) |v| {
            try w.writeString(v);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.round_message = (flags >> 0) & 1 != 0;
        self.supports_streaming = (flags >> 1) & 1 != 0;
        self.nosound = (flags >> 3) & 1 != 0;
        self.duration = try r.readDouble();
        self.w = try r.readInt();
        self.h = try r.readInt();
        if ((flags >> 2) & 1 != 0) {
            self.preload_prefix_size = try r.readInt();
        } else {
            self.preload_prefix_size = null;
        }
        if ((flags >> 4) & 1 != 0) {
            self.video_start_ts = try r.readDouble();
        } else {
            self.video_start_ts = null;
        }
        if ((flags >> 5) & 1 != 0) {
            self.video_codec = try r.readString();
        } else {
            self.video_codec = null;
        }
        _ = allocator;
        return self;
    }
};

pub const documentAttributeAudio = struct {
    pub const constructor_id: u32 = 0x9852f9c6;
    voice: bool = false,
    duration: i32,
    title: ?[]const u8 = null,
    performer: ?[]const u8 = null,
    waveform: ?[]const u8 = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.voice) flags |= 1 << 10;
        if (self.title != null) flags |= 1 << 0;
        if (self.performer != null) flags |= 1 << 1;
        if (self.waveform != null) flags |= 1 << 2;
        try w.writeUInt(flags);
        try w.writeInt(self.duration);
        if (self.title) |v| {
            try w.writeString(v);
        }
        if (self.performer) |v| {
            try w.writeString(v);
        }
        if (self.waveform) |v| {
            try w.writeBytes(v);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.voice = (flags >> 10) & 1 != 0;
        self.duration = try r.readInt();
        if ((flags >> 0) & 1 != 0) {
            self.title = try r.readString();
        } else {
            self.title = null;
        }
        if ((flags >> 1) & 1 != 0) {
            self.performer = try r.readString();
        } else {
            self.performer = null;
        }
        if ((flags >> 2) & 1 != 0) {
            self.waveform = try r.readBytes();
        } else {
            self.waveform = null;
        }
        _ = allocator;
        return self;
    }
};

pub const documentAttributeFilename = struct {
    pub const constructor_id: u32 = 0x15590068;
    file_name: []const u8,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeString(self.file_name);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.file_name = try r.readString();
        _ = allocator;
        return self;
    }
};

pub const documentAttributeHasStickers = struct {
    pub const constructor_id: u32 = 0x9801d2f7;

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        _ = self;
        _ = w;
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        _ = allocator;
        _ = r;
        return .{};
    }
};

pub const documentAttributeCustomEmoji = struct {
    pub const constructor_id: u32 = 0xfd149899;
    free: bool = false,
    text_color: bool = false,
    alt: []const u8,
    stickerset: api.InputStickerSet,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.free) flags |= 1 << 0;
        if (self.text_color) flags |= 1 << 1;
        try w.writeUInt(flags);
        try w.writeString(self.alt);
        try self.stickerset.serialize(w);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.free = (flags >> 0) & 1 != 0;
        self.text_color = (flags >> 1) & 1 != 0;
        self.alt = try r.readString();
        self.stickerset = try api.InputStickerSet.deserialize(allocator, r);
        return self;
    }
};

pub const maskCoords = struct {
    pub const constructor_id: u32 = 0xaed6dbb2;
    n: i32,
    x: f64,
    y: f64,
    zoom: f64,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeInt(self.n);
        try w.writeDouble(self.x);
        try w.writeDouble(self.y);
        try w.writeDouble(self.zoom);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.n = try r.readInt();
        self.x = try r.readDouble();
        self.y = try r.readDouble();
        self.zoom = try r.readDouble();
        _ = allocator;
        return self;
    }
};

pub const webDocument = struct {
    pub const constructor_id: u32 = 0x1c570ed1;
    url: []const u8,
    access_hash: i64,
    size: i32,
    mime_type: []const u8,
    attributes: []api.DocumentAttribute,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeString(self.url);
        try w.writeLong(self.access_hash);
        try w.writeInt(self.size);
        try w.writeString(self.mime_type);
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.attributes.len);
        for (self.attributes) |elem_868| {
            try elem_868.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.url = try r.readString();
        self.access_hash = try r.readLong();
        self.size = try r.readInt();
        self.mime_type = try r.readString();
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_869 = try r.readVectorLength();
        const vec_870 = allocator.alloc(api.DocumentAttribute, len_869) catch return error.OutOfMemory;
        for (vec_870) |*elem_871| {
            elem_871.* = try api.DocumentAttribute.deserialize(allocator, r);
        }
        self.attributes = vec_870;
        return self;
    }
};

pub const webDocumentNoProxy = struct {
    pub const constructor_id: u32 = 0xf9c8bcc6;
    url: []const u8,
    size: i32,
    mime_type: []const u8,
    attributes: []api.DocumentAttribute,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeString(self.url);
        try w.writeInt(self.size);
        try w.writeString(self.mime_type);
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.attributes.len);
        for (self.attributes) |elem_872| {
            try elem_872.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.url = try r.readString();
        self.size = try r.readInt();
        self.mime_type = try r.readString();
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_873 = try r.readVectorLength();
        const vec_874 = allocator.alloc(api.DocumentAttribute, len_873) catch return error.OutOfMemory;
        for (vec_874) |*elem_875| {
            elem_875.* = try api.DocumentAttribute.deserialize(allocator, r);
        }
        self.attributes = vec_874;
        return self;
    }
};

pub const inputWebDocument = struct {
    pub const constructor_id: u32 = 0x9bed434d;
    url: []const u8,
    size: i32,
    mime_type: []const u8,
    attributes: []api.DocumentAttribute,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeString(self.url);
        try w.writeInt(self.size);
        try w.writeString(self.mime_type);
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.attributes.len);
        for (self.attributes) |elem_876| {
            try elem_876.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.url = try r.readString();
        self.size = try r.readInt();
        self.mime_type = try r.readString();
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_877 = try r.readVectorLength();
        const vec_878 = allocator.alloc(api.DocumentAttribute, len_877) catch return error.OutOfMemory;
        for (vec_878) |*elem_879| {
            elem_879.* = try api.DocumentAttribute.deserialize(allocator, r);
        }
        self.attributes = vec_878;
        return self;
    }
};

pub const phoneConnection = struct {
    pub const constructor_id: u32 = 0x9cc123c7;
    tcp: bool = false,
    id: i64,
    ip: []const u8,
    ipv6: []const u8,
    port: i32,
    peer_tag: []const u8,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.tcp) flags |= 1 << 0;
        try w.writeUInt(flags);
        try w.writeLong(self.id);
        try w.writeString(self.ip);
        try w.writeString(self.ipv6);
        try w.writeInt(self.port);
        try w.writeBytes(self.peer_tag);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.tcp = (flags >> 0) & 1 != 0;
        self.id = try r.readLong();
        self.ip = try r.readString();
        self.ipv6 = try r.readString();
        self.port = try r.readInt();
        self.peer_tag = try r.readBytes();
        _ = allocator;
        return self;
    }
};

pub const phoneConnectionWebrtc = struct {
    pub const constructor_id: u32 = 0x635fe375;
    turn: bool = false,
    stun: bool = false,
    id: i64,
    ip: []const u8,
    ipv6: []const u8,
    port: i32,
    username: []const u8,
    password: []const u8,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.turn) flags |= 1 << 0;
        if (self.stun) flags |= 1 << 1;
        try w.writeUInt(flags);
        try w.writeLong(self.id);
        try w.writeString(self.ip);
        try w.writeString(self.ipv6);
        try w.writeInt(self.port);
        try w.writeString(self.username);
        try w.writeString(self.password);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.turn = (flags >> 0) & 1 != 0;
        self.stun = (flags >> 1) & 1 != 0;
        self.id = try r.readLong();
        self.ip = try r.readString();
        self.ipv6 = try r.readString();
        self.port = try r.readInt();
        self.username = try r.readString();
        self.password = try r.readString();
        _ = allocator;
        return self;
    }
};

pub const videoSize = struct {
    pub const constructor_id: u32 = 0xde33b094;
    type_: []const u8,
    w: i32,
    h: i32,
    size: i32,
    video_start_ts: ?f64 = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.video_start_ts != null) flags |= 1 << 0;
        try w.writeUInt(flags);
        try w.writeString(self.type_);
        try w.writeInt(self.w);
        try w.writeInt(self.h);
        try w.writeInt(self.size);
        if (self.video_start_ts) |v| {
            try w.writeDouble(v);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.type_ = try r.readString();
        self.w = try r.readInt();
        self.h = try r.readInt();
        self.size = try r.readInt();
        if ((flags >> 0) & 1 != 0) {
            self.video_start_ts = try r.readDouble();
        } else {
            self.video_start_ts = null;
        }
        _ = allocator;
        return self;
    }
};

pub const videoSizeEmojiMarkup = struct {
    pub const constructor_id: u32 = 0xf85c413c;
    emoji_id: i64,
    background_colors: []i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeLong(self.emoji_id);
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.background_colors.len);
        for (self.background_colors) |elem_880| {
            try w.writeInt(elem_880);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.emoji_id = try r.readLong();
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_881 = try r.readVectorLength();
        const vec_882 = allocator.alloc(i32, len_881) catch return error.OutOfMemory;
        for (vec_882) |*elem_883| {
            elem_883.* = try r.readInt();
        }
        self.background_colors = vec_882;
        return self;
    }
};

pub const videoSizeStickerMarkup = struct {
    pub const constructor_id: u32 = 0x0da082fe;
    stickerset: api.InputStickerSet,
    sticker_id: i64,
    background_colors: []i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try self.stickerset.serialize(w);
        try w.writeLong(self.sticker_id);
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.background_colors.len);
        for (self.background_colors) |elem_884| {
            try w.writeInt(elem_884);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.stickerset = try api.InputStickerSet.deserialize(allocator, r);
        self.sticker_id = try r.readLong();
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_885 = try r.readVectorLength();
        const vec_886 = allocator.alloc(i32, len_885) catch return error.OutOfMemory;
        for (vec_886) |*elem_887| {
            elem_887.* = try r.readInt();
        }
        self.background_colors = vec_886;
        return self;
    }
};

pub const mediaAreaCoordinates = struct {
    pub const constructor_id: u32 = 0xcfc9e002;
    x: f64,
    y: f64,
    w: f64,
    h: f64,
    rotation: f64,
    radius: ?f64 = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.radius != null) flags |= 1 << 0;
        try w.writeUInt(flags);
        try w.writeDouble(self.x);
        try w.writeDouble(self.y);
        try w.writeDouble(self.w);
        try w.writeDouble(self.h);
        try w.writeDouble(self.rotation);
        if (self.radius) |v| {
            try w.writeDouble(v);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.x = try r.readDouble();
        self.y = try r.readDouble();
        self.w = try r.readDouble();
        self.h = try r.readDouble();
        self.rotation = try r.readDouble();
        if ((flags >> 0) & 1 != 0) {
            self.radius = try r.readDouble();
        } else {
            self.radius = null;
        }
        _ = allocator;
        return self;
    }
};

pub const mediaAreaVenue = struct {
    pub const constructor_id: u32 = 0xbe82db9c;
    coordinates: api.MediaAreaCoordinates,
    geo: api.GeoPoint,
    title: []const u8,
    address: []const u8,
    provider: []const u8,
    venue_id: []const u8,
    venue_type: []const u8,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try self.coordinates.serialize(w);
        try self.geo.serialize(w);
        try w.writeString(self.title);
        try w.writeString(self.address);
        try w.writeString(self.provider);
        try w.writeString(self.venue_id);
        try w.writeString(self.venue_type);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.coordinates = try api.MediaAreaCoordinates.deserialize(allocator, r);
        self.geo = try api.GeoPoint.deserialize(allocator, r);
        self.title = try r.readString();
        self.address = try r.readString();
        self.provider = try r.readString();
        self.venue_id = try r.readString();
        self.venue_type = try r.readString();
        return self;
    }
};

pub const inputMediaAreaVenue = struct {
    pub const constructor_id: u32 = 0xb282217f;
    coordinates: api.MediaAreaCoordinates,
    query_id: i64,
    result_id: []const u8,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try self.coordinates.serialize(w);
        try w.writeLong(self.query_id);
        try w.writeString(self.result_id);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.coordinates = try api.MediaAreaCoordinates.deserialize(allocator, r);
        self.query_id = try r.readLong();
        self.result_id = try r.readString();
        return self;
    }
};

pub const mediaAreaGeoPoint = struct {
    pub const constructor_id: u32 = 0xcad5452d;
    coordinates: api.MediaAreaCoordinates,
    geo: api.GeoPoint,
    address: ?api.GeoPointAddress = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.address != null) flags |= 1 << 0;
        try w.writeUInt(flags);
        try self.coordinates.serialize(w);
        try self.geo.serialize(w);
        if (self.address) |v| {
            try v.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.coordinates = try api.MediaAreaCoordinates.deserialize(allocator, r);
        self.geo = try api.GeoPoint.deserialize(allocator, r);
        if ((flags >> 0) & 1 != 0) {
            self.address = try api.GeoPointAddress.deserialize(allocator, r);
        } else {
            self.address = null;
        }
        return self;
    }
};

pub const mediaAreaSuggestedReaction = struct {
    pub const constructor_id: u32 = 0x14455871;
    dark: bool = false,
    flipped: bool = false,
    coordinates: api.MediaAreaCoordinates,
    reaction: api.Reaction,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.dark) flags |= 1 << 0;
        if (self.flipped) flags |= 1 << 1;
        try w.writeUInt(flags);
        try self.coordinates.serialize(w);
        try self.reaction.serialize(w);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.dark = (flags >> 0) & 1 != 0;
        self.flipped = (flags >> 1) & 1 != 0;
        self.coordinates = try api.MediaAreaCoordinates.deserialize(allocator, r);
        self.reaction = try api.Reaction.deserialize(allocator, r);
        return self;
    }
};

pub const mediaAreaChannelPost = struct {
    pub const constructor_id: u32 = 0x770416af;
    coordinates: api.MediaAreaCoordinates,
    channel_id: i64,
    msg_id: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try self.coordinates.serialize(w);
        try w.writeLong(self.channel_id);
        try w.writeInt(self.msg_id);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.coordinates = try api.MediaAreaCoordinates.deserialize(allocator, r);
        self.channel_id = try r.readLong();
        self.msg_id = try r.readInt();
        return self;
    }
};

pub const inputMediaAreaChannelPost = struct {
    pub const constructor_id: u32 = 0x2271f2bf;
    coordinates: api.MediaAreaCoordinates,
    channel: api.InputChannel,
    msg_id: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try self.coordinates.serialize(w);
        try self.channel.serialize(w);
        try w.writeInt(self.msg_id);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.coordinates = try api.MediaAreaCoordinates.deserialize(allocator, r);
        self.channel = try api.InputChannel.deserialize(allocator, r);
        self.msg_id = try r.readInt();
        return self;
    }
};

pub const mediaAreaUrl = struct {
    pub const constructor_id: u32 = 0x37381085;
    coordinates: api.MediaAreaCoordinates,
    url: []const u8,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try self.coordinates.serialize(w);
        try w.writeString(self.url);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.coordinates = try api.MediaAreaCoordinates.deserialize(allocator, r);
        self.url = try r.readString();
        return self;
    }
};

pub const mediaAreaWeather = struct {
    pub const constructor_id: u32 = 0x49a6549c;
    coordinates: api.MediaAreaCoordinates,
    emoji: []const u8,
    temperature_c: f64,
    color: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try self.coordinates.serialize(w);
        try w.writeString(self.emoji);
        try w.writeDouble(self.temperature_c);
        try w.writeInt(self.color);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.coordinates = try api.MediaAreaCoordinates.deserialize(allocator, r);
        self.emoji = try r.readString();
        self.temperature_c = try r.readDouble();
        self.color = try r.readInt();
        return self;
    }
};

pub const mediaAreaStarGift = struct {
    pub const constructor_id: u32 = 0x5787686d;
    coordinates: api.MediaAreaCoordinates,
    slug: []const u8,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try self.coordinates.serialize(w);
        try w.writeString(self.slug);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.coordinates = try api.MediaAreaCoordinates.deserialize(allocator, r);
        self.slug = try r.readString();
        return self;
    }
};

pub const geoPointAddress = struct {
    pub const constructor_id: u32 = 0xde4c5d93;
    country_iso2: []const u8,
    state: ?[]const u8 = null,
    city: ?[]const u8 = null,
    street: ?[]const u8 = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.state != null) flags |= 1 << 0;
        if (self.city != null) flags |= 1 << 1;
        if (self.street != null) flags |= 1 << 2;
        try w.writeUInt(flags);
        try w.writeString(self.country_iso2);
        if (self.state) |v| {
            try w.writeString(v);
        }
        if (self.city) |v| {
            try w.writeString(v);
        }
        if (self.street) |v| {
            try w.writeString(v);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.country_iso2 = try r.readString();
        if ((flags >> 0) & 1 != 0) {
            self.state = try r.readString();
        } else {
            self.state = null;
        }
        if ((flags >> 1) & 1 != 0) {
            self.city = try r.readString();
        } else {
            self.city = null;
        }
        if ((flags >> 2) & 1 != 0) {
            self.street = try r.readString();
        } else {
            self.street = null;
        }
        _ = allocator;
        return self;
    }
};

pub const InputChatPhoto = union(enum) {
    inputChatPhotoEmpty: api.inputChatPhotoEmpty,
    inputChatUploadedPhoto: api.inputChatUploadedPhoto,
    inputChatPhoto: api.inputChatPhoto,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.inputChatPhotoEmpty.constructor_id => return .{ .inputChatPhotoEmpty = try api.inputChatPhotoEmpty.deserialize(allocator, r) },
            api.inputChatUploadedPhoto.constructor_id => return .{ .inputChatUploadedPhoto = try api.inputChatUploadedPhoto.deserialize(allocator, r) },
            api.inputChatPhoto.constructor_id => return .{ .inputChatPhoto = try api.inputChatPhoto.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const InputGeoPoint = union(enum) {
    inputGeoPointEmpty: api.inputGeoPointEmpty,
    inputGeoPoint: api.inputGeoPoint,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.inputGeoPointEmpty.constructor_id => return .{ .inputGeoPointEmpty = try api.inputGeoPointEmpty.deserialize(allocator, r) },
            api.inputGeoPoint.constructor_id => return .{ .inputGeoPoint = try api.inputGeoPoint.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const InputPhoto = union(enum) {
    inputPhotoEmpty: api.inputPhotoEmpty,
    inputPhoto: api.inputPhoto,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.inputPhotoEmpty.constructor_id => return .{ .inputPhotoEmpty = try api.inputPhotoEmpty.deserialize(allocator, r) },
            api.inputPhoto.constructor_id => return .{ .inputPhoto = try api.inputPhoto.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const Photo = union(enum) {
    photoEmpty: api.photoEmpty,
    photo: api.photo,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.photoEmpty.constructor_id => return .{ .photoEmpty = try api.photoEmpty.deserialize(allocator, r) },
            api.photo.constructor_id => return .{ .photo = try api.photo.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const GeoPoint = union(enum) {
    geoPointEmpty: api.geoPointEmpty,
    geoPoint: api.geoPoint,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.geoPointEmpty.constructor_id => return .{ .geoPointEmpty = try api.geoPointEmpty.deserialize(allocator, r) },
            api.geoPoint.constructor_id => return .{ .geoPoint = try api.geoPoint.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const Photos = union(enum) {
    photos_: api.photos.photos_,
    photosSlice: api.photos.photosSlice,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.photos.photos_.constructor_id => return .{ .photos_ = try api.photos.photos_.deserialize(allocator, r) },
            api.photos.photosSlice.constructor_id => return .{ .photosSlice = try api.photos.photosSlice.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const Photo_ = union(enum) {
    photo: api.photos.photo,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.photos.photo.constructor_id => return .{ .photo = try api.photos.photo.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const InputDocument = union(enum) {
    inputDocumentEmpty: api.inputDocumentEmpty,
    inputDocument: api.inputDocument,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.inputDocumentEmpty.constructor_id => return .{ .inputDocumentEmpty = try api.inputDocumentEmpty.deserialize(allocator, r) },
            api.inputDocument.constructor_id => return .{ .inputDocument = try api.inputDocument.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const Document = union(enum) {
    documentEmpty: api.documentEmpty,
    document: api.document,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.documentEmpty.constructor_id => return .{ .documentEmpty = try api.documentEmpty.deserialize(allocator, r) },
            api.document.constructor_id => return .{ .document = try api.document.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const DocumentAttribute = union(enum) {
    documentAttributeImageSize: api.documentAttributeImageSize,
    documentAttributeAnimated: api.documentAttributeAnimated,
    documentAttributeSticker: api.documentAttributeSticker,
    documentAttributeVideo: api.documentAttributeVideo,
    documentAttributeAudio: api.documentAttributeAudio,
    documentAttributeFilename: api.documentAttributeFilename,
    documentAttributeHasStickers: api.documentAttributeHasStickers,
    documentAttributeCustomEmoji: api.documentAttributeCustomEmoji,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.documentAttributeImageSize.constructor_id => return .{ .documentAttributeImageSize = try api.documentAttributeImageSize.deserialize(allocator, r) },
            api.documentAttributeAnimated.constructor_id => return .{ .documentAttributeAnimated = try api.documentAttributeAnimated.deserialize(allocator, r) },
            api.documentAttributeSticker.constructor_id => return .{ .documentAttributeSticker = try api.documentAttributeSticker.deserialize(allocator, r) },
            api.documentAttributeVideo.constructor_id => return .{ .documentAttributeVideo = try api.documentAttributeVideo.deserialize(allocator, r) },
            api.documentAttributeAudio.constructor_id => return .{ .documentAttributeAudio = try api.documentAttributeAudio.deserialize(allocator, r) },
            api.documentAttributeFilename.constructor_id => return .{ .documentAttributeFilename = try api.documentAttributeFilename.deserialize(allocator, r) },
            api.documentAttributeHasStickers.constructor_id => return .{ .documentAttributeHasStickers = try api.documentAttributeHasStickers.deserialize(allocator, r) },
            api.documentAttributeCustomEmoji.constructor_id => return .{ .documentAttributeCustomEmoji = try api.documentAttributeCustomEmoji.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const MaskCoords = union(enum) {
    maskCoords: api.maskCoords,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.maskCoords.constructor_id => return .{ .maskCoords = try api.maskCoords.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const WebDocument = union(enum) {
    webDocument: api.webDocument,
    webDocumentNoProxy: api.webDocumentNoProxy,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.webDocument.constructor_id => return .{ .webDocument = try api.webDocument.deserialize(allocator, r) },
            api.webDocumentNoProxy.constructor_id => return .{ .webDocumentNoProxy = try api.webDocumentNoProxy.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const InputWebDocument = union(enum) {
    inputWebDocument: api.inputWebDocument,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.inputWebDocument.constructor_id => return .{ .inputWebDocument = try api.inputWebDocument.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const PhoneConnection = union(enum) {
    phoneConnection: api.phoneConnection,
    phoneConnectionWebrtc: api.phoneConnectionWebrtc,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.phoneConnection.constructor_id => return .{ .phoneConnection = try api.phoneConnection.deserialize(allocator, r) },
            api.phoneConnectionWebrtc.constructor_id => return .{ .phoneConnectionWebrtc = try api.phoneConnectionWebrtc.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const VideoSize = union(enum) {
    videoSize: api.videoSize,
    videoSizeEmojiMarkup: api.videoSizeEmojiMarkup,
    videoSizeStickerMarkup: api.videoSizeStickerMarkup,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.videoSize.constructor_id => return .{ .videoSize = try api.videoSize.deserialize(allocator, r) },
            api.videoSizeEmojiMarkup.constructor_id => return .{ .videoSizeEmojiMarkup = try api.videoSizeEmojiMarkup.deserialize(allocator, r) },
            api.videoSizeStickerMarkup.constructor_id => return .{ .videoSizeStickerMarkup = try api.videoSizeStickerMarkup.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const MediaAreaCoordinates = union(enum) {
    mediaAreaCoordinates: api.mediaAreaCoordinates,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.mediaAreaCoordinates.constructor_id => return .{ .mediaAreaCoordinates = try api.mediaAreaCoordinates.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const MediaArea = union(enum) {
    mediaAreaVenue: api.mediaAreaVenue,
    inputMediaAreaVenue: api.inputMediaAreaVenue,
    mediaAreaGeoPoint: api.mediaAreaGeoPoint,
    mediaAreaSuggestedReaction: api.mediaAreaSuggestedReaction,
    mediaAreaChannelPost: api.mediaAreaChannelPost,
    inputMediaAreaChannelPost: api.inputMediaAreaChannelPost,
    mediaAreaUrl: api.mediaAreaUrl,
    mediaAreaWeather: api.mediaAreaWeather,
    mediaAreaStarGift: api.mediaAreaStarGift,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.mediaAreaVenue.constructor_id => return .{ .mediaAreaVenue = try api.mediaAreaVenue.deserialize(allocator, r) },
            api.inputMediaAreaVenue.constructor_id => return .{ .inputMediaAreaVenue = try api.inputMediaAreaVenue.deserialize(allocator, r) },
            api.mediaAreaGeoPoint.constructor_id => return .{ .mediaAreaGeoPoint = try api.mediaAreaGeoPoint.deserialize(allocator, r) },
            api.mediaAreaSuggestedReaction.constructor_id => return .{ .mediaAreaSuggestedReaction = try api.mediaAreaSuggestedReaction.deserialize(allocator, r) },
            api.mediaAreaChannelPost.constructor_id => return .{ .mediaAreaChannelPost = try api.mediaAreaChannelPost.deserialize(allocator, r) },
            api.inputMediaAreaChannelPost.constructor_id => return .{ .inputMediaAreaChannelPost = try api.inputMediaAreaChannelPost.deserialize(allocator, r) },
            api.mediaAreaUrl.constructor_id => return .{ .mediaAreaUrl = try api.mediaAreaUrl.deserialize(allocator, r) },
            api.mediaAreaWeather.constructor_id => return .{ .mediaAreaWeather = try api.mediaAreaWeather.deserialize(allocator, r) },
            api.mediaAreaStarGift.constructor_id => return .{ .mediaAreaStarGift = try api.mediaAreaStarGift.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const GeoPointAddress = union(enum) {
    geoPointAddress: api.geoPointAddress,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.geoPointAddress.constructor_id => return .{ .geoPointAddress = try api.geoPointAddress.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};


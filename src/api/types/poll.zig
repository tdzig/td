//! GENERATED by td TL code generator from "api.tl".
//! Do not edit by hand; regenerate with td-gen.

const std = @import("std");
const td = @import("../../root.zig");
const Writer = td.tl.Writer;
const Reader = td.tl.Reader;
const TlError = td.TlError;
const vector_constructor_id = td.tl.vector_constructor_id;

const api = @import("../mod.zig");

pub const pollAnswer = struct {
    pub const constructor_id: u32 = 0x4b7d786a;
    text: api.TextWithEntities,
    option: []const u8,
    media: ?*api.MessageMedia = null,
    added_by: ?api.Peer = null,
    date: ?i32 = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.media != null) flags |= 1 << 0;
        if (self.added_by != null) flags |= 1 << 1;
        if (self.date != null) flags |= 1 << 1;
        try w.writeUInt(flags);
        try self.text.serialize(w);
        try w.writeBytes(self.option);
        if (self.media) |v| {
            try v.serialize(w);
        }
        if (self.added_by) |v| {
            try v.serialize(w);
        }
        if (self.date) |v| {
            try w.writeInt(v);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.text = try api.TextWithEntities.deserialize(allocator, r);
        self.option = try r.readBytes();
        if ((flags >> 0) & 1 != 0) {
                        const ptr_2231 = allocator.create(api.MessageMedia) catch return error.OutOfMemory;
            ptr_2231.* = try api.MessageMedia.deserialize(allocator, r);
            self.media = ptr_2231;
        } else {
            self.media = null;
        }
        if ((flags >> 1) & 1 != 0) {
            self.added_by = try api.Peer.deserialize(allocator, r);
        } else {
            self.added_by = null;
        }
        if ((flags >> 1) & 1 != 0) {
            self.date = try r.readInt();
        } else {
            self.date = null;
        }
        return self;
    }
};

pub const inputPollAnswer = struct {
    pub const constructor_id: u32 = 0x199fed96;
    text: api.TextWithEntities,
    media: ?*api.InputMedia = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.media != null) flags |= 1 << 0;
        try w.writeUInt(flags);
        try self.text.serialize(w);
        if (self.media) |v| {
            try v.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.text = try api.TextWithEntities.deserialize(allocator, r);
        if ((flags >> 0) & 1 != 0) {
                        const ptr_2232 = allocator.create(api.InputMedia) catch return error.OutOfMemory;
            ptr_2232.* = try api.InputMedia.deserialize(allocator, r);
            self.media = ptr_2232;
        } else {
            self.media = null;
        }
        return self;
    }
};

pub const poll = struct {
    pub const constructor_id: u32 = 0x966e2dbf;
    id: i64,
    closed: bool = false,
    public_voters: bool = false,
    multiple_choice: bool = false,
    quiz: bool = false,
    open_answers: bool = false,
    revoting_disabled: bool = false,
    shuffle_answers: bool = false,
    hide_results_until_close: bool = false,
    creator: bool = false,
    subscribers_only: bool = false,
    question: api.TextWithEntities,
    answers: []*api.PollAnswer,
    close_period: ?i32 = null,
    close_date: ?i32 = null,
    countries_iso2: ?[][]const u8 = null,
    hash: i64,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.closed) flags |= 1 << 0;
        if (self.public_voters) flags |= 1 << 1;
        if (self.multiple_choice) flags |= 1 << 2;
        if (self.quiz) flags |= 1 << 3;
        if (self.open_answers) flags |= 1 << 6;
        if (self.revoting_disabled) flags |= 1 << 7;
        if (self.shuffle_answers) flags |= 1 << 8;
        if (self.hide_results_until_close) flags |= 1 << 9;
        if (self.creator) flags |= 1 << 10;
        if (self.subscribers_only) flags |= 1 << 11;
        if (self.close_period != null) flags |= 1 << 4;
        if (self.close_date != null) flags |= 1 << 5;
        if (self.countries_iso2 != null) flags |= 1 << 12;
        try w.writeLong(self.id);
        try w.writeUInt(flags);
        try self.question.serialize(w);
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.answers.len);
        for (self.answers) |elem_2233| {
            try elem_2233.serialize(w);
        }
        if (self.close_period) |v| {
            try w.writeInt(v);
        }
        if (self.close_date) |v| {
            try w.writeInt(v);
        }
        if (self.countries_iso2) |v| {
            try w.writeUInt(vector_constructor_id);
            try w.writeVectorLength(v.len);
            for (v) |elem_2234| {
                try w.writeString(elem_2234);
            }
        }
        try w.writeLong(self.hash);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.id = try r.readLong();
        const flags = try r.readUInt();
        self.closed = (flags >> 0) & 1 != 0;
        self.public_voters = (flags >> 1) & 1 != 0;
        self.multiple_choice = (flags >> 2) & 1 != 0;
        self.quiz = (flags >> 3) & 1 != 0;
        self.open_answers = (flags >> 6) & 1 != 0;
        self.revoting_disabled = (flags >> 7) & 1 != 0;
        self.shuffle_answers = (flags >> 8) & 1 != 0;
        self.hide_results_until_close = (flags >> 9) & 1 != 0;
        self.creator = (flags >> 10) & 1 != 0;
        self.subscribers_only = (flags >> 11) & 1 != 0;
        self.question = try api.TextWithEntities.deserialize(allocator, r);
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_2235 = try r.readVectorLength();
        const vec_2236 = allocator.alloc(*api.PollAnswer, len_2235) catch return error.OutOfMemory;
        for (vec_2236) |*elem_2237| {
                        const ptr_2238 = allocator.create(api.PollAnswer) catch return error.OutOfMemory;
            ptr_2238.* = try api.PollAnswer.deserialize(allocator, r);
            elem_2237.* = ptr_2238;
        }
        self.answers = vec_2236;
        if ((flags >> 4) & 1 != 0) {
            self.close_period = try r.readInt();
        } else {
            self.close_period = null;
        }
        if ((flags >> 5) & 1 != 0) {
            self.close_date = try r.readInt();
        } else {
            self.close_date = null;
        }
        if ((flags >> 12) & 1 != 0) {
            if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
            const len_2239 = try r.readVectorLength();
            const vec_2240 = allocator.alloc([]const u8, len_2239) catch return error.OutOfMemory;
            for (vec_2240) |*elem_2241| {
                elem_2241.* = try r.readString();
            }
            self.countries_iso2 = vec_2240;
        } else {
            self.countries_iso2 = null;
        }
        self.hash = try r.readLong();
        return self;
    }
};

pub const pollAnswerVoters = struct {
    pub const constructor_id: u32 = 0x3645230a;
    chosen: bool = false,
    correct: bool = false,
    option: []const u8,
    voters: ?i32 = null,
    recent_voters: ?[]api.Peer = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.chosen) flags |= 1 << 0;
        if (self.correct) flags |= 1 << 1;
        if (self.voters != null) flags |= 1 << 2;
        if (self.recent_voters != null) flags |= 1 << 2;
        try w.writeUInt(flags);
        try w.writeBytes(self.option);
        if (self.voters) |v| {
            try w.writeInt(v);
        }
        if (self.recent_voters) |v| {
            try w.writeUInt(vector_constructor_id);
            try w.writeVectorLength(v.len);
            for (v) |elem_2242| {
                try elem_2242.serialize(w);
            }
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.chosen = (flags >> 0) & 1 != 0;
        self.correct = (flags >> 1) & 1 != 0;
        self.option = try r.readBytes();
        if ((flags >> 2) & 1 != 0) {
            self.voters = try r.readInt();
        } else {
            self.voters = null;
        }
        if ((flags >> 2) & 1 != 0) {
            if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
            const len_2243 = try r.readVectorLength();
            const vec_2244 = allocator.alloc(api.Peer, len_2243) catch return error.OutOfMemory;
            for (vec_2244) |*elem_2245| {
                elem_2245.* = try api.Peer.deserialize(allocator, r);
            }
            self.recent_voters = vec_2244;
        } else {
            self.recent_voters = null;
        }
        return self;
    }
};

pub const pollResults = struct {
    pub const constructor_id: u32 = 0xba7bb15e;
    min: bool = false,
    has_unread_votes: bool = false,
    can_view_stats: bool = false,
    results: ?[]api.PollAnswerVoters = null,
    total_voters: ?i32 = null,
    recent_voters: ?[]api.Peer = null,
    solution: ?[]const u8 = null,
    solution_entities: ?[]api.MessageEntity = null,
    solution_media: ?*api.MessageMedia = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.min) flags |= 1 << 0;
        if (self.has_unread_votes) flags |= 1 << 6;
        if (self.can_view_stats) flags |= 1 << 7;
        if (self.results != null) flags |= 1 << 1;
        if (self.total_voters != null) flags |= 1 << 2;
        if (self.recent_voters != null) flags |= 1 << 3;
        if (self.solution != null) flags |= 1 << 4;
        if (self.solution_entities != null) flags |= 1 << 4;
        if (self.solution_media != null) flags |= 1 << 5;
        try w.writeUInt(flags);
        if (self.results) |v| {
            try w.writeUInt(vector_constructor_id);
            try w.writeVectorLength(v.len);
            for (v) |elem_2246| {
                try elem_2246.serialize(w);
            }
        }
        if (self.total_voters) |v| {
            try w.writeInt(v);
        }
        if (self.recent_voters) |v| {
            try w.writeUInt(vector_constructor_id);
            try w.writeVectorLength(v.len);
            for (v) |elem_2247| {
                try elem_2247.serialize(w);
            }
        }
        if (self.solution) |v| {
            try w.writeString(v);
        }
        if (self.solution_entities) |v| {
            try w.writeUInt(vector_constructor_id);
            try w.writeVectorLength(v.len);
            for (v) |elem_2248| {
                try elem_2248.serialize(w);
            }
        }
        if (self.solution_media) |v| {
            try v.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.min = (flags >> 0) & 1 != 0;
        self.has_unread_votes = (flags >> 6) & 1 != 0;
        self.can_view_stats = (flags >> 7) & 1 != 0;
        if ((flags >> 1) & 1 != 0) {
            if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
            const len_2249 = try r.readVectorLength();
            const vec_2250 = allocator.alloc(api.PollAnswerVoters, len_2249) catch return error.OutOfMemory;
            for (vec_2250) |*elem_2251| {
                elem_2251.* = try api.PollAnswerVoters.deserialize(allocator, r);
            }
            self.results = vec_2250;
        } else {
            self.results = null;
        }
        if ((flags >> 2) & 1 != 0) {
            self.total_voters = try r.readInt();
        } else {
            self.total_voters = null;
        }
        if ((flags >> 3) & 1 != 0) {
            if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
            const len_2252 = try r.readVectorLength();
            const vec_2253 = allocator.alloc(api.Peer, len_2252) catch return error.OutOfMemory;
            for (vec_2253) |*elem_2254| {
                elem_2254.* = try api.Peer.deserialize(allocator, r);
            }
            self.recent_voters = vec_2253;
        } else {
            self.recent_voters = null;
        }
        if ((flags >> 4) & 1 != 0) {
            self.solution = try r.readString();
        } else {
            self.solution = null;
        }
        if ((flags >> 4) & 1 != 0) {
            if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
            const len_2255 = try r.readVectorLength();
            const vec_2256 = allocator.alloc(api.MessageEntity, len_2255) catch return error.OutOfMemory;
            for (vec_2256) |*elem_2257| {
                elem_2257.* = try api.MessageEntity.deserialize(allocator, r);
            }
            self.solution_entities = vec_2256;
        } else {
            self.solution_entities = null;
        }
        if ((flags >> 5) & 1 != 0) {
                        const ptr_2258 = allocator.create(api.MessageMedia) catch return error.OutOfMemory;
            ptr_2258.* = try api.MessageMedia.deserialize(allocator, r);
            self.solution_media = ptr_2258;
        } else {
            self.solution_media = null;
        }
        return self;
    }
};

pub const PollAnswer = union(enum) {
    pollAnswer: api.pollAnswer,
    inputPollAnswer: api.inputPollAnswer,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.pollAnswer.constructor_id => return .{ .pollAnswer = try api.pollAnswer.deserialize(allocator, r) },
            api.inputPollAnswer.constructor_id => return .{ .inputPollAnswer = try api.inputPollAnswer.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const Poll = union(enum) {
    poll: api.poll,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.poll.constructor_id => return .{ .poll = try api.poll.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const PollAnswerVoters = union(enum) {
    pollAnswerVoters: api.pollAnswerVoters,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.pollAnswerVoters.constructor_id => return .{ .pollAnswerVoters = try api.pollAnswerVoters.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const PollResults = union(enum) {
    pollResults: api.pollResults,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.pollResults.constructor_id => return .{ .pollResults = try api.pollResults.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};


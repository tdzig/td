//! GENERATED by td TL code generator from "api.tl".
//! Do not edit by hand; regenerate with td-gen.

const std = @import("std");
const td = @import("../../root.zig");
const Writer = td.tl.Writer;
const Reader = td.tl.Reader;
const TlError = td.TlError;
const vector_constructor_id = td.tl.vector_constructor_id;

const api = @import("../mod.zig");

pub const prepaidGiveaway = struct {
    pub const constructor_id: u32 = 0xb2539d54;
    id: i64,
    months: i32,
    quantity: i32,
    date: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeLong(self.id);
        try w.writeInt(self.months);
        try w.writeInt(self.quantity);
        try w.writeInt(self.date);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.id = try r.readLong();
        self.months = try r.readInt();
        self.quantity = try r.readInt();
        self.date = try r.readInt();
        _ = allocator;
        return self;
    }
};

pub const prepaidStarsGiveaway = struct {
    pub const constructor_id: u32 = 0x9a9d77e0;
    id: i64,
    stars: i64,
    quantity: i32,
    boosts: i32,
    date: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeLong(self.id);
        try w.writeLong(self.stars);
        try w.writeInt(self.quantity);
        try w.writeInt(self.boosts);
        try w.writeInt(self.date);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.id = try r.readLong();
        self.stars = try r.readLong();
        self.quantity = try r.readInt();
        self.boosts = try r.readInt();
        self.date = try r.readInt();
        _ = allocator;
        return self;
    }
};

pub const starsTransactionPeerUnsupported = struct {
    pub const constructor_id: u32 = 0x95f2bfe4;

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        _ = self;
        _ = w;
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        _ = allocator;
        _ = r;
        return .{};
    }
};

pub const starsTransactionPeerAppStore = struct {
    pub const constructor_id: u32 = 0xb457b375;

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        _ = self;
        _ = w;
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        _ = allocator;
        _ = r;
        return .{};
    }
};

pub const starsTransactionPeerPlayMarket = struct {
    pub const constructor_id: u32 = 0x7b560a0b;

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        _ = self;
        _ = w;
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        _ = allocator;
        _ = r;
        return .{};
    }
};

pub const starsTransactionPeerPremiumBot = struct {
    pub const constructor_id: u32 = 0x250dbaf8;

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        _ = self;
        _ = w;
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        _ = allocator;
        _ = r;
        return .{};
    }
};

pub const starsTransactionPeerFragment = struct {
    pub const constructor_id: u32 = 0xe92fd902;

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        _ = self;
        _ = w;
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        _ = allocator;
        _ = r;
        return .{};
    }
};

pub const starsTransactionPeer = struct {
    pub const constructor_id: u32 = 0xd80da15d;
    peer: api.Peer,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try self.peer.serialize(w);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.peer = try api.Peer.deserialize(allocator, r);
        return self;
    }
};

pub const starsTransactionPeerAds = struct {
    pub const constructor_id: u32 = 0x60682812;

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        _ = self;
        _ = w;
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        _ = allocator;
        _ = r;
        return .{};
    }
};

pub const starsTransactionPeerAPI = struct {
    pub const constructor_id: u32 = 0xf9677aad;

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        _ = self;
        _ = w;
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        _ = allocator;
        _ = r;
        return .{};
    }
};

pub const starsTopupOption = struct {
    pub const constructor_id: u32 = 0x0bd915c0;
    extended: bool = false,
    stars: i64,
    store_product: ?[]const u8 = null,
    currency: []const u8,
    amount: i64,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.extended) flags |= 1 << 1;
        if (self.store_product != null) flags |= 1 << 0;
        try w.writeUInt(flags);
        try w.writeLong(self.stars);
        if (self.store_product) |v| {
            try w.writeString(v);
        }
        try w.writeString(self.currency);
        try w.writeLong(self.amount);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.extended = (flags >> 1) & 1 != 0;
        self.stars = try r.readLong();
        if ((flags >> 0) & 1 != 0) {
            self.store_product = try r.readString();
        } else {
            self.store_product = null;
        }
        self.currency = try r.readString();
        self.amount = try r.readLong();
        _ = allocator;
        return self;
    }
};

pub const starsTransaction = struct {
    pub const constructor_id: u32 = 0x13659eb0;
    refund: bool = false,
    pending: bool = false,
    failed: bool = false,
    gift: bool = false,
    reaction: bool = false,
    stargift_upgrade: bool = false,
    business_transfer: bool = false,
    stargift_resale: bool = false,
    posts_search: bool = false,
    stargift_prepaid_upgrade: bool = false,
    stargift_drop_original_details: bool = false,
    phonegroup_message: bool = false,
    stargift_auction_bid: bool = false,
    offer: bool = false,
    id: []const u8,
    amount: api.StarsAmount,
    date: i32,
    peer: api.StarsTransactionPeer,
    title: ?[]const u8 = null,
    description: ?[]const u8 = null,
    photo: ?api.WebDocument = null,
    transaction_date: ?i32 = null,
    transaction_url: ?[]const u8 = null,
    bot_payload: ?[]const u8 = null,
    msg_id: ?i32 = null,
    extended_media: ?[]*api.MessageMedia = null,
    subscription_period: ?i32 = null,
    giveaway_post_id: ?i32 = null,
    stargift: ?api.StarGift = null,
    floodskip_number: ?i32 = null,
    starref_commission_permille: ?i32 = null,
    starref_peer: ?api.Peer = null,
    starref_amount: ?api.StarsAmount = null,
    paid_messages: ?i32 = null,
    premium_gift_months: ?i32 = null,
    ads_proceeds_from_date: ?i32 = null,
    ads_proceeds_to_date: ?i32 = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.refund) flags |= 1 << 3;
        if (self.pending) flags |= 1 << 4;
        if (self.failed) flags |= 1 << 6;
        if (self.gift) flags |= 1 << 10;
        if (self.reaction) flags |= 1 << 11;
        if (self.stargift_upgrade) flags |= 1 << 18;
        if (self.business_transfer) flags |= 1 << 21;
        if (self.stargift_resale) flags |= 1 << 22;
        if (self.posts_search) flags |= 1 << 24;
        if (self.stargift_prepaid_upgrade) flags |= 1 << 25;
        if (self.stargift_drop_original_details) flags |= 1 << 26;
        if (self.phonegroup_message) flags |= 1 << 27;
        if (self.stargift_auction_bid) flags |= 1 << 28;
        if (self.offer) flags |= 1 << 29;
        if (self.title != null) flags |= 1 << 0;
        if (self.description != null) flags |= 1 << 1;
        if (self.photo != null) flags |= 1 << 2;
        if (self.transaction_date != null) flags |= 1 << 5;
        if (self.transaction_url != null) flags |= 1 << 5;
        if (self.bot_payload != null) flags |= 1 << 7;
        if (self.msg_id != null) flags |= 1 << 8;
        if (self.extended_media != null) flags |= 1 << 9;
        if (self.subscription_period != null) flags |= 1 << 12;
        if (self.giveaway_post_id != null) flags |= 1 << 13;
        if (self.stargift != null) flags |= 1 << 14;
        if (self.floodskip_number != null) flags |= 1 << 15;
        if (self.starref_commission_permille != null) flags |= 1 << 16;
        if (self.starref_peer != null) flags |= 1 << 17;
        if (self.starref_amount != null) flags |= 1 << 17;
        if (self.paid_messages != null) flags |= 1 << 19;
        if (self.premium_gift_months != null) flags |= 1 << 20;
        if (self.ads_proceeds_from_date != null) flags |= 1 << 23;
        if (self.ads_proceeds_to_date != null) flags |= 1 << 23;
        try w.writeUInt(flags);
        try w.writeString(self.id);
        try self.amount.serialize(w);
        try w.writeInt(self.date);
        try self.peer.serialize(w);
        if (self.title) |v| {
            try w.writeString(v);
        }
        if (self.description) |v| {
            try w.writeString(v);
        }
        if (self.photo) |v| {
            try v.serialize(w);
        }
        if (self.transaction_date) |v| {
            try w.writeInt(v);
        }
        if (self.transaction_url) |v| {
            try w.writeString(v);
        }
        if (self.bot_payload) |v| {
            try w.writeBytes(v);
        }
        if (self.msg_id) |v| {
            try w.writeInt(v);
        }
        if (self.extended_media) |v| {
            try w.writeUInt(vector_constructor_id);
            try w.writeVectorLength(v.len);
            for (v) |elem_2490| {
                try elem_2490.serialize(w);
            }
        }
        if (self.subscription_period) |v| {
            try w.writeInt(v);
        }
        if (self.giveaway_post_id) |v| {
            try w.writeInt(v);
        }
        if (self.stargift) |v| {
            try v.serialize(w);
        }
        if (self.floodskip_number) |v| {
            try w.writeInt(v);
        }
        if (self.starref_commission_permille) |v| {
            try w.writeInt(v);
        }
        if (self.starref_peer) |v| {
            try v.serialize(w);
        }
        if (self.starref_amount) |v| {
            try v.serialize(w);
        }
        if (self.paid_messages) |v| {
            try w.writeInt(v);
        }
        if (self.premium_gift_months) |v| {
            try w.writeInt(v);
        }
        if (self.ads_proceeds_from_date) |v| {
            try w.writeInt(v);
        }
        if (self.ads_proceeds_to_date) |v| {
            try w.writeInt(v);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.refund = (flags >> 3) & 1 != 0;
        self.pending = (flags >> 4) & 1 != 0;
        self.failed = (flags >> 6) & 1 != 0;
        self.gift = (flags >> 10) & 1 != 0;
        self.reaction = (flags >> 11) & 1 != 0;
        self.stargift_upgrade = (flags >> 18) & 1 != 0;
        self.business_transfer = (flags >> 21) & 1 != 0;
        self.stargift_resale = (flags >> 22) & 1 != 0;
        self.posts_search = (flags >> 24) & 1 != 0;
        self.stargift_prepaid_upgrade = (flags >> 25) & 1 != 0;
        self.stargift_drop_original_details = (flags >> 26) & 1 != 0;
        self.phonegroup_message = (flags >> 27) & 1 != 0;
        self.stargift_auction_bid = (flags >> 28) & 1 != 0;
        self.offer = (flags >> 29) & 1 != 0;
        self.id = try r.readString();
        self.amount = try api.StarsAmount.deserialize(allocator, r);
        self.date = try r.readInt();
        self.peer = try api.StarsTransactionPeer.deserialize(allocator, r);
        if ((flags >> 0) & 1 != 0) {
            self.title = try r.readString();
        } else {
            self.title = null;
        }
        if ((flags >> 1) & 1 != 0) {
            self.description = try r.readString();
        } else {
            self.description = null;
        }
        if ((flags >> 2) & 1 != 0) {
            self.photo = try api.WebDocument.deserialize(allocator, r);
        } else {
            self.photo = null;
        }
        if ((flags >> 5) & 1 != 0) {
            self.transaction_date = try r.readInt();
        } else {
            self.transaction_date = null;
        }
        if ((flags >> 5) & 1 != 0) {
            self.transaction_url = try r.readString();
        } else {
            self.transaction_url = null;
        }
        if ((flags >> 7) & 1 != 0) {
            self.bot_payload = try r.readBytes();
        } else {
            self.bot_payload = null;
        }
        if ((flags >> 8) & 1 != 0) {
            self.msg_id = try r.readInt();
        } else {
            self.msg_id = null;
        }
        if ((flags >> 9) & 1 != 0) {
            if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
            const len_2491 = try r.readVectorLength();
            const vec_2492 = allocator.alloc(*api.MessageMedia, len_2491) catch return error.OutOfMemory;
            for (vec_2492) |*elem_2493| {
                                const ptr_2494 = allocator.create(api.MessageMedia) catch return error.OutOfMemory;
                ptr_2494.* = try api.MessageMedia.deserialize(allocator, r);
                elem_2493.* = ptr_2494;
            }
            self.extended_media = vec_2492;
        } else {
            self.extended_media = null;
        }
        if ((flags >> 12) & 1 != 0) {
            self.subscription_period = try r.readInt();
        } else {
            self.subscription_period = null;
        }
        if ((flags >> 13) & 1 != 0) {
            self.giveaway_post_id = try r.readInt();
        } else {
            self.giveaway_post_id = null;
        }
        if ((flags >> 14) & 1 != 0) {
            self.stargift = try api.StarGift.deserialize(allocator, r);
        } else {
            self.stargift = null;
        }
        if ((flags >> 15) & 1 != 0) {
            self.floodskip_number = try r.readInt();
        } else {
            self.floodskip_number = null;
        }
        if ((flags >> 16) & 1 != 0) {
            self.starref_commission_permille = try r.readInt();
        } else {
            self.starref_commission_permille = null;
        }
        if ((flags >> 17) & 1 != 0) {
            self.starref_peer = try api.Peer.deserialize(allocator, r);
        } else {
            self.starref_peer = null;
        }
        if ((flags >> 17) & 1 != 0) {
            self.starref_amount = try api.StarsAmount.deserialize(allocator, r);
        } else {
            self.starref_amount = null;
        }
        if ((flags >> 19) & 1 != 0) {
            self.paid_messages = try r.readInt();
        } else {
            self.paid_messages = null;
        }
        if ((flags >> 20) & 1 != 0) {
            self.premium_gift_months = try r.readInt();
        } else {
            self.premium_gift_months = null;
        }
        if ((flags >> 23) & 1 != 0) {
            self.ads_proceeds_from_date = try r.readInt();
        } else {
            self.ads_proceeds_from_date = null;
        }
        if ((flags >> 23) & 1 != 0) {
            self.ads_proceeds_to_date = try r.readInt();
        } else {
            self.ads_proceeds_to_date = null;
        }
        return self;
    }
};

pub const starsRevenueStatus = struct {
    pub const constructor_id: u32 = 0xfebe5491;
    withdrawal_enabled: bool = false,
    current_balance: api.StarsAmount,
    available_balance: api.StarsAmount,
    overall_revenue: api.StarsAmount,
    next_withdrawal_at: ?i32 = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.withdrawal_enabled) flags |= 1 << 0;
        if (self.next_withdrawal_at != null) flags |= 1 << 1;
        try w.writeUInt(flags);
        try self.current_balance.serialize(w);
        try self.available_balance.serialize(w);
        try self.overall_revenue.serialize(w);
        if (self.next_withdrawal_at) |v| {
            try w.writeInt(v);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.withdrawal_enabled = (flags >> 0) & 1 != 0;
        self.current_balance = try api.StarsAmount.deserialize(allocator, r);
        self.available_balance = try api.StarsAmount.deserialize(allocator, r);
        self.overall_revenue = try api.StarsAmount.deserialize(allocator, r);
        if ((flags >> 1) & 1 != 0) {
            self.next_withdrawal_at = try r.readInt();
        } else {
            self.next_withdrawal_at = null;
        }
        return self;
    }
};

pub const inputStarsTransaction = struct {
    pub const constructor_id: u32 = 0x206ae6d1;
    refund: bool = false,
    id: []const u8,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.refund) flags |= 1 << 0;
        try w.writeUInt(flags);
        try w.writeString(self.id);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.refund = (flags >> 0) & 1 != 0;
        self.id = try r.readString();
        _ = allocator;
        return self;
    }
};

pub const starsGiftOption = struct {
    pub const constructor_id: u32 = 0x5e0589f1;
    extended: bool = false,
    stars: i64,
    store_product: ?[]const u8 = null,
    currency: []const u8,
    amount: i64,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.extended) flags |= 1 << 1;
        if (self.store_product != null) flags |= 1 << 0;
        try w.writeUInt(flags);
        try w.writeLong(self.stars);
        if (self.store_product) |v| {
            try w.writeString(v);
        }
        try w.writeString(self.currency);
        try w.writeLong(self.amount);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.extended = (flags >> 1) & 1 != 0;
        self.stars = try r.readLong();
        if ((flags >> 0) & 1 != 0) {
            self.store_product = try r.readString();
        } else {
            self.store_product = null;
        }
        self.currency = try r.readString();
        self.amount = try r.readLong();
        _ = allocator;
        return self;
    }
};

pub const starsSubscriptionPricing = struct {
    pub const constructor_id: u32 = 0x05416d58;
    period: i32,
    amount: i64,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeInt(self.period);
        try w.writeLong(self.amount);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.period = try r.readInt();
        self.amount = try r.readLong();
        _ = allocator;
        return self;
    }
};

pub const starsSubscription = struct {
    pub const constructor_id: u32 = 0x2e6eab1a;
    canceled: bool = false,
    can_refulfill: bool = false,
    missing_balance: bool = false,
    bot_canceled: bool = false,
    id: []const u8,
    peer: api.Peer,
    until_date: i32,
    pricing: api.StarsSubscriptionPricing,
    chat_invite_hash: ?[]const u8 = null,
    title: ?[]const u8 = null,
    photo: ?api.WebDocument = null,
    invoice_slug: ?[]const u8 = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.canceled) flags |= 1 << 0;
        if (self.can_refulfill) flags |= 1 << 1;
        if (self.missing_balance) flags |= 1 << 2;
        if (self.bot_canceled) flags |= 1 << 7;
        if (self.chat_invite_hash != null) flags |= 1 << 3;
        if (self.title != null) flags |= 1 << 4;
        if (self.photo != null) flags |= 1 << 5;
        if (self.invoice_slug != null) flags |= 1 << 6;
        try w.writeUInt(flags);
        try w.writeString(self.id);
        try self.peer.serialize(w);
        try w.writeInt(self.until_date);
        try self.pricing.serialize(w);
        if (self.chat_invite_hash) |v| {
            try w.writeString(v);
        }
        if (self.title) |v| {
            try w.writeString(v);
        }
        if (self.photo) |v| {
            try v.serialize(w);
        }
        if (self.invoice_slug) |v| {
            try w.writeString(v);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.canceled = (flags >> 0) & 1 != 0;
        self.can_refulfill = (flags >> 1) & 1 != 0;
        self.missing_balance = (flags >> 2) & 1 != 0;
        self.bot_canceled = (flags >> 7) & 1 != 0;
        self.id = try r.readString();
        self.peer = try api.Peer.deserialize(allocator, r);
        self.until_date = try r.readInt();
        self.pricing = try api.StarsSubscriptionPricing.deserialize(allocator, r);
        if ((flags >> 3) & 1 != 0) {
            self.chat_invite_hash = try r.readString();
        } else {
            self.chat_invite_hash = null;
        }
        if ((flags >> 4) & 1 != 0) {
            self.title = try r.readString();
        } else {
            self.title = null;
        }
        if ((flags >> 5) & 1 != 0) {
            self.photo = try api.WebDocument.deserialize(allocator, r);
        } else {
            self.photo = null;
        }
        if ((flags >> 6) & 1 != 0) {
            self.invoice_slug = try r.readString();
        } else {
            self.invoice_slug = null;
        }
        return self;
    }
};

pub const starsGiveawayOption = struct {
    pub const constructor_id: u32 = 0x94ce852a;
    extended: bool = false,
    default: bool = false,
    stars: i64,
    yearly_boosts: i32,
    store_product: ?[]const u8 = null,
    currency: []const u8,
    amount: i64,
    winners: []api.StarsGiveawayWinnersOption,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.extended) flags |= 1 << 0;
        if (self.default) flags |= 1 << 1;
        if (self.store_product != null) flags |= 1 << 2;
        try w.writeUInt(flags);
        try w.writeLong(self.stars);
        try w.writeInt(self.yearly_boosts);
        if (self.store_product) |v| {
            try w.writeString(v);
        }
        try w.writeString(self.currency);
        try w.writeLong(self.amount);
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.winners.len);
        for (self.winners) |elem_2495| {
            try elem_2495.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.extended = (flags >> 0) & 1 != 0;
        self.default = (flags >> 1) & 1 != 0;
        self.stars = try r.readLong();
        self.yearly_boosts = try r.readInt();
        if ((flags >> 2) & 1 != 0) {
            self.store_product = try r.readString();
        } else {
            self.store_product = null;
        }
        self.currency = try r.readString();
        self.amount = try r.readLong();
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_2496 = try r.readVectorLength();
        const vec_2497 = allocator.alloc(api.StarsGiveawayWinnersOption, len_2496) catch return error.OutOfMemory;
        for (vec_2497) |*elem_2498| {
            elem_2498.* = try api.StarsGiveawayWinnersOption.deserialize(allocator, r);
        }
        self.winners = vec_2497;
        return self;
    }
};

pub const starsGiveawayWinnersOption = struct {
    pub const constructor_id: u32 = 0x54236209;
    default: bool = false,
    users: i32,
    per_user_stars: i64,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.default) flags |= 1 << 0;
        try w.writeUInt(flags);
        try w.writeInt(self.users);
        try w.writeLong(self.per_user_stars);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.default = (flags >> 0) & 1 != 0;
        self.users = try r.readInt();
        self.per_user_stars = try r.readLong();
        _ = allocator;
        return self;
    }
};

pub const starGift = struct {
    pub const constructor_id: u32 = 0x313a9547;
    limited: bool = false,
    sold_out: bool = false,
    birthday: bool = false,
    require_premium: bool = false,
    limited_per_user: bool = false,
    peer_color_available: bool = false,
    auction: bool = false,
    id: i64,
    sticker: api.Document,
    stars: i64,
    availability_remains: ?i32 = null,
    availability_total: ?i32 = null,
    availability_resale: ?i64 = null,
    convert_stars: i64,
    first_sale_date: ?i32 = null,
    last_sale_date: ?i32 = null,
    upgrade_stars: ?i64 = null,
    resell_min_stars: ?i64 = null,
    title: ?[]const u8 = null,
    released_by: ?api.Peer = null,
    per_user_total: ?i32 = null,
    per_user_remains: ?i32 = null,
    locked_until_date: ?i32 = null,
    auction_slug: ?[]const u8 = null,
    gifts_per_round: ?i32 = null,
    auction_start_date: ?i32 = null,
    upgrade_variants: ?i32 = null,
    background: ?api.StarGiftBackground = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.limited) flags |= 1 << 0;
        if (self.sold_out) flags |= 1 << 1;
        if (self.birthday) flags |= 1 << 2;
        if (self.require_premium) flags |= 1 << 7;
        if (self.limited_per_user) flags |= 1 << 8;
        if (self.peer_color_available) flags |= 1 << 10;
        if (self.auction) flags |= 1 << 11;
        if (self.availability_remains != null) flags |= 1 << 0;
        if (self.availability_total != null) flags |= 1 << 0;
        if (self.availability_resale != null) flags |= 1 << 4;
        if (self.first_sale_date != null) flags |= 1 << 1;
        if (self.last_sale_date != null) flags |= 1 << 1;
        if (self.upgrade_stars != null) flags |= 1 << 3;
        if (self.resell_min_stars != null) flags |= 1 << 4;
        if (self.title != null) flags |= 1 << 5;
        if (self.released_by != null) flags |= 1 << 6;
        if (self.per_user_total != null) flags |= 1 << 8;
        if (self.per_user_remains != null) flags |= 1 << 8;
        if (self.locked_until_date != null) flags |= 1 << 9;
        if (self.auction_slug != null) flags |= 1 << 11;
        if (self.gifts_per_round != null) flags |= 1 << 11;
        if (self.auction_start_date != null) flags |= 1 << 11;
        if (self.upgrade_variants != null) flags |= 1 << 12;
        if (self.background != null) flags |= 1 << 13;
        try w.writeUInt(flags);
        try w.writeLong(self.id);
        try self.sticker.serialize(w);
        try w.writeLong(self.stars);
        if (self.availability_remains) |v| {
            try w.writeInt(v);
        }
        if (self.availability_total) |v| {
            try w.writeInt(v);
        }
        if (self.availability_resale) |v| {
            try w.writeLong(v);
        }
        try w.writeLong(self.convert_stars);
        if (self.first_sale_date) |v| {
            try w.writeInt(v);
        }
        if (self.last_sale_date) |v| {
            try w.writeInt(v);
        }
        if (self.upgrade_stars) |v| {
            try w.writeLong(v);
        }
        if (self.resell_min_stars) |v| {
            try w.writeLong(v);
        }
        if (self.title) |v| {
            try w.writeString(v);
        }
        if (self.released_by) |v| {
            try v.serialize(w);
        }
        if (self.per_user_total) |v| {
            try w.writeInt(v);
        }
        if (self.per_user_remains) |v| {
            try w.writeInt(v);
        }
        if (self.locked_until_date) |v| {
            try w.writeInt(v);
        }
        if (self.auction_slug) |v| {
            try w.writeString(v);
        }
        if (self.gifts_per_round) |v| {
            try w.writeInt(v);
        }
        if (self.auction_start_date) |v| {
            try w.writeInt(v);
        }
        if (self.upgrade_variants) |v| {
            try w.writeInt(v);
        }
        if (self.background) |v| {
            try v.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.limited = (flags >> 0) & 1 != 0;
        self.sold_out = (flags >> 1) & 1 != 0;
        self.birthday = (flags >> 2) & 1 != 0;
        self.require_premium = (flags >> 7) & 1 != 0;
        self.limited_per_user = (flags >> 8) & 1 != 0;
        self.peer_color_available = (flags >> 10) & 1 != 0;
        self.auction = (flags >> 11) & 1 != 0;
        self.id = try r.readLong();
        self.sticker = try api.Document.deserialize(allocator, r);
        self.stars = try r.readLong();
        if ((flags >> 0) & 1 != 0) {
            self.availability_remains = try r.readInt();
        } else {
            self.availability_remains = null;
        }
        if ((flags >> 0) & 1 != 0) {
            self.availability_total = try r.readInt();
        } else {
            self.availability_total = null;
        }
        if ((flags >> 4) & 1 != 0) {
            self.availability_resale = try r.readLong();
        } else {
            self.availability_resale = null;
        }
        self.convert_stars = try r.readLong();
        if ((flags >> 1) & 1 != 0) {
            self.first_sale_date = try r.readInt();
        } else {
            self.first_sale_date = null;
        }
        if ((flags >> 1) & 1 != 0) {
            self.last_sale_date = try r.readInt();
        } else {
            self.last_sale_date = null;
        }
        if ((flags >> 3) & 1 != 0) {
            self.upgrade_stars = try r.readLong();
        } else {
            self.upgrade_stars = null;
        }
        if ((flags >> 4) & 1 != 0) {
            self.resell_min_stars = try r.readLong();
        } else {
            self.resell_min_stars = null;
        }
        if ((flags >> 5) & 1 != 0) {
            self.title = try r.readString();
        } else {
            self.title = null;
        }
        if ((flags >> 6) & 1 != 0) {
            self.released_by = try api.Peer.deserialize(allocator, r);
        } else {
            self.released_by = null;
        }
        if ((flags >> 8) & 1 != 0) {
            self.per_user_total = try r.readInt();
        } else {
            self.per_user_total = null;
        }
        if ((flags >> 8) & 1 != 0) {
            self.per_user_remains = try r.readInt();
        } else {
            self.per_user_remains = null;
        }
        if ((flags >> 9) & 1 != 0) {
            self.locked_until_date = try r.readInt();
        } else {
            self.locked_until_date = null;
        }
        if ((flags >> 11) & 1 != 0) {
            self.auction_slug = try r.readString();
        } else {
            self.auction_slug = null;
        }
        if ((flags >> 11) & 1 != 0) {
            self.gifts_per_round = try r.readInt();
        } else {
            self.gifts_per_round = null;
        }
        if ((flags >> 11) & 1 != 0) {
            self.auction_start_date = try r.readInt();
        } else {
            self.auction_start_date = null;
        }
        if ((flags >> 12) & 1 != 0) {
            self.upgrade_variants = try r.readInt();
        } else {
            self.upgrade_variants = null;
        }
        if ((flags >> 13) & 1 != 0) {
            self.background = try api.StarGiftBackground.deserialize(allocator, r);
        } else {
            self.background = null;
        }
        return self;
    }
};

pub const starGiftUnique = struct {
    pub const constructor_id: u32 = 0x85f0a9cd;
    require_premium: bool = false,
    resale_ton_only: bool = false,
    theme_available: bool = false,
    burned: bool = false,
    crafted: bool = false,
    id: i64,
    gift_id: i64,
    title: []const u8,
    slug: []const u8,
    num: i32,
    owner_id: ?api.Peer = null,
    owner_name: ?[]const u8 = null,
    owner_address: ?[]const u8 = null,
    attributes: []api.StarGiftAttribute,
    availability_issued: i32,
    availability_total: i32,
    gift_address: ?[]const u8 = null,
    resell_amount: ?[]api.StarsAmount = null,
    released_by: ?api.Peer = null,
    value_amount: ?i64 = null,
    value_currency: ?[]const u8 = null,
    value_usd_amount: ?i64 = null,
    theme_peer: ?api.Peer = null,
    peer_color: ?api.PeerColor = null,
    host_id: ?api.Peer = null,
    offer_min_stars: ?i32 = null,
    craft_chance_permille: ?i32 = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.require_premium) flags |= 1 << 6;
        if (self.resale_ton_only) flags |= 1 << 7;
        if (self.theme_available) flags |= 1 << 9;
        if (self.burned) flags |= 1 << 14;
        if (self.crafted) flags |= 1 << 15;
        if (self.owner_id != null) flags |= 1 << 0;
        if (self.owner_name != null) flags |= 1 << 1;
        if (self.owner_address != null) flags |= 1 << 2;
        if (self.gift_address != null) flags |= 1 << 3;
        if (self.resell_amount != null) flags |= 1 << 4;
        if (self.released_by != null) flags |= 1 << 5;
        if (self.value_amount != null) flags |= 1 << 8;
        if (self.value_currency != null) flags |= 1 << 8;
        if (self.value_usd_amount != null) flags |= 1 << 8;
        if (self.theme_peer != null) flags |= 1 << 10;
        if (self.peer_color != null) flags |= 1 << 11;
        if (self.host_id != null) flags |= 1 << 12;
        if (self.offer_min_stars != null) flags |= 1 << 13;
        if (self.craft_chance_permille != null) flags |= 1 << 16;
        try w.writeUInt(flags);
        try w.writeLong(self.id);
        try w.writeLong(self.gift_id);
        try w.writeString(self.title);
        try w.writeString(self.slug);
        try w.writeInt(self.num);
        if (self.owner_id) |v| {
            try v.serialize(w);
        }
        if (self.owner_name) |v| {
            try w.writeString(v);
        }
        if (self.owner_address) |v| {
            try w.writeString(v);
        }
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.attributes.len);
        for (self.attributes) |elem_2499| {
            try elem_2499.serialize(w);
        }
        try w.writeInt(self.availability_issued);
        try w.writeInt(self.availability_total);
        if (self.gift_address) |v| {
            try w.writeString(v);
        }
        if (self.resell_amount) |v| {
            try w.writeUInt(vector_constructor_id);
            try w.writeVectorLength(v.len);
            for (v) |elem_2500| {
                try elem_2500.serialize(w);
            }
        }
        if (self.released_by) |v| {
            try v.serialize(w);
        }
        if (self.value_amount) |v| {
            try w.writeLong(v);
        }
        if (self.value_currency) |v| {
            try w.writeString(v);
        }
        if (self.value_usd_amount) |v| {
            try w.writeLong(v);
        }
        if (self.theme_peer) |v| {
            try v.serialize(w);
        }
        if (self.peer_color) |v| {
            try v.serialize(w);
        }
        if (self.host_id) |v| {
            try v.serialize(w);
        }
        if (self.offer_min_stars) |v| {
            try w.writeInt(v);
        }
        if (self.craft_chance_permille) |v| {
            try w.writeInt(v);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.require_premium = (flags >> 6) & 1 != 0;
        self.resale_ton_only = (flags >> 7) & 1 != 0;
        self.theme_available = (flags >> 9) & 1 != 0;
        self.burned = (flags >> 14) & 1 != 0;
        self.crafted = (flags >> 15) & 1 != 0;
        self.id = try r.readLong();
        self.gift_id = try r.readLong();
        self.title = try r.readString();
        self.slug = try r.readString();
        self.num = try r.readInt();
        if ((flags >> 0) & 1 != 0) {
            self.owner_id = try api.Peer.deserialize(allocator, r);
        } else {
            self.owner_id = null;
        }
        if ((flags >> 1) & 1 != 0) {
            self.owner_name = try r.readString();
        } else {
            self.owner_name = null;
        }
        if ((flags >> 2) & 1 != 0) {
            self.owner_address = try r.readString();
        } else {
            self.owner_address = null;
        }
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_2501 = try r.readVectorLength();
        const vec_2502 = allocator.alloc(api.StarGiftAttribute, len_2501) catch return error.OutOfMemory;
        for (vec_2502) |*elem_2503| {
            elem_2503.* = try api.StarGiftAttribute.deserialize(allocator, r);
        }
        self.attributes = vec_2502;
        self.availability_issued = try r.readInt();
        self.availability_total = try r.readInt();
        if ((flags >> 3) & 1 != 0) {
            self.gift_address = try r.readString();
        } else {
            self.gift_address = null;
        }
        if ((flags >> 4) & 1 != 0) {
            if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
            const len_2504 = try r.readVectorLength();
            const vec_2505 = allocator.alloc(api.StarsAmount, len_2504) catch return error.OutOfMemory;
            for (vec_2505) |*elem_2506| {
                elem_2506.* = try api.StarsAmount.deserialize(allocator, r);
            }
            self.resell_amount = vec_2505;
        } else {
            self.resell_amount = null;
        }
        if ((flags >> 5) & 1 != 0) {
            self.released_by = try api.Peer.deserialize(allocator, r);
        } else {
            self.released_by = null;
        }
        if ((flags >> 8) & 1 != 0) {
            self.value_amount = try r.readLong();
        } else {
            self.value_amount = null;
        }
        if ((flags >> 8) & 1 != 0) {
            self.value_currency = try r.readString();
        } else {
            self.value_currency = null;
        }
        if ((flags >> 8) & 1 != 0) {
            self.value_usd_amount = try r.readLong();
        } else {
            self.value_usd_amount = null;
        }
        if ((flags >> 10) & 1 != 0) {
            self.theme_peer = try api.Peer.deserialize(allocator, r);
        } else {
            self.theme_peer = null;
        }
        if ((flags >> 11) & 1 != 0) {
            self.peer_color = try api.PeerColor.deserialize(allocator, r);
        } else {
            self.peer_color = null;
        }
        if ((flags >> 12) & 1 != 0) {
            self.host_id = try api.Peer.deserialize(allocator, r);
        } else {
            self.host_id = null;
        }
        if ((flags >> 13) & 1 != 0) {
            self.offer_min_stars = try r.readInt();
        } else {
            self.offer_min_stars = null;
        }
        if ((flags >> 16) & 1 != 0) {
            self.craft_chance_permille = try r.readInt();
        } else {
            self.craft_chance_permille = null;
        }
        return self;
    }
};

pub const starsAmount = struct {
    pub const constructor_id: u32 = 0xbbb6b4a3;
    amount: i64,
    nanos: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeLong(self.amount);
        try w.writeInt(self.nanos);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.amount = try r.readLong();
        self.nanos = try r.readInt();
        _ = allocator;
        return self;
    }
};

pub const starsTonAmount = struct {
    pub const constructor_id: u32 = 0x74aee3e0;
    amount: i64,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeLong(self.amount);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.amount = try r.readLong();
        _ = allocator;
        return self;
    }
};

pub const starGiftAttributeModel = struct {
    pub const constructor_id: u32 = 0x565251e2;
    crafted: bool = false,
    name: []const u8,
    document: api.Document,
    rarity: api.StarGiftAttributeRarity,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.crafted) flags |= 1 << 0;
        try w.writeUInt(flags);
        try w.writeString(self.name);
        try self.document.serialize(w);
        try self.rarity.serialize(w);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.crafted = (flags >> 0) & 1 != 0;
        self.name = try r.readString();
        self.document = try api.Document.deserialize(allocator, r);
        self.rarity = try api.StarGiftAttributeRarity.deserialize(allocator, r);
        return self;
    }
};

pub const starGiftAttributePattern = struct {
    pub const constructor_id: u32 = 0x4e7085ea;
    name: []const u8,
    document: api.Document,
    rarity: api.StarGiftAttributeRarity,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeString(self.name);
        try self.document.serialize(w);
        try self.rarity.serialize(w);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.name = try r.readString();
        self.document = try api.Document.deserialize(allocator, r);
        self.rarity = try api.StarGiftAttributeRarity.deserialize(allocator, r);
        return self;
    }
};

pub const starGiftAttributeBackdrop = struct {
    pub const constructor_id: u32 = 0x9f2504e4;
    name: []const u8,
    backdrop_id: i32,
    center_color: i32,
    edge_color: i32,
    pattern_color: i32,
    text_color: i32,
    rarity: api.StarGiftAttributeRarity,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeString(self.name);
        try w.writeInt(self.backdrop_id);
        try w.writeInt(self.center_color);
        try w.writeInt(self.edge_color);
        try w.writeInt(self.pattern_color);
        try w.writeInt(self.text_color);
        try self.rarity.serialize(w);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.name = try r.readString();
        self.backdrop_id = try r.readInt();
        self.center_color = try r.readInt();
        self.edge_color = try r.readInt();
        self.pattern_color = try r.readInt();
        self.text_color = try r.readInt();
        self.rarity = try api.StarGiftAttributeRarity.deserialize(allocator, r);
        return self;
    }
};

pub const starGiftAttributeOriginalDetails = struct {
    pub const constructor_id: u32 = 0xe0bff26c;
    sender_id: ?api.Peer = null,
    recipient_id: api.Peer,
    date: i32,
    message: ?api.TextWithEntities = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.sender_id != null) flags |= 1 << 0;
        if (self.message != null) flags |= 1 << 1;
        try w.writeUInt(flags);
        if (self.sender_id) |v| {
            try v.serialize(w);
        }
        try self.recipient_id.serialize(w);
        try w.writeInt(self.date);
        if (self.message) |v| {
            try v.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        if ((flags >> 0) & 1 != 0) {
            self.sender_id = try api.Peer.deserialize(allocator, r);
        } else {
            self.sender_id = null;
        }
        self.recipient_id = try api.Peer.deserialize(allocator, r);
        self.date = try r.readInt();
        if ((flags >> 1) & 1 != 0) {
            self.message = try api.TextWithEntities.deserialize(allocator, r);
        } else {
            self.message = null;
        }
        return self;
    }
};

pub const savedStarGift = struct {
    pub const constructor_id: u32 = 0x41df43fc;
    name_hidden: bool = false,
    unsaved: bool = false,
    refunded: bool = false,
    can_upgrade: bool = false,
    pinned_to_top: bool = false,
    upgrade_separate: bool = false,
    from_id: ?api.Peer = null,
    date: i32,
    gift: api.StarGift,
    message: ?api.TextWithEntities = null,
    msg_id: ?i32 = null,
    saved_id: ?i64 = null,
    convert_stars: ?i64 = null,
    upgrade_stars: ?i64 = null,
    can_export_at: ?i32 = null,
    transfer_stars: ?i64 = null,
    can_transfer_at: ?i32 = null,
    can_resell_at: ?i32 = null,
    collection_id: ?[]i32 = null,
    prepaid_upgrade_hash: ?[]const u8 = null,
    drop_original_details_stars: ?i64 = null,
    gift_num: ?i32 = null,
    can_craft_at: ?i32 = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.name_hidden) flags |= 1 << 0;
        if (self.unsaved) flags |= 1 << 5;
        if (self.refunded) flags |= 1 << 9;
        if (self.can_upgrade) flags |= 1 << 10;
        if (self.pinned_to_top) flags |= 1 << 12;
        if (self.upgrade_separate) flags |= 1 << 17;
        if (self.from_id != null) flags |= 1 << 1;
        if (self.message != null) flags |= 1 << 2;
        if (self.msg_id != null) flags |= 1 << 3;
        if (self.saved_id != null) flags |= 1 << 11;
        if (self.convert_stars != null) flags |= 1 << 4;
        if (self.upgrade_stars != null) flags |= 1 << 6;
        if (self.can_export_at != null) flags |= 1 << 7;
        if (self.transfer_stars != null) flags |= 1 << 8;
        if (self.can_transfer_at != null) flags |= 1 << 13;
        if (self.can_resell_at != null) flags |= 1 << 14;
        if (self.collection_id != null) flags |= 1 << 15;
        if (self.prepaid_upgrade_hash != null) flags |= 1 << 16;
        if (self.drop_original_details_stars != null) flags |= 1 << 18;
        if (self.gift_num != null) flags |= 1 << 19;
        if (self.can_craft_at != null) flags |= 1 << 20;
        try w.writeUInt(flags);
        if (self.from_id) |v| {
            try v.serialize(w);
        }
        try w.writeInt(self.date);
        try self.gift.serialize(w);
        if (self.message) |v| {
            try v.serialize(w);
        }
        if (self.msg_id) |v| {
            try w.writeInt(v);
        }
        if (self.saved_id) |v| {
            try w.writeLong(v);
        }
        if (self.convert_stars) |v| {
            try w.writeLong(v);
        }
        if (self.upgrade_stars) |v| {
            try w.writeLong(v);
        }
        if (self.can_export_at) |v| {
            try w.writeInt(v);
        }
        if (self.transfer_stars) |v| {
            try w.writeLong(v);
        }
        if (self.can_transfer_at) |v| {
            try w.writeInt(v);
        }
        if (self.can_resell_at) |v| {
            try w.writeInt(v);
        }
        if (self.collection_id) |v| {
            try w.writeUInt(vector_constructor_id);
            try w.writeVectorLength(v.len);
            for (v) |elem_2507| {
                try w.writeInt(elem_2507);
            }
        }
        if (self.prepaid_upgrade_hash) |v| {
            try w.writeString(v);
        }
        if (self.drop_original_details_stars) |v| {
            try w.writeLong(v);
        }
        if (self.gift_num) |v| {
            try w.writeInt(v);
        }
        if (self.can_craft_at) |v| {
            try w.writeInt(v);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.name_hidden = (flags >> 0) & 1 != 0;
        self.unsaved = (flags >> 5) & 1 != 0;
        self.refunded = (flags >> 9) & 1 != 0;
        self.can_upgrade = (flags >> 10) & 1 != 0;
        self.pinned_to_top = (flags >> 12) & 1 != 0;
        self.upgrade_separate = (flags >> 17) & 1 != 0;
        if ((flags >> 1) & 1 != 0) {
            self.from_id = try api.Peer.deserialize(allocator, r);
        } else {
            self.from_id = null;
        }
        self.date = try r.readInt();
        self.gift = try api.StarGift.deserialize(allocator, r);
        if ((flags >> 2) & 1 != 0) {
            self.message = try api.TextWithEntities.deserialize(allocator, r);
        } else {
            self.message = null;
        }
        if ((flags >> 3) & 1 != 0) {
            self.msg_id = try r.readInt();
        } else {
            self.msg_id = null;
        }
        if ((flags >> 11) & 1 != 0) {
            self.saved_id = try r.readLong();
        } else {
            self.saved_id = null;
        }
        if ((flags >> 4) & 1 != 0) {
            self.convert_stars = try r.readLong();
        } else {
            self.convert_stars = null;
        }
        if ((flags >> 6) & 1 != 0) {
            self.upgrade_stars = try r.readLong();
        } else {
            self.upgrade_stars = null;
        }
        if ((flags >> 7) & 1 != 0) {
            self.can_export_at = try r.readInt();
        } else {
            self.can_export_at = null;
        }
        if ((flags >> 8) & 1 != 0) {
            self.transfer_stars = try r.readLong();
        } else {
            self.transfer_stars = null;
        }
        if ((flags >> 13) & 1 != 0) {
            self.can_transfer_at = try r.readInt();
        } else {
            self.can_transfer_at = null;
        }
        if ((flags >> 14) & 1 != 0) {
            self.can_resell_at = try r.readInt();
        } else {
            self.can_resell_at = null;
        }
        if ((flags >> 15) & 1 != 0) {
            if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
            const len_2508 = try r.readVectorLength();
            const vec_2509 = allocator.alloc(i32, len_2508) catch return error.OutOfMemory;
            for (vec_2509) |*elem_2510| {
                elem_2510.* = try r.readInt();
            }
            self.collection_id = vec_2509;
        } else {
            self.collection_id = null;
        }
        if ((flags >> 16) & 1 != 0) {
            self.prepaid_upgrade_hash = try r.readString();
        } else {
            self.prepaid_upgrade_hash = null;
        }
        if ((flags >> 18) & 1 != 0) {
            self.drop_original_details_stars = try r.readLong();
        } else {
            self.drop_original_details_stars = null;
        }
        if ((flags >> 19) & 1 != 0) {
            self.gift_num = try r.readInt();
        } else {
            self.gift_num = null;
        }
        if ((flags >> 20) & 1 != 0) {
            self.can_craft_at = try r.readInt();
        } else {
            self.can_craft_at = null;
        }
        return self;
    }
};

pub const disallowedGiftsSettings = struct {
    pub const constructor_id: u32 = 0x71f276c4;
    disallow_unlimited_stargifts: bool = false,
    disallow_limited_stargifts: bool = false,
    disallow_unique_stargifts: bool = false,
    disallow_premium_gifts: bool = false,
    disallow_stargifts_from_channels: bool = false,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.disallow_unlimited_stargifts) flags |= 1 << 0;
        if (self.disallow_limited_stargifts) flags |= 1 << 1;
        if (self.disallow_unique_stargifts) flags |= 1 << 2;
        if (self.disallow_premium_gifts) flags |= 1 << 3;
        if (self.disallow_stargifts_from_channels) flags |= 1 << 4;
        try w.writeUInt(flags);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.disallow_unlimited_stargifts = (flags >> 0) & 1 != 0;
        self.disallow_limited_stargifts = (flags >> 1) & 1 != 0;
        self.disallow_unique_stargifts = (flags >> 2) & 1 != 0;
        self.disallow_premium_gifts = (flags >> 3) & 1 != 0;
        self.disallow_stargifts_from_channels = (flags >> 4) & 1 != 0;
        _ = allocator;
        return self;
    }
};

pub const starGiftAttributeIdModel = struct {
    pub const constructor_id: u32 = 0x48aaae3c;
    document_id: i64,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeLong(self.document_id);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.document_id = try r.readLong();
        _ = allocator;
        return self;
    }
};

pub const starGiftAttributeIdPattern = struct {
    pub const constructor_id: u32 = 0x4a162433;
    document_id: i64,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeLong(self.document_id);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.document_id = try r.readLong();
        _ = allocator;
        return self;
    }
};

pub const starGiftAttributeIdBackdrop = struct {
    pub const constructor_id: u32 = 0x1f01c757;
    backdrop_id: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeInt(self.backdrop_id);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.backdrop_id = try r.readInt();
        _ = allocator;
        return self;
    }
};

pub const starGiftAttributeCounter = struct {
    pub const constructor_id: u32 = 0x2eb1b658;
    attribute: api.StarGiftAttributeId,
    count: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try self.attribute.serialize(w);
        try w.writeInt(self.count);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.attribute = try api.StarGiftAttributeId.deserialize(allocator, r);
        self.count = try r.readInt();
        return self;
    }
};

pub const starsRating = struct {
    pub const constructor_id: u32 = 0x1b0e4f07;
    level: i32,
    current_level_stars: i64,
    stars: i64,
    next_level_stars: ?i64 = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.next_level_stars != null) flags |= 1 << 0;
        try w.writeUInt(flags);
        try w.writeInt(self.level);
        try w.writeLong(self.current_level_stars);
        try w.writeLong(self.stars);
        if (self.next_level_stars) |v| {
            try w.writeLong(v);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.level = try r.readInt();
        self.current_level_stars = try r.readLong();
        self.stars = try r.readLong();
        if ((flags >> 0) & 1 != 0) {
            self.next_level_stars = try r.readLong();
        } else {
            self.next_level_stars = null;
        }
        _ = allocator;
        return self;
    }
};

pub const starGiftCollection = struct {
    pub const constructor_id: u32 = 0x9d6b13b0;
    collection_id: i32,
    title: []const u8,
    icon: ?api.Document = null,
    gifts_count: i32,
    hash: i64,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.icon != null) flags |= 1 << 0;
        try w.writeUInt(flags);
        try w.writeInt(self.collection_id);
        try w.writeString(self.title);
        if (self.icon) |v| {
            try v.serialize(w);
        }
        try w.writeInt(self.gifts_count);
        try w.writeLong(self.hash);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.collection_id = try r.readInt();
        self.title = try r.readString();
        if ((flags >> 0) & 1 != 0) {
            self.icon = try api.Document.deserialize(allocator, r);
        } else {
            self.icon = null;
        }
        self.gifts_count = try r.readInt();
        self.hash = try r.readLong();
        return self;
    }
};

pub const starGiftUpgradePrice = struct {
    pub const constructor_id: u32 = 0x99ea331d;
    date: i32,
    upgrade_stars: i64,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeInt(self.date);
        try w.writeLong(self.upgrade_stars);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.date = try r.readInt();
        self.upgrade_stars = try r.readLong();
        _ = allocator;
        return self;
    }
};

pub const auctionBidLevel = struct {
    pub const constructor_id: u32 = 0x310240cc;
    pos: i32,
    amount: i64,
    date: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeInt(self.pos);
        try w.writeLong(self.amount);
        try w.writeInt(self.date);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.pos = try r.readInt();
        self.amount = try r.readLong();
        self.date = try r.readInt();
        _ = allocator;
        return self;
    }
};

pub const starGiftAuctionStateNotModified = struct {
    pub const constructor_id: u32 = 0xfe333952;

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        _ = self;
        _ = w;
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        _ = allocator;
        _ = r;
        return .{};
    }
};

pub const starGiftAuctionState = struct {
    pub const constructor_id: u32 = 0x771a4e66;
    version: i32,
    start_date: i32,
    end_date: i32,
    min_bid_amount: i64,
    bid_levels: []api.AuctionBidLevel,
    top_bidders: []i64,
    next_round_at: i32,
    last_gift_num: i32,
    gifts_left: i32,
    current_round: i32,
    total_rounds: i32,
    rounds: []api.StarGiftAuctionRound,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeInt(self.version);
        try w.writeInt(self.start_date);
        try w.writeInt(self.end_date);
        try w.writeLong(self.min_bid_amount);
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.bid_levels.len);
        for (self.bid_levels) |elem_2511| {
            try elem_2511.serialize(w);
        }
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.top_bidders.len);
        for (self.top_bidders) |elem_2512| {
            try w.writeLong(elem_2512);
        }
        try w.writeInt(self.next_round_at);
        try w.writeInt(self.last_gift_num);
        try w.writeInt(self.gifts_left);
        try w.writeInt(self.current_round);
        try w.writeInt(self.total_rounds);
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.rounds.len);
        for (self.rounds) |elem_2513| {
            try elem_2513.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.version = try r.readInt();
        self.start_date = try r.readInt();
        self.end_date = try r.readInt();
        self.min_bid_amount = try r.readLong();
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_2514 = try r.readVectorLength();
        const vec_2515 = allocator.alloc(api.AuctionBidLevel, len_2514) catch return error.OutOfMemory;
        for (vec_2515) |*elem_2516| {
            elem_2516.* = try api.AuctionBidLevel.deserialize(allocator, r);
        }
        self.bid_levels = vec_2515;
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_2517 = try r.readVectorLength();
        const vec_2518 = allocator.alloc(i64, len_2517) catch return error.OutOfMemory;
        for (vec_2518) |*elem_2519| {
            elem_2519.* = try r.readLong();
        }
        self.top_bidders = vec_2518;
        self.next_round_at = try r.readInt();
        self.last_gift_num = try r.readInt();
        self.gifts_left = try r.readInt();
        self.current_round = try r.readInt();
        self.total_rounds = try r.readInt();
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_2520 = try r.readVectorLength();
        const vec_2521 = allocator.alloc(api.StarGiftAuctionRound, len_2520) catch return error.OutOfMemory;
        for (vec_2521) |*elem_2522| {
            elem_2522.* = try api.StarGiftAuctionRound.deserialize(allocator, r);
        }
        self.rounds = vec_2521;
        return self;
    }
};

pub const starGiftAuctionStateFinished = struct {
    pub const constructor_id: u32 = 0x972dabbf;
    start_date: i32,
    end_date: i32,
    average_price: i64,
    listed_count: ?i32 = null,
    fragment_listed_count: ?i32 = null,
    fragment_listed_url: ?[]const u8 = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.listed_count != null) flags |= 1 << 0;
        if (self.fragment_listed_count != null) flags |= 1 << 1;
        if (self.fragment_listed_url != null) flags |= 1 << 1;
        try w.writeUInt(flags);
        try w.writeInt(self.start_date);
        try w.writeInt(self.end_date);
        try w.writeLong(self.average_price);
        if (self.listed_count) |v| {
            try w.writeInt(v);
        }
        if (self.fragment_listed_count) |v| {
            try w.writeInt(v);
        }
        if (self.fragment_listed_url) |v| {
            try w.writeString(v);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.start_date = try r.readInt();
        self.end_date = try r.readInt();
        self.average_price = try r.readLong();
        if ((flags >> 0) & 1 != 0) {
            self.listed_count = try r.readInt();
        } else {
            self.listed_count = null;
        }
        if ((flags >> 1) & 1 != 0) {
            self.fragment_listed_count = try r.readInt();
        } else {
            self.fragment_listed_count = null;
        }
        if ((flags >> 1) & 1 != 0) {
            self.fragment_listed_url = try r.readString();
        } else {
            self.fragment_listed_url = null;
        }
        _ = allocator;
        return self;
    }
};

pub const starGiftAuctionUserState = struct {
    pub const constructor_id: u32 = 0x2eeed1c4;
    returned: bool = false,
    bid_amount: ?i64 = null,
    bid_date: ?i32 = null,
    min_bid_amount: ?i64 = null,
    bid_peer: ?api.Peer = null,
    acquired_count: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.returned) flags |= 1 << 1;
        if (self.bid_amount != null) flags |= 1 << 0;
        if (self.bid_date != null) flags |= 1 << 0;
        if (self.min_bid_amount != null) flags |= 1 << 0;
        if (self.bid_peer != null) flags |= 1 << 0;
        try w.writeUInt(flags);
        if (self.bid_amount) |v| {
            try w.writeLong(v);
        }
        if (self.bid_date) |v| {
            try w.writeInt(v);
        }
        if (self.min_bid_amount) |v| {
            try w.writeLong(v);
        }
        if (self.bid_peer) |v| {
            try v.serialize(w);
        }
        try w.writeInt(self.acquired_count);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.returned = (flags >> 1) & 1 != 0;
        if ((flags >> 0) & 1 != 0) {
            self.bid_amount = try r.readLong();
        } else {
            self.bid_amount = null;
        }
        if ((flags >> 0) & 1 != 0) {
            self.bid_date = try r.readInt();
        } else {
            self.bid_date = null;
        }
        if ((flags >> 0) & 1 != 0) {
            self.min_bid_amount = try r.readLong();
        } else {
            self.min_bid_amount = null;
        }
        if ((flags >> 0) & 1 != 0) {
            self.bid_peer = try api.Peer.deserialize(allocator, r);
        } else {
            self.bid_peer = null;
        }
        self.acquired_count = try r.readInt();
        return self;
    }
};

pub const starGiftAuctionAcquiredGift = struct {
    pub const constructor_id: u32 = 0x42b00348;
    name_hidden: bool = false,
    peer: api.Peer,
    date: i32,
    bid_amount: i64,
    round: i32,
    pos: i32,
    message: ?api.TextWithEntities = null,
    gift_num: ?i32 = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.name_hidden) flags |= 1 << 0;
        if (self.message != null) flags |= 1 << 1;
        if (self.gift_num != null) flags |= 1 << 2;
        try w.writeUInt(flags);
        try self.peer.serialize(w);
        try w.writeInt(self.date);
        try w.writeLong(self.bid_amount);
        try w.writeInt(self.round);
        try w.writeInt(self.pos);
        if (self.message) |v| {
            try v.serialize(w);
        }
        if (self.gift_num) |v| {
            try w.writeInt(v);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.name_hidden = (flags >> 0) & 1 != 0;
        self.peer = try api.Peer.deserialize(allocator, r);
        self.date = try r.readInt();
        self.bid_amount = try r.readLong();
        self.round = try r.readInt();
        self.pos = try r.readInt();
        if ((flags >> 1) & 1 != 0) {
            self.message = try api.TextWithEntities.deserialize(allocator, r);
        } else {
            self.message = null;
        }
        if ((flags >> 2) & 1 != 0) {
            self.gift_num = try r.readInt();
        } else {
            self.gift_num = null;
        }
        return self;
    }
};

pub const starGiftActiveAuctionState = struct {
    pub const constructor_id: u32 = 0xd31bc45d;
    gift: api.StarGift,
    state: api.StarGiftAuctionState,
    user_state: api.StarGiftAuctionUserState,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try self.gift.serialize(w);
        try self.state.serialize(w);
        try self.user_state.serialize(w);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.gift = try api.StarGift.deserialize(allocator, r);
        self.state = try api.StarGiftAuctionState.deserialize(allocator, r);
        self.user_state = try api.StarGiftAuctionUserState.deserialize(allocator, r);
        return self;
    }
};

pub const inputStarGiftAuction = struct {
    pub const constructor_id: u32 = 0x02e16c98;
    gift_id: i64,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeLong(self.gift_id);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.gift_id = try r.readLong();
        _ = allocator;
        return self;
    }
};

pub const inputStarGiftAuctionSlug = struct {
    pub const constructor_id: u32 = 0x7ab58308;
    slug: []const u8,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeString(self.slug);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.slug = try r.readString();
        _ = allocator;
        return self;
    }
};

pub const starGiftBackground = struct {
    pub const constructor_id: u32 = 0xaff56398;
    center_color: i32,
    edge_color: i32,
    text_color: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeInt(self.center_color);
        try w.writeInt(self.edge_color);
        try w.writeInt(self.text_color);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.center_color = try r.readInt();
        self.edge_color = try r.readInt();
        self.text_color = try r.readInt();
        _ = allocator;
        return self;
    }
};

pub const starGiftAuctionRound = struct {
    pub const constructor_id: u32 = 0x3aae0528;
    num: i32,
    duration: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeInt(self.num);
        try w.writeInt(self.duration);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.num = try r.readInt();
        self.duration = try r.readInt();
        _ = allocator;
        return self;
    }
};

pub const starGiftAuctionRoundExtendable = struct {
    pub const constructor_id: u32 = 0x0aa021e5;
    num: i32,
    duration: i32,
    extend_top: i32,
    extend_window: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeInt(self.num);
        try w.writeInt(self.duration);
        try w.writeInt(self.extend_top);
        try w.writeInt(self.extend_window);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.num = try r.readInt();
        self.duration = try r.readInt();
        self.extend_top = try r.readInt();
        self.extend_window = try r.readInt();
        _ = allocator;
        return self;
    }
};

pub const starGiftAttributeRarity = struct {
    pub const constructor_id: u32 = 0x36437737;
    permille: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeInt(self.permille);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.permille = try r.readInt();
        _ = allocator;
        return self;
    }
};

pub const starGiftAttributeRarityUncommon = struct {
    pub const constructor_id: u32 = 0xdbce6389;

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        _ = self;
        _ = w;
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        _ = allocator;
        _ = r;
        return .{};
    }
};

pub const starGiftAttributeRarityRare = struct {
    pub const constructor_id: u32 = 0xf08d516b;

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        _ = self;
        _ = w;
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        _ = allocator;
        _ = r;
        return .{};
    }
};

pub const starGiftAttributeRarityEpic = struct {
    pub const constructor_id: u32 = 0x78fbf3a8;

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        _ = self;
        _ = w;
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        _ = allocator;
        _ = r;
        return .{};
    }
};

pub const starGiftAttributeRarityLegendary = struct {
    pub const constructor_id: u32 = 0xcef7e7a8;

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        _ = self;
        _ = w;
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        _ = allocator;
        _ = r;
        return .{};
    }
};

pub const PrepaidGiveaway = union(enum) {
    prepaidGiveaway: api.prepaidGiveaway,
    prepaidStarsGiveaway: api.prepaidStarsGiveaway,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.prepaidGiveaway.constructor_id => return .{ .prepaidGiveaway = try api.prepaidGiveaway.deserialize(allocator, r) },
            api.prepaidStarsGiveaway.constructor_id => return .{ .prepaidStarsGiveaway = try api.prepaidStarsGiveaway.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const StarsTransactionPeer = union(enum) {
    starsTransactionPeerUnsupported: api.starsTransactionPeerUnsupported,
    starsTransactionPeerAppStore: api.starsTransactionPeerAppStore,
    starsTransactionPeerPlayMarket: api.starsTransactionPeerPlayMarket,
    starsTransactionPeerPremiumBot: api.starsTransactionPeerPremiumBot,
    starsTransactionPeerFragment: api.starsTransactionPeerFragment,
    starsTransactionPeer: api.starsTransactionPeer,
    starsTransactionPeerAds: api.starsTransactionPeerAds,
    starsTransactionPeerAPI: api.starsTransactionPeerAPI,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.starsTransactionPeerUnsupported.constructor_id => return .{ .starsTransactionPeerUnsupported = try api.starsTransactionPeerUnsupported.deserialize(allocator, r) },
            api.starsTransactionPeerAppStore.constructor_id => return .{ .starsTransactionPeerAppStore = try api.starsTransactionPeerAppStore.deserialize(allocator, r) },
            api.starsTransactionPeerPlayMarket.constructor_id => return .{ .starsTransactionPeerPlayMarket = try api.starsTransactionPeerPlayMarket.deserialize(allocator, r) },
            api.starsTransactionPeerPremiumBot.constructor_id => return .{ .starsTransactionPeerPremiumBot = try api.starsTransactionPeerPremiumBot.deserialize(allocator, r) },
            api.starsTransactionPeerFragment.constructor_id => return .{ .starsTransactionPeerFragment = try api.starsTransactionPeerFragment.deserialize(allocator, r) },
            api.starsTransactionPeer.constructor_id => return .{ .starsTransactionPeer = try api.starsTransactionPeer.deserialize(allocator, r) },
            api.starsTransactionPeerAds.constructor_id => return .{ .starsTransactionPeerAds = try api.starsTransactionPeerAds.deserialize(allocator, r) },
            api.starsTransactionPeerAPI.constructor_id => return .{ .starsTransactionPeerAPI = try api.starsTransactionPeerAPI.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const StarsTopupOption = union(enum) {
    starsTopupOption: api.starsTopupOption,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.starsTopupOption.constructor_id => return .{ .starsTopupOption = try api.starsTopupOption.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const StarsTransaction = union(enum) {
    starsTransaction: api.starsTransaction,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.starsTransaction.constructor_id => return .{ .starsTransaction = try api.starsTransaction.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const StarsRevenueStatus = union(enum) {
    starsRevenueStatus: api.starsRevenueStatus,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.starsRevenueStatus.constructor_id => return .{ .starsRevenueStatus = try api.starsRevenueStatus.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const InputStarsTransaction = union(enum) {
    inputStarsTransaction: api.inputStarsTransaction,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.inputStarsTransaction.constructor_id => return .{ .inputStarsTransaction = try api.inputStarsTransaction.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const StarsGiftOption = union(enum) {
    starsGiftOption: api.starsGiftOption,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.starsGiftOption.constructor_id => return .{ .starsGiftOption = try api.starsGiftOption.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const StarsSubscriptionPricing = union(enum) {
    starsSubscriptionPricing: api.starsSubscriptionPricing,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.starsSubscriptionPricing.constructor_id => return .{ .starsSubscriptionPricing = try api.starsSubscriptionPricing.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const StarsSubscription = union(enum) {
    starsSubscription: api.starsSubscription,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.starsSubscription.constructor_id => return .{ .starsSubscription = try api.starsSubscription.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const StarsGiveawayOption = union(enum) {
    starsGiveawayOption: api.starsGiveawayOption,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.starsGiveawayOption.constructor_id => return .{ .starsGiveawayOption = try api.starsGiveawayOption.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const StarsGiveawayWinnersOption = union(enum) {
    starsGiveawayWinnersOption: api.starsGiveawayWinnersOption,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.starsGiveawayWinnersOption.constructor_id => return .{ .starsGiveawayWinnersOption = try api.starsGiveawayWinnersOption.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const StarGift = union(enum) {
    starGift: api.starGift,
    starGiftUnique: api.starGiftUnique,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.starGift.constructor_id => return .{ .starGift = try api.starGift.deserialize(allocator, r) },
            api.starGiftUnique.constructor_id => return .{ .starGiftUnique = try api.starGiftUnique.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const StarsAmount = union(enum) {
    starsAmount: api.starsAmount,
    starsTonAmount: api.starsTonAmount,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.starsAmount.constructor_id => return .{ .starsAmount = try api.starsAmount.deserialize(allocator, r) },
            api.starsTonAmount.constructor_id => return .{ .starsTonAmount = try api.starsTonAmount.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const StarGiftAttribute = union(enum) {
    starGiftAttributeModel: api.starGiftAttributeModel,
    starGiftAttributePattern: api.starGiftAttributePattern,
    starGiftAttributeBackdrop: api.starGiftAttributeBackdrop,
    starGiftAttributeOriginalDetails: api.starGiftAttributeOriginalDetails,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.starGiftAttributeModel.constructor_id => return .{ .starGiftAttributeModel = try api.starGiftAttributeModel.deserialize(allocator, r) },
            api.starGiftAttributePattern.constructor_id => return .{ .starGiftAttributePattern = try api.starGiftAttributePattern.deserialize(allocator, r) },
            api.starGiftAttributeBackdrop.constructor_id => return .{ .starGiftAttributeBackdrop = try api.starGiftAttributeBackdrop.deserialize(allocator, r) },
            api.starGiftAttributeOriginalDetails.constructor_id => return .{ .starGiftAttributeOriginalDetails = try api.starGiftAttributeOriginalDetails.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const SavedStarGift = union(enum) {
    savedStarGift: api.savedStarGift,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.savedStarGift.constructor_id => return .{ .savedStarGift = try api.savedStarGift.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const DisallowedGiftsSettings = union(enum) {
    disallowedGiftsSettings: api.disallowedGiftsSettings,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.disallowedGiftsSettings.constructor_id => return .{ .disallowedGiftsSettings = try api.disallowedGiftsSettings.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const StarGiftAttributeId = union(enum) {
    starGiftAttributeIdModel: api.starGiftAttributeIdModel,
    starGiftAttributeIdPattern: api.starGiftAttributeIdPattern,
    starGiftAttributeIdBackdrop: api.starGiftAttributeIdBackdrop,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.starGiftAttributeIdModel.constructor_id => return .{ .starGiftAttributeIdModel = try api.starGiftAttributeIdModel.deserialize(allocator, r) },
            api.starGiftAttributeIdPattern.constructor_id => return .{ .starGiftAttributeIdPattern = try api.starGiftAttributeIdPattern.deserialize(allocator, r) },
            api.starGiftAttributeIdBackdrop.constructor_id => return .{ .starGiftAttributeIdBackdrop = try api.starGiftAttributeIdBackdrop.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const StarGiftAttributeCounter = union(enum) {
    starGiftAttributeCounter: api.starGiftAttributeCounter,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.starGiftAttributeCounter.constructor_id => return .{ .starGiftAttributeCounter = try api.starGiftAttributeCounter.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const StarsRating = union(enum) {
    starsRating: api.starsRating,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.starsRating.constructor_id => return .{ .starsRating = try api.starsRating.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const StarGiftCollection = union(enum) {
    starGiftCollection: api.starGiftCollection,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.starGiftCollection.constructor_id => return .{ .starGiftCollection = try api.starGiftCollection.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const StarGiftUpgradePrice = union(enum) {
    starGiftUpgradePrice: api.starGiftUpgradePrice,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.starGiftUpgradePrice.constructor_id => return .{ .starGiftUpgradePrice = try api.starGiftUpgradePrice.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const AuctionBidLevel = union(enum) {
    auctionBidLevel: api.auctionBidLevel,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.auctionBidLevel.constructor_id => return .{ .auctionBidLevel = try api.auctionBidLevel.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const StarGiftAuctionState = union(enum) {
    starGiftAuctionStateNotModified: api.starGiftAuctionStateNotModified,
    starGiftAuctionState: api.starGiftAuctionState,
    starGiftAuctionStateFinished: api.starGiftAuctionStateFinished,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.starGiftAuctionStateNotModified.constructor_id => return .{ .starGiftAuctionStateNotModified = try api.starGiftAuctionStateNotModified.deserialize(allocator, r) },
            api.starGiftAuctionState.constructor_id => return .{ .starGiftAuctionState = try api.starGiftAuctionState.deserialize(allocator, r) },
            api.starGiftAuctionStateFinished.constructor_id => return .{ .starGiftAuctionStateFinished = try api.starGiftAuctionStateFinished.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const StarGiftAuctionUserState = union(enum) {
    starGiftAuctionUserState: api.starGiftAuctionUserState,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.starGiftAuctionUserState.constructor_id => return .{ .starGiftAuctionUserState = try api.starGiftAuctionUserState.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const StarGiftAuctionAcquiredGift = union(enum) {
    starGiftAuctionAcquiredGift: api.starGiftAuctionAcquiredGift,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.starGiftAuctionAcquiredGift.constructor_id => return .{ .starGiftAuctionAcquiredGift = try api.starGiftAuctionAcquiredGift.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const StarGiftActiveAuctionState = union(enum) {
    starGiftActiveAuctionState: api.starGiftActiveAuctionState,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.starGiftActiveAuctionState.constructor_id => return .{ .starGiftActiveAuctionState = try api.starGiftActiveAuctionState.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const InputStarGiftAuction = union(enum) {
    inputStarGiftAuction: api.inputStarGiftAuction,
    inputStarGiftAuctionSlug: api.inputStarGiftAuctionSlug,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.inputStarGiftAuction.constructor_id => return .{ .inputStarGiftAuction = try api.inputStarGiftAuction.deserialize(allocator, r) },
            api.inputStarGiftAuctionSlug.constructor_id => return .{ .inputStarGiftAuctionSlug = try api.inputStarGiftAuctionSlug.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const StarGiftBackground = union(enum) {
    starGiftBackground: api.starGiftBackground,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.starGiftBackground.constructor_id => return .{ .starGiftBackground = try api.starGiftBackground.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const StarGiftAuctionRound = union(enum) {
    starGiftAuctionRound: api.starGiftAuctionRound,
    starGiftAuctionRoundExtendable: api.starGiftAuctionRoundExtendable,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.starGiftAuctionRound.constructor_id => return .{ .starGiftAuctionRound = try api.starGiftAuctionRound.deserialize(allocator, r) },
            api.starGiftAuctionRoundExtendable.constructor_id => return .{ .starGiftAuctionRoundExtendable = try api.starGiftAuctionRoundExtendable.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const StarGiftAttributeRarity = union(enum) {
    starGiftAttributeRarity: api.starGiftAttributeRarity,
    starGiftAttributeRarityUncommon: api.starGiftAttributeRarityUncommon,
    starGiftAttributeRarityRare: api.starGiftAttributeRarityRare,
    starGiftAttributeRarityEpic: api.starGiftAttributeRarityEpic,
    starGiftAttributeRarityLegendary: api.starGiftAttributeRarityLegendary,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.starGiftAttributeRarity.constructor_id => return .{ .starGiftAttributeRarity = try api.starGiftAttributeRarity.deserialize(allocator, r) },
            api.starGiftAttributeRarityUncommon.constructor_id => return .{ .starGiftAttributeRarityUncommon = try api.starGiftAttributeRarityUncommon.deserialize(allocator, r) },
            api.starGiftAttributeRarityRare.constructor_id => return .{ .starGiftAttributeRarityRare = try api.starGiftAttributeRarityRare.deserialize(allocator, r) },
            api.starGiftAttributeRarityEpic.constructor_id => return .{ .starGiftAttributeRarityEpic = try api.starGiftAttributeRarityEpic.deserialize(allocator, r) },
            api.starGiftAttributeRarityLegendary.constructor_id => return .{ .starGiftAttributeRarityLegendary = try api.starGiftAttributeRarityLegendary.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};


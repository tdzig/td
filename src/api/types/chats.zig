//! GENERATED by td TL code generator from "api.tl".
//! Do not edit by hand; regenerate with td-gen.

const std = @import("std");
const td = @import("../../root.zig");
const Writer = td.tl.Writer;
const Reader = td.tl.Reader;
const TlError = td.TlError;
const vector_constructor_id = td.tl.vector_constructor_id;

const api = @import("../mod.zig");

pub const chatEmpty = struct {
    pub const constructor_id: u32 = 0x29562865;
    id: i64,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeLong(self.id);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.id = try r.readLong();
        _ = allocator;
        return self;
    }
};

pub const chat = struct {
    pub const constructor_id: u32 = 0x41cbf256;
    creator: bool = false,
    left: bool = false,
    deactivated: bool = false,
    call_active: bool = false,
    call_not_empty: bool = false,
    noforwards: bool = false,
    id: i64,
    title: []const u8,
    photo: api.ChatPhoto,
    participants_count: i32,
    date: i32,
    version: i32,
    migrated_to: ?api.InputChannel = null,
    admin_rights: ?api.ChatAdminRights = null,
    default_banned_rights: ?api.ChatBannedRights = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.creator) flags |= 1 << 0;
        if (self.left) flags |= 1 << 2;
        if (self.deactivated) flags |= 1 << 5;
        if (self.call_active) flags |= 1 << 23;
        if (self.call_not_empty) flags |= 1 << 24;
        if (self.noforwards) flags |= 1 << 25;
        if (self.migrated_to != null) flags |= 1 << 6;
        if (self.admin_rights != null) flags |= 1 << 14;
        if (self.default_banned_rights != null) flags |= 1 << 18;
        try w.writeUInt(flags);
        try w.writeLong(self.id);
        try w.writeString(self.title);
        try self.photo.serialize(w);
        try w.writeInt(self.participants_count);
        try w.writeInt(self.date);
        try w.writeInt(self.version);
        if (self.migrated_to) |v| {
            try v.serialize(w);
        }
        if (self.admin_rights) |v| {
            try v.serialize(w);
        }
        if (self.default_banned_rights) |v| {
            try v.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.creator = (flags >> 0) & 1 != 0;
        self.left = (flags >> 2) & 1 != 0;
        self.deactivated = (flags >> 5) & 1 != 0;
        self.call_active = (flags >> 23) & 1 != 0;
        self.call_not_empty = (flags >> 24) & 1 != 0;
        self.noforwards = (flags >> 25) & 1 != 0;
        self.id = try r.readLong();
        self.title = try r.readString();
        self.photo = try api.ChatPhoto.deserialize(allocator, r);
        self.participants_count = try r.readInt();
        self.date = try r.readInt();
        self.version = try r.readInt();
        if ((flags >> 6) & 1 != 0) {
            self.migrated_to = try api.InputChannel.deserialize(allocator, r);
        } else {
            self.migrated_to = null;
        }
        if ((flags >> 14) & 1 != 0) {
            self.admin_rights = try api.ChatAdminRights.deserialize(allocator, r);
        } else {
            self.admin_rights = null;
        }
        if ((flags >> 18) & 1 != 0) {
            self.default_banned_rights = try api.ChatBannedRights.deserialize(allocator, r);
        } else {
            self.default_banned_rights = null;
        }
        return self;
    }
};

pub const chatForbidden = struct {
    pub const constructor_id: u32 = 0x6592a1a7;
    id: i64,
    title: []const u8,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeLong(self.id);
        try w.writeString(self.title);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.id = try r.readLong();
        self.title = try r.readString();
        _ = allocator;
        return self;
    }
};

pub const channel = struct {
    pub const constructor_id: u32 = 0xd49f34c6;
    creator: bool = false,
    left: bool = false,
    broadcast: bool = false,
    verified: bool = false,
    megagroup: bool = false,
    restricted: bool = false,
    signatures: bool = false,
    min: bool = false,
    scam: bool = false,
    has_link: bool = false,
    has_geo: bool = false,
    slowmode_enabled: bool = false,
    call_active: bool = false,
    call_not_empty: bool = false,
    fake: bool = false,
    gigagroup: bool = false,
    noforwards: bool = false,
    join_to_send: bool = false,
    join_request: bool = false,
    forum: bool = false,
    stories_hidden: bool = false,
    stories_hidden_min: bool = false,
    stories_unavailable: bool = false,
    signature_profiles: bool = false,
    autotranslation: bool = false,
    broadcast_messages_allowed: bool = false,
    monoforum: bool = false,
    forum_tabs: bool = false,
    id: i64,
    access_hash: ?i64 = null,
    title: []const u8,
    username: ?[]const u8 = null,
    photo: api.ChatPhoto,
    date: i32,
    restriction_reason: ?[]api.RestrictionReason = null,
    admin_rights: ?api.ChatAdminRights = null,
    banned_rights: ?api.ChatBannedRights = null,
    default_banned_rights: ?api.ChatBannedRights = null,
    participants_count: ?i32 = null,
    usernames: ?[]api.Username = null,
    stories_max_id: ?api.RecentStory = null,
    color: ?api.PeerColor = null,
    profile_color: ?api.PeerColor = null,
    emoji_status: ?api.EmojiStatus = null,
    level: ?i32 = null,
    subscription_until_date: ?i32 = null,
    bot_verification_icon: ?i64 = null,
    send_paid_messages_stars: ?i64 = null,
    linked_monoforum_id: ?i64 = null,
    linked_community_id: ?i64 = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.creator) flags |= 1 << 0;
        if (self.left) flags |= 1 << 2;
        if (self.broadcast) flags |= 1 << 5;
        if (self.verified) flags |= 1 << 7;
        if (self.megagroup) flags |= 1 << 8;
        if (self.restricted) flags |= 1 << 9;
        if (self.signatures) flags |= 1 << 11;
        if (self.min) flags |= 1 << 12;
        if (self.scam) flags |= 1 << 19;
        if (self.has_link) flags |= 1 << 20;
        if (self.has_geo) flags |= 1 << 21;
        if (self.slowmode_enabled) flags |= 1 << 22;
        if (self.call_active) flags |= 1 << 23;
        if (self.call_not_empty) flags |= 1 << 24;
        if (self.fake) flags |= 1 << 25;
        if (self.gigagroup) flags |= 1 << 26;
        if (self.noforwards) flags |= 1 << 27;
        if (self.join_to_send) flags |= 1 << 28;
        if (self.join_request) flags |= 1 << 29;
        if (self.forum) flags |= 1 << 30;
        if (self.access_hash != null) flags |= 1 << 13;
        if (self.username != null) flags |= 1 << 6;
        if (self.restriction_reason != null) flags |= 1 << 9;
        if (self.admin_rights != null) flags |= 1 << 14;
        if (self.banned_rights != null) flags |= 1 << 15;
        if (self.default_banned_rights != null) flags |= 1 << 18;
        if (self.participants_count != null) flags |= 1 << 17;
        var flags2: u32 = 0;
        if (self.stories_hidden) flags2 |= 1 << 1;
        if (self.stories_hidden_min) flags2 |= 1 << 2;
        if (self.stories_unavailable) flags2 |= 1 << 3;
        if (self.signature_profiles) flags2 |= 1 << 12;
        if (self.autotranslation) flags2 |= 1 << 15;
        if (self.broadcast_messages_allowed) flags2 |= 1 << 16;
        if (self.monoforum) flags2 |= 1 << 17;
        if (self.forum_tabs) flags2 |= 1 << 19;
        if (self.usernames != null) flags2 |= 1 << 0;
        if (self.stories_max_id != null) flags2 |= 1 << 4;
        if (self.color != null) flags2 |= 1 << 7;
        if (self.profile_color != null) flags2 |= 1 << 8;
        if (self.emoji_status != null) flags2 |= 1 << 9;
        if (self.level != null) flags2 |= 1 << 10;
        if (self.subscription_until_date != null) flags2 |= 1 << 11;
        if (self.bot_verification_icon != null) flags2 |= 1 << 13;
        if (self.send_paid_messages_stars != null) flags2 |= 1 << 14;
        if (self.linked_monoforum_id != null) flags2 |= 1 << 18;
        if (self.linked_community_id != null) flags2 |= 1 << 20;
        try w.writeUInt(flags);
        try w.writeUInt(flags2);
        try w.writeLong(self.id);
        if (self.access_hash) |v| {
            try w.writeLong(v);
        }
        try w.writeString(self.title);
        if (self.username) |v| {
            try w.writeString(v);
        }
        try self.photo.serialize(w);
        try w.writeInt(self.date);
        if (self.restriction_reason) |v| {
            try w.writeUInt(vector_constructor_id);
            try w.writeVectorLength(v.len);
            for (v) |elem_916| {
                try elem_916.serialize(w);
            }
        }
        if (self.admin_rights) |v| {
            try v.serialize(w);
        }
        if (self.banned_rights) |v| {
            try v.serialize(w);
        }
        if (self.default_banned_rights) |v| {
            try v.serialize(w);
        }
        if (self.participants_count) |v| {
            try w.writeInt(v);
        }
        if (self.usernames) |v| {
            try w.writeUInt(vector_constructor_id);
            try w.writeVectorLength(v.len);
            for (v) |elem_917| {
                try elem_917.serialize(w);
            }
        }
        if (self.stories_max_id) |v| {
            try v.serialize(w);
        }
        if (self.color) |v| {
            try v.serialize(w);
        }
        if (self.profile_color) |v| {
            try v.serialize(w);
        }
        if (self.emoji_status) |v| {
            try v.serialize(w);
        }
        if (self.level) |v| {
            try w.writeInt(v);
        }
        if (self.subscription_until_date) |v| {
            try w.writeInt(v);
        }
        if (self.bot_verification_icon) |v| {
            try w.writeLong(v);
        }
        if (self.send_paid_messages_stars) |v| {
            try w.writeLong(v);
        }
        if (self.linked_monoforum_id) |v| {
            try w.writeLong(v);
        }
        if (self.linked_community_id) |v| {
            try w.writeLong(v);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.creator = (flags >> 0) & 1 != 0;
        self.left = (flags >> 2) & 1 != 0;
        self.broadcast = (flags >> 5) & 1 != 0;
        self.verified = (flags >> 7) & 1 != 0;
        self.megagroup = (flags >> 8) & 1 != 0;
        self.restricted = (flags >> 9) & 1 != 0;
        self.signatures = (flags >> 11) & 1 != 0;
        self.min = (flags >> 12) & 1 != 0;
        self.scam = (flags >> 19) & 1 != 0;
        self.has_link = (flags >> 20) & 1 != 0;
        self.has_geo = (flags >> 21) & 1 != 0;
        self.slowmode_enabled = (flags >> 22) & 1 != 0;
        self.call_active = (flags >> 23) & 1 != 0;
        self.call_not_empty = (flags >> 24) & 1 != 0;
        self.fake = (flags >> 25) & 1 != 0;
        self.gigagroup = (flags >> 26) & 1 != 0;
        self.noforwards = (flags >> 27) & 1 != 0;
        self.join_to_send = (flags >> 28) & 1 != 0;
        self.join_request = (flags >> 29) & 1 != 0;
        self.forum = (flags >> 30) & 1 != 0;
        const flags2 = try r.readUInt();
        self.stories_hidden = (flags2 >> 1) & 1 != 0;
        self.stories_hidden_min = (flags2 >> 2) & 1 != 0;
        self.stories_unavailable = (flags2 >> 3) & 1 != 0;
        self.signature_profiles = (flags2 >> 12) & 1 != 0;
        self.autotranslation = (flags2 >> 15) & 1 != 0;
        self.broadcast_messages_allowed = (flags2 >> 16) & 1 != 0;
        self.monoforum = (flags2 >> 17) & 1 != 0;
        self.forum_tabs = (flags2 >> 19) & 1 != 0;
        self.id = try r.readLong();
        if ((flags >> 13) & 1 != 0) {
            self.access_hash = try r.readLong();
        } else {
            self.access_hash = null;
        }
        self.title = try r.readString();
        if ((flags >> 6) & 1 != 0) {
            self.username = try r.readString();
        } else {
            self.username = null;
        }
        self.photo = try api.ChatPhoto.deserialize(allocator, r);
        self.date = try r.readInt();
        if ((flags >> 9) & 1 != 0) {
            if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
            const len_918 = try r.readVectorLength();
            const vec_919 = allocator.alloc(api.RestrictionReason, len_918) catch return error.OutOfMemory;
            for (vec_919) |*elem_920| {
                elem_920.* = try api.RestrictionReason.deserialize(allocator, r);
            }
            self.restriction_reason = vec_919;
        } else {
            self.restriction_reason = null;
        }
        if ((flags >> 14) & 1 != 0) {
            self.admin_rights = try api.ChatAdminRights.deserialize(allocator, r);
        } else {
            self.admin_rights = null;
        }
        if ((flags >> 15) & 1 != 0) {
            self.banned_rights = try api.ChatBannedRights.deserialize(allocator, r);
        } else {
            self.banned_rights = null;
        }
        if ((flags >> 18) & 1 != 0) {
            self.default_banned_rights = try api.ChatBannedRights.deserialize(allocator, r);
        } else {
            self.default_banned_rights = null;
        }
        if ((flags >> 17) & 1 != 0) {
            self.participants_count = try r.readInt();
        } else {
            self.participants_count = null;
        }
        if ((flags2 >> 0) & 1 != 0) {
            if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
            const len_921 = try r.readVectorLength();
            const vec_922 = allocator.alloc(api.Username, len_921) catch return error.OutOfMemory;
            for (vec_922) |*elem_923| {
                elem_923.* = try api.Username.deserialize(allocator, r);
            }
            self.usernames = vec_922;
        } else {
            self.usernames = null;
        }
        if ((flags2 >> 4) & 1 != 0) {
            self.stories_max_id = try api.RecentStory.deserialize(allocator, r);
        } else {
            self.stories_max_id = null;
        }
        if ((flags2 >> 7) & 1 != 0) {
            self.color = try api.PeerColor.deserialize(allocator, r);
        } else {
            self.color = null;
        }
        if ((flags2 >> 8) & 1 != 0) {
            self.profile_color = try api.PeerColor.deserialize(allocator, r);
        } else {
            self.profile_color = null;
        }
        if ((flags2 >> 9) & 1 != 0) {
            self.emoji_status = try api.EmojiStatus.deserialize(allocator, r);
        } else {
            self.emoji_status = null;
        }
        if ((flags2 >> 10) & 1 != 0) {
            self.level = try r.readInt();
        } else {
            self.level = null;
        }
        if ((flags2 >> 11) & 1 != 0) {
            self.subscription_until_date = try r.readInt();
        } else {
            self.subscription_until_date = null;
        }
        if ((flags2 >> 13) & 1 != 0) {
            self.bot_verification_icon = try r.readLong();
        } else {
            self.bot_verification_icon = null;
        }
        if ((flags2 >> 14) & 1 != 0) {
            self.send_paid_messages_stars = try r.readLong();
        } else {
            self.send_paid_messages_stars = null;
        }
        if ((flags2 >> 18) & 1 != 0) {
            self.linked_monoforum_id = try r.readLong();
        } else {
            self.linked_monoforum_id = null;
        }
        if ((flags2 >> 20) & 1 != 0) {
            self.linked_community_id = try r.readLong();
        } else {
            self.linked_community_id = null;
        }
        return self;
    }
};

pub const channelForbidden = struct {
    pub const constructor_id: u32 = 0x17d493d5;
    broadcast: bool = false,
    megagroup: bool = false,
    monoforum: bool = false,
    id: i64,
    access_hash: i64,
    title: []const u8,
    until_date: ?i32 = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.broadcast) flags |= 1 << 5;
        if (self.megagroup) flags |= 1 << 8;
        if (self.monoforum) flags |= 1 << 10;
        if (self.until_date != null) flags |= 1 << 16;
        try w.writeUInt(flags);
        try w.writeLong(self.id);
        try w.writeLong(self.access_hash);
        try w.writeString(self.title);
        if (self.until_date) |v| {
            try w.writeInt(v);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.broadcast = (flags >> 5) & 1 != 0;
        self.megagroup = (flags >> 8) & 1 != 0;
        self.monoforum = (flags >> 10) & 1 != 0;
        self.id = try r.readLong();
        self.access_hash = try r.readLong();
        self.title = try r.readString();
        if ((flags >> 16) & 1 != 0) {
            self.until_date = try r.readInt();
        } else {
            self.until_date = null;
        }
        _ = allocator;
        return self;
    }
};

pub const communityForbidden = struct {
    pub const constructor_id: u32 = 0xfd3cdab8;
    id: i64,
    access_hash: ?i64 = null,
    title: []const u8,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.access_hash != null) flags |= 1 << 13;
        try w.writeUInt(flags);
        try w.writeLong(self.id);
        if (self.access_hash) |v| {
            try w.writeLong(v);
        }
        try w.writeString(self.title);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.id = try r.readLong();
        if ((flags >> 13) & 1 != 0) {
            self.access_hash = try r.readLong();
        } else {
            self.access_hash = null;
        }
        self.title = try r.readString();
        _ = allocator;
        return self;
    }
};

pub const community = struct {
    pub const constructor_id: u32 = 0x65efe954;
    creator: bool = false,
    left: bool = false,
    min: bool = false,
    collapsed_in_dialogs: bool = false,
    id: i64,
    access_hash: ?i64 = null,
    title: []const u8,
    photo: api.ChatPhoto,
    date: i32,
    admin_rights: ?api.ChatAdminRights = null,
    default_banned_rights: ?api.ChatBannedRights = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.creator) flags |= 1 << 0;
        if (self.left) flags |= 1 << 2;
        if (self.min) flags |= 1 << 12;
        if (self.access_hash != null) flags |= 1 << 13;
        if (self.admin_rights != null) flags |= 1 << 14;
        if (self.default_banned_rights != null) flags |= 1 << 18;
        var flags2: u32 = 0;
        if (self.collapsed_in_dialogs) flags2 |= 1 << 20;
        try w.writeUInt(flags);
        try w.writeUInt(flags2);
        try w.writeLong(self.id);
        if (self.access_hash) |v| {
            try w.writeLong(v);
        }
        try w.writeString(self.title);
        try self.photo.serialize(w);
        try w.writeInt(self.date);
        if (self.admin_rights) |v| {
            try v.serialize(w);
        }
        if (self.default_banned_rights) |v| {
            try v.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.creator = (flags >> 0) & 1 != 0;
        self.left = (flags >> 2) & 1 != 0;
        self.min = (flags >> 12) & 1 != 0;
        const flags2 = try r.readUInt();
        self.collapsed_in_dialogs = (flags2 >> 20) & 1 != 0;
        self.id = try r.readLong();
        if ((flags >> 13) & 1 != 0) {
            self.access_hash = try r.readLong();
        } else {
            self.access_hash = null;
        }
        self.title = try r.readString();
        self.photo = try api.ChatPhoto.deserialize(allocator, r);
        self.date = try r.readInt();
        if ((flags >> 14) & 1 != 0) {
            self.admin_rights = try api.ChatAdminRights.deserialize(allocator, r);
        } else {
            self.admin_rights = null;
        }
        if ((flags >> 18) & 1 != 0) {
            self.default_banned_rights = try api.ChatBannedRights.deserialize(allocator, r);
        } else {
            self.default_banned_rights = null;
        }
        return self;
    }
};

pub const chatFull = struct {
    pub const constructor_id: u32 = 0x2633421b;
    can_set_username: bool = false,
    has_scheduled: bool = false,
    translations_disabled: bool = false,
    has_welcome_messages: bool = false,
    id: i64,
    about: []const u8,
    participants: api.ChatParticipants,
    chat_photo: ?api.Photo = null,
    notify_settings: api.PeerNotifySettings,
    exported_invite: ?api.ExportedChatInvite = null,
    bot_info: ?[]api.BotInfo = null,
    pinned_msg_id: ?i32 = null,
    folder_id: ?i32 = null,
    call: ?api.InputGroupCall = null,
    ttl_period: ?i32 = null,
    groupcall_default_join_as: ?api.Peer = null,
    theme_emoticon: ?[]const u8 = null,
    requests_pending: ?i32 = null,
    recent_requesters: ?[]i64 = null,
    available_reactions: ?api.ChatReactions = null,
    reactions_limit: ?i32 = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.can_set_username) flags |= 1 << 7;
        if (self.has_scheduled) flags |= 1 << 8;
        if (self.translations_disabled) flags |= 1 << 19;
        if (self.has_welcome_messages) flags |= 1 << 21;
        if (self.chat_photo != null) flags |= 1 << 2;
        if (self.exported_invite != null) flags |= 1 << 13;
        if (self.bot_info != null) flags |= 1 << 3;
        if (self.pinned_msg_id != null) flags |= 1 << 6;
        if (self.folder_id != null) flags |= 1 << 11;
        if (self.call != null) flags |= 1 << 12;
        if (self.ttl_period != null) flags |= 1 << 14;
        if (self.groupcall_default_join_as != null) flags |= 1 << 15;
        if (self.theme_emoticon != null) flags |= 1 << 16;
        if (self.requests_pending != null) flags |= 1 << 17;
        if (self.recent_requesters != null) flags |= 1 << 17;
        if (self.available_reactions != null) flags |= 1 << 18;
        if (self.reactions_limit != null) flags |= 1 << 20;
        try w.writeUInt(flags);
        try w.writeLong(self.id);
        try w.writeString(self.about);
        try self.participants.serialize(w);
        if (self.chat_photo) |v| {
            try v.serialize(w);
        }
        try self.notify_settings.serialize(w);
        if (self.exported_invite) |v| {
            try v.serialize(w);
        }
        if (self.bot_info) |v| {
            try w.writeUInt(vector_constructor_id);
            try w.writeVectorLength(v.len);
            for (v) |elem_924| {
                try elem_924.serialize(w);
            }
        }
        if (self.pinned_msg_id) |v| {
            try w.writeInt(v);
        }
        if (self.folder_id) |v| {
            try w.writeInt(v);
        }
        if (self.call) |v| {
            try v.serialize(w);
        }
        if (self.ttl_period) |v| {
            try w.writeInt(v);
        }
        if (self.groupcall_default_join_as) |v| {
            try v.serialize(w);
        }
        if (self.theme_emoticon) |v| {
            try w.writeString(v);
        }
        if (self.requests_pending) |v| {
            try w.writeInt(v);
        }
        if (self.recent_requesters) |v| {
            try w.writeUInt(vector_constructor_id);
            try w.writeVectorLength(v.len);
            for (v) |elem_925| {
                try w.writeLong(elem_925);
            }
        }
        if (self.available_reactions) |v| {
            try v.serialize(w);
        }
        if (self.reactions_limit) |v| {
            try w.writeInt(v);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.can_set_username = (flags >> 7) & 1 != 0;
        self.has_scheduled = (flags >> 8) & 1 != 0;
        self.translations_disabled = (flags >> 19) & 1 != 0;
        self.has_welcome_messages = (flags >> 21) & 1 != 0;
        self.id = try r.readLong();
        self.about = try r.readString();
        self.participants = try api.ChatParticipants.deserialize(allocator, r);
        if ((flags >> 2) & 1 != 0) {
            self.chat_photo = try api.Photo.deserialize(allocator, r);
        } else {
            self.chat_photo = null;
        }
        self.notify_settings = try api.PeerNotifySettings.deserialize(allocator, r);
        if ((flags >> 13) & 1 != 0) {
            self.exported_invite = try api.ExportedChatInvite.deserialize(allocator, r);
        } else {
            self.exported_invite = null;
        }
        if ((flags >> 3) & 1 != 0) {
            if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
            const len_926 = try r.readVectorLength();
            const vec_927 = allocator.alloc(api.BotInfo, len_926) catch return error.OutOfMemory;
            for (vec_927) |*elem_928| {
                elem_928.* = try api.BotInfo.deserialize(allocator, r);
            }
            self.bot_info = vec_927;
        } else {
            self.bot_info = null;
        }
        if ((flags >> 6) & 1 != 0) {
            self.pinned_msg_id = try r.readInt();
        } else {
            self.pinned_msg_id = null;
        }
        if ((flags >> 11) & 1 != 0) {
            self.folder_id = try r.readInt();
        } else {
            self.folder_id = null;
        }
        if ((flags >> 12) & 1 != 0) {
            self.call = try api.InputGroupCall.deserialize(allocator, r);
        } else {
            self.call = null;
        }
        if ((flags >> 14) & 1 != 0) {
            self.ttl_period = try r.readInt();
        } else {
            self.ttl_period = null;
        }
        if ((flags >> 15) & 1 != 0) {
            self.groupcall_default_join_as = try api.Peer.deserialize(allocator, r);
        } else {
            self.groupcall_default_join_as = null;
        }
        if ((flags >> 16) & 1 != 0) {
            self.theme_emoticon = try r.readString();
        } else {
            self.theme_emoticon = null;
        }
        if ((flags >> 17) & 1 != 0) {
            self.requests_pending = try r.readInt();
        } else {
            self.requests_pending = null;
        }
        if ((flags >> 17) & 1 != 0) {
            if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
            const len_929 = try r.readVectorLength();
            const vec_930 = allocator.alloc(i64, len_929) catch return error.OutOfMemory;
            for (vec_930) |*elem_931| {
                elem_931.* = try r.readLong();
            }
            self.recent_requesters = vec_930;
        } else {
            self.recent_requesters = null;
        }
        if ((flags >> 18) & 1 != 0) {
            self.available_reactions = try api.ChatReactions.deserialize(allocator, r);
        } else {
            self.available_reactions = null;
        }
        if ((flags >> 20) & 1 != 0) {
            self.reactions_limit = try r.readInt();
        } else {
            self.reactions_limit = null;
        }
        return self;
    }
};

pub const channelFull = struct {
    pub const constructor_id: u32 = 0xa04e8d3a;
    can_view_participants: bool = false,
    can_set_username: bool = false,
    can_set_stickers: bool = false,
    hidden_prehistory: bool = false,
    can_set_location: bool = false,
    has_scheduled: bool = false,
    can_view_stats: bool = false,
    blocked: bool = false,
    can_delete_channel: bool = false,
    antispam: bool = false,
    participants_hidden: bool = false,
    translations_disabled: bool = false,
    stories_pinned_available: bool = false,
    view_forum_as_messages: bool = false,
    restricted_sponsored: bool = false,
    can_view_revenue: bool = false,
    paid_media_allowed: bool = false,
    can_view_stars_revenue: bool = false,
    paid_reactions_available: bool = false,
    stargifts_available: bool = false,
    paid_messages_available: bool = false,
    has_welcome_messages: bool = false,
    id: i64,
    about: []const u8,
    participants_count: ?i32 = null,
    admins_count: ?i32 = null,
    kicked_count: ?i32 = null,
    banned_count: ?i32 = null,
    online_count: ?i32 = null,
    read_inbox_max_id: i32,
    read_outbox_max_id: i32,
    unread_count: i32,
    chat_photo: api.Photo,
    notify_settings: api.PeerNotifySettings,
    exported_invite: ?api.ExportedChatInvite = null,
    bot_info: []api.BotInfo,
    migrated_from_chat_id: ?i64 = null,
    migrated_from_max_id: ?i32 = null,
    pinned_msg_id: ?i32 = null,
    stickerset: ?api.StickerSet = null,
    available_min_id: ?i32 = null,
    folder_id: ?i32 = null,
    linked_chat_id: ?i64 = null,
    location: ?api.ChannelLocation = null,
    slowmode_seconds: ?i32 = null,
    slowmode_next_send_date: ?i32 = null,
    stats_dc: ?i32 = null,
    pts: i32,
    call: ?api.InputGroupCall = null,
    ttl_period: ?i32 = null,
    pending_suggestions: ?[][]const u8 = null,
    groupcall_default_join_as: ?api.Peer = null,
    theme_emoticon: ?[]const u8 = null,
    requests_pending: ?i32 = null,
    recent_requesters: ?[]i64 = null,
    default_send_as: ?api.Peer = null,
    available_reactions: ?api.ChatReactions = null,
    reactions_limit: ?i32 = null,
    stories: ?api.PeerStories = null,
    wallpaper: ?api.WallPaper = null,
    boosts_applied: ?i32 = null,
    boosts_unrestrict: ?i32 = null,
    emojiset: ?api.StickerSet = null,
    bot_verification: ?api.BotVerification = null,
    stargifts_count: ?i32 = null,
    send_paid_messages_stars: ?i64 = null,
    main_tab: ?api.ProfileTab = null,
    guard_bot_id: ?i64 = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.can_view_participants) flags |= 1 << 3;
        if (self.can_set_username) flags |= 1 << 6;
        if (self.can_set_stickers) flags |= 1 << 7;
        if (self.hidden_prehistory) flags |= 1 << 10;
        if (self.can_set_location) flags |= 1 << 16;
        if (self.has_scheduled) flags |= 1 << 19;
        if (self.can_view_stats) flags |= 1 << 20;
        if (self.blocked) flags |= 1 << 22;
        if (self.participants_count != null) flags |= 1 << 0;
        if (self.admins_count != null) flags |= 1 << 1;
        if (self.kicked_count != null) flags |= 1 << 2;
        if (self.banned_count != null) flags |= 1 << 2;
        if (self.online_count != null) flags |= 1 << 13;
        if (self.exported_invite != null) flags |= 1 << 23;
        if (self.migrated_from_chat_id != null) flags |= 1 << 4;
        if (self.migrated_from_max_id != null) flags |= 1 << 4;
        if (self.pinned_msg_id != null) flags |= 1 << 5;
        if (self.stickerset != null) flags |= 1 << 8;
        if (self.available_min_id != null) flags |= 1 << 9;
        if (self.folder_id != null) flags |= 1 << 11;
        if (self.linked_chat_id != null) flags |= 1 << 14;
        if (self.location != null) flags |= 1 << 15;
        if (self.slowmode_seconds != null) flags |= 1 << 17;
        if (self.slowmode_next_send_date != null) flags |= 1 << 18;
        if (self.stats_dc != null) flags |= 1 << 12;
        if (self.call != null) flags |= 1 << 21;
        if (self.ttl_period != null) flags |= 1 << 24;
        if (self.pending_suggestions != null) flags |= 1 << 25;
        if (self.groupcall_default_join_as != null) flags |= 1 << 26;
        if (self.theme_emoticon != null) flags |= 1 << 27;
        if (self.requests_pending != null) flags |= 1 << 28;
        if (self.recent_requesters != null) flags |= 1 << 28;
        if (self.default_send_as != null) flags |= 1 << 29;
        if (self.available_reactions != null) flags |= 1 << 30;
        var flags2: u32 = 0;
        if (self.can_delete_channel) flags2 |= 1 << 0;
        if (self.antispam) flags2 |= 1 << 1;
        if (self.participants_hidden) flags2 |= 1 << 2;
        if (self.translations_disabled) flags2 |= 1 << 3;
        if (self.stories_pinned_available) flags2 |= 1 << 5;
        if (self.view_forum_as_messages) flags2 |= 1 << 6;
        if (self.restricted_sponsored) flags2 |= 1 << 11;
        if (self.can_view_revenue) flags2 |= 1 << 12;
        if (self.paid_media_allowed) flags2 |= 1 << 14;
        if (self.can_view_stars_revenue) flags2 |= 1 << 15;
        if (self.paid_reactions_available) flags2 |= 1 << 16;
        if (self.stargifts_available) flags2 |= 1 << 19;
        if (self.paid_messages_available) flags2 |= 1 << 20;
        if (self.has_welcome_messages) flags2 |= 1 << 24;
        if (self.reactions_limit != null) flags2 |= 1 << 13;
        if (self.stories != null) flags2 |= 1 << 4;
        if (self.wallpaper != null) flags2 |= 1 << 7;
        if (self.boosts_applied != null) flags2 |= 1 << 8;
        if (self.boosts_unrestrict != null) flags2 |= 1 << 9;
        if (self.emojiset != null) flags2 |= 1 << 10;
        if (self.bot_verification != null) flags2 |= 1 << 17;
        if (self.stargifts_count != null) flags2 |= 1 << 18;
        if (self.send_paid_messages_stars != null) flags2 |= 1 << 21;
        if (self.main_tab != null) flags2 |= 1 << 22;
        if (self.guard_bot_id != null) flags2 |= 1 << 23;
        try w.writeUInt(flags);
        try w.writeUInt(flags2);
        try w.writeLong(self.id);
        try w.writeString(self.about);
        if (self.participants_count) |v| {
            try w.writeInt(v);
        }
        if (self.admins_count) |v| {
            try w.writeInt(v);
        }
        if (self.kicked_count) |v| {
            try w.writeInt(v);
        }
        if (self.banned_count) |v| {
            try w.writeInt(v);
        }
        if (self.online_count) |v| {
            try w.writeInt(v);
        }
        try w.writeInt(self.read_inbox_max_id);
        try w.writeInt(self.read_outbox_max_id);
        try w.writeInt(self.unread_count);
        try self.chat_photo.serialize(w);
        try self.notify_settings.serialize(w);
        if (self.exported_invite) |v| {
            try v.serialize(w);
        }
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.bot_info.len);
        for (self.bot_info) |elem_932| {
            try elem_932.serialize(w);
        }
        if (self.migrated_from_chat_id) |v| {
            try w.writeLong(v);
        }
        if (self.migrated_from_max_id) |v| {
            try w.writeInt(v);
        }
        if (self.pinned_msg_id) |v| {
            try w.writeInt(v);
        }
        if (self.stickerset) |v| {
            try v.serialize(w);
        }
        if (self.available_min_id) |v| {
            try w.writeInt(v);
        }
        if (self.folder_id) |v| {
            try w.writeInt(v);
        }
        if (self.linked_chat_id) |v| {
            try w.writeLong(v);
        }
        if (self.location) |v| {
            try v.serialize(w);
        }
        if (self.slowmode_seconds) |v| {
            try w.writeInt(v);
        }
        if (self.slowmode_next_send_date) |v| {
            try w.writeInt(v);
        }
        if (self.stats_dc) |v| {
            try w.writeInt(v);
        }
        try w.writeInt(self.pts);
        if (self.call) |v| {
            try v.serialize(w);
        }
        if (self.ttl_period) |v| {
            try w.writeInt(v);
        }
        if (self.pending_suggestions) |v| {
            try w.writeUInt(vector_constructor_id);
            try w.writeVectorLength(v.len);
            for (v) |elem_933| {
                try w.writeString(elem_933);
            }
        }
        if (self.groupcall_default_join_as) |v| {
            try v.serialize(w);
        }
        if (self.theme_emoticon) |v| {
            try w.writeString(v);
        }
        if (self.requests_pending) |v| {
            try w.writeInt(v);
        }
        if (self.recent_requesters) |v| {
            try w.writeUInt(vector_constructor_id);
            try w.writeVectorLength(v.len);
            for (v) |elem_934| {
                try w.writeLong(elem_934);
            }
        }
        if (self.default_send_as) |v| {
            try v.serialize(w);
        }
        if (self.available_reactions) |v| {
            try v.serialize(w);
        }
        if (self.reactions_limit) |v| {
            try w.writeInt(v);
        }
        if (self.stories) |v| {
            try v.serialize(w);
        }
        if (self.wallpaper) |v| {
            try v.serialize(w);
        }
        if (self.boosts_applied) |v| {
            try w.writeInt(v);
        }
        if (self.boosts_unrestrict) |v| {
            try w.writeInt(v);
        }
        if (self.emojiset) |v| {
            try v.serialize(w);
        }
        if (self.bot_verification) |v| {
            try v.serialize(w);
        }
        if (self.stargifts_count) |v| {
            try w.writeInt(v);
        }
        if (self.send_paid_messages_stars) |v| {
            try w.writeLong(v);
        }
        if (self.main_tab) |v| {
            try v.serialize(w);
        }
        if (self.guard_bot_id) |v| {
            try w.writeLong(v);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.can_view_participants = (flags >> 3) & 1 != 0;
        self.can_set_username = (flags >> 6) & 1 != 0;
        self.can_set_stickers = (flags >> 7) & 1 != 0;
        self.hidden_prehistory = (flags >> 10) & 1 != 0;
        self.can_set_location = (flags >> 16) & 1 != 0;
        self.has_scheduled = (flags >> 19) & 1 != 0;
        self.can_view_stats = (flags >> 20) & 1 != 0;
        self.blocked = (flags >> 22) & 1 != 0;
        const flags2 = try r.readUInt();
        self.can_delete_channel = (flags2 >> 0) & 1 != 0;
        self.antispam = (flags2 >> 1) & 1 != 0;
        self.participants_hidden = (flags2 >> 2) & 1 != 0;
        self.translations_disabled = (flags2 >> 3) & 1 != 0;
        self.stories_pinned_available = (flags2 >> 5) & 1 != 0;
        self.view_forum_as_messages = (flags2 >> 6) & 1 != 0;
        self.restricted_sponsored = (flags2 >> 11) & 1 != 0;
        self.can_view_revenue = (flags2 >> 12) & 1 != 0;
        self.paid_media_allowed = (flags2 >> 14) & 1 != 0;
        self.can_view_stars_revenue = (flags2 >> 15) & 1 != 0;
        self.paid_reactions_available = (flags2 >> 16) & 1 != 0;
        self.stargifts_available = (flags2 >> 19) & 1 != 0;
        self.paid_messages_available = (flags2 >> 20) & 1 != 0;
        self.has_welcome_messages = (flags2 >> 24) & 1 != 0;
        self.id = try r.readLong();
        self.about = try r.readString();
        if ((flags >> 0) & 1 != 0) {
            self.participants_count = try r.readInt();
        } else {
            self.participants_count = null;
        }
        if ((flags >> 1) & 1 != 0) {
            self.admins_count = try r.readInt();
        } else {
            self.admins_count = null;
        }
        if ((flags >> 2) & 1 != 0) {
            self.kicked_count = try r.readInt();
        } else {
            self.kicked_count = null;
        }
        if ((flags >> 2) & 1 != 0) {
            self.banned_count = try r.readInt();
        } else {
            self.banned_count = null;
        }
        if ((flags >> 13) & 1 != 0) {
            self.online_count = try r.readInt();
        } else {
            self.online_count = null;
        }
        self.read_inbox_max_id = try r.readInt();
        self.read_outbox_max_id = try r.readInt();
        self.unread_count = try r.readInt();
        self.chat_photo = try api.Photo.deserialize(allocator, r);
        self.notify_settings = try api.PeerNotifySettings.deserialize(allocator, r);
        if ((flags >> 23) & 1 != 0) {
            self.exported_invite = try api.ExportedChatInvite.deserialize(allocator, r);
        } else {
            self.exported_invite = null;
        }
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_935 = try r.readVectorLength();
        const vec_936 = allocator.alloc(api.BotInfo, len_935) catch return error.OutOfMemory;
        for (vec_936) |*elem_937| {
            elem_937.* = try api.BotInfo.deserialize(allocator, r);
        }
        self.bot_info = vec_936;
        if ((flags >> 4) & 1 != 0) {
            self.migrated_from_chat_id = try r.readLong();
        } else {
            self.migrated_from_chat_id = null;
        }
        if ((flags >> 4) & 1 != 0) {
            self.migrated_from_max_id = try r.readInt();
        } else {
            self.migrated_from_max_id = null;
        }
        if ((flags >> 5) & 1 != 0) {
            self.pinned_msg_id = try r.readInt();
        } else {
            self.pinned_msg_id = null;
        }
        if ((flags >> 8) & 1 != 0) {
            self.stickerset = try api.StickerSet.deserialize(allocator, r);
        } else {
            self.stickerset = null;
        }
        if ((flags >> 9) & 1 != 0) {
            self.available_min_id = try r.readInt();
        } else {
            self.available_min_id = null;
        }
        if ((flags >> 11) & 1 != 0) {
            self.folder_id = try r.readInt();
        } else {
            self.folder_id = null;
        }
        if ((flags >> 14) & 1 != 0) {
            self.linked_chat_id = try r.readLong();
        } else {
            self.linked_chat_id = null;
        }
        if ((flags >> 15) & 1 != 0) {
            self.location = try api.ChannelLocation.deserialize(allocator, r);
        } else {
            self.location = null;
        }
        if ((flags >> 17) & 1 != 0) {
            self.slowmode_seconds = try r.readInt();
        } else {
            self.slowmode_seconds = null;
        }
        if ((flags >> 18) & 1 != 0) {
            self.slowmode_next_send_date = try r.readInt();
        } else {
            self.slowmode_next_send_date = null;
        }
        if ((flags >> 12) & 1 != 0) {
            self.stats_dc = try r.readInt();
        } else {
            self.stats_dc = null;
        }
        self.pts = try r.readInt();
        if ((flags >> 21) & 1 != 0) {
            self.call = try api.InputGroupCall.deserialize(allocator, r);
        } else {
            self.call = null;
        }
        if ((flags >> 24) & 1 != 0) {
            self.ttl_period = try r.readInt();
        } else {
            self.ttl_period = null;
        }
        if ((flags >> 25) & 1 != 0) {
            if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
            const len_938 = try r.readVectorLength();
            const vec_939 = allocator.alloc([]const u8, len_938) catch return error.OutOfMemory;
            for (vec_939) |*elem_940| {
                elem_940.* = try r.readString();
            }
            self.pending_suggestions = vec_939;
        } else {
            self.pending_suggestions = null;
        }
        if ((flags >> 26) & 1 != 0) {
            self.groupcall_default_join_as = try api.Peer.deserialize(allocator, r);
        } else {
            self.groupcall_default_join_as = null;
        }
        if ((flags >> 27) & 1 != 0) {
            self.theme_emoticon = try r.readString();
        } else {
            self.theme_emoticon = null;
        }
        if ((flags >> 28) & 1 != 0) {
            self.requests_pending = try r.readInt();
        } else {
            self.requests_pending = null;
        }
        if ((flags >> 28) & 1 != 0) {
            if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
            const len_941 = try r.readVectorLength();
            const vec_942 = allocator.alloc(i64, len_941) catch return error.OutOfMemory;
            for (vec_942) |*elem_943| {
                elem_943.* = try r.readLong();
            }
            self.recent_requesters = vec_942;
        } else {
            self.recent_requesters = null;
        }
        if ((flags >> 29) & 1 != 0) {
            self.default_send_as = try api.Peer.deserialize(allocator, r);
        } else {
            self.default_send_as = null;
        }
        if ((flags >> 30) & 1 != 0) {
            self.available_reactions = try api.ChatReactions.deserialize(allocator, r);
        } else {
            self.available_reactions = null;
        }
        if ((flags2 >> 13) & 1 != 0) {
            self.reactions_limit = try r.readInt();
        } else {
            self.reactions_limit = null;
        }
        if ((flags2 >> 4) & 1 != 0) {
            self.stories = try api.PeerStories.deserialize(allocator, r);
        } else {
            self.stories = null;
        }
        if ((flags2 >> 7) & 1 != 0) {
            self.wallpaper = try api.WallPaper.deserialize(allocator, r);
        } else {
            self.wallpaper = null;
        }
        if ((flags2 >> 8) & 1 != 0) {
            self.boosts_applied = try r.readInt();
        } else {
            self.boosts_applied = null;
        }
        if ((flags2 >> 9) & 1 != 0) {
            self.boosts_unrestrict = try r.readInt();
        } else {
            self.boosts_unrestrict = null;
        }
        if ((flags2 >> 10) & 1 != 0) {
            self.emojiset = try api.StickerSet.deserialize(allocator, r);
        } else {
            self.emojiset = null;
        }
        if ((flags2 >> 17) & 1 != 0) {
            self.bot_verification = try api.BotVerification.deserialize(allocator, r);
        } else {
            self.bot_verification = null;
        }
        if ((flags2 >> 18) & 1 != 0) {
            self.stargifts_count = try r.readInt();
        } else {
            self.stargifts_count = null;
        }
        if ((flags2 >> 21) & 1 != 0) {
            self.send_paid_messages_stars = try r.readLong();
        } else {
            self.send_paid_messages_stars = null;
        }
        if ((flags2 >> 22) & 1 != 0) {
            self.main_tab = try api.ProfileTab.deserialize(allocator, r);
        } else {
            self.main_tab = null;
        }
        if ((flags2 >> 23) & 1 != 0) {
            self.guard_bot_id = try r.readLong();
        } else {
            self.guard_bot_id = null;
        }
        return self;
    }
};

pub const communityFull = struct {
    pub const constructor_id: u32 = 0xcbb7a507;
    id: i64,
    about: []const u8,
    chat_photo: api.Photo,
    linked_peers: []api.CommunityPeer,
    admins_count: ?i32 = null,
    kicked_count: ?i32 = null,
    peer_link_requests_pending: ?i32 = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.admins_count != null) flags |= 1 << 1;
        if (self.kicked_count != null) flags |= 1 << 2;
        if (self.peer_link_requests_pending != null) flags |= 1 << 0;
        try w.writeUInt(flags);
        try w.writeLong(self.id);
        try w.writeString(self.about);
        try self.chat_photo.serialize(w);
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.linked_peers.len);
        for (self.linked_peers) |elem_944| {
            try elem_944.serialize(w);
        }
        if (self.admins_count) |v| {
            try w.writeInt(v);
        }
        if (self.kicked_count) |v| {
            try w.writeInt(v);
        }
        if (self.peer_link_requests_pending) |v| {
            try w.writeInt(v);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.id = try r.readLong();
        self.about = try r.readString();
        self.chat_photo = try api.Photo.deserialize(allocator, r);
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_945 = try r.readVectorLength();
        const vec_946 = allocator.alloc(api.CommunityPeer, len_945) catch return error.OutOfMemory;
        for (vec_946) |*elem_947| {
            elem_947.* = try api.CommunityPeer.deserialize(allocator, r);
        }
        self.linked_peers = vec_946;
        if ((flags >> 1) & 1 != 0) {
            self.admins_count = try r.readInt();
        } else {
            self.admins_count = null;
        }
        if ((flags >> 2) & 1 != 0) {
            self.kicked_count = try r.readInt();
        } else {
            self.kicked_count = null;
        }
        if ((flags >> 0) & 1 != 0) {
            self.peer_link_requests_pending = try r.readInt();
        } else {
            self.peer_link_requests_pending = null;
        }
        return self;
    }
};

pub const chatParticipant = struct {
    pub const constructor_id: u32 = 0x38e79fde;
    user_id: i64,
    inviter_id: i64,
    date: i32,
    rank: ?[]const u8 = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.rank != null) flags |= 1 << 0;
        try w.writeUInt(flags);
        try w.writeLong(self.user_id);
        try w.writeLong(self.inviter_id);
        try w.writeInt(self.date);
        if (self.rank) |v| {
            try w.writeString(v);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.user_id = try r.readLong();
        self.inviter_id = try r.readLong();
        self.date = try r.readInt();
        if ((flags >> 0) & 1 != 0) {
            self.rank = try r.readString();
        } else {
            self.rank = null;
        }
        _ = allocator;
        return self;
    }
};

pub const chatParticipantCreator = struct {
    pub const constructor_id: u32 = 0xe1f867b8;
    user_id: i64,
    rank: ?[]const u8 = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.rank != null) flags |= 1 << 0;
        try w.writeUInt(flags);
        try w.writeLong(self.user_id);
        if (self.rank) |v| {
            try w.writeString(v);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.user_id = try r.readLong();
        if ((flags >> 0) & 1 != 0) {
            self.rank = try r.readString();
        } else {
            self.rank = null;
        }
        _ = allocator;
        return self;
    }
};

pub const chatParticipantAdmin = struct {
    pub const constructor_id: u32 = 0x0360d5d2;
    user_id: i64,
    inviter_id: i64,
    date: i32,
    rank: ?[]const u8 = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.rank != null) flags |= 1 << 0;
        try w.writeUInt(flags);
        try w.writeLong(self.user_id);
        try w.writeLong(self.inviter_id);
        try w.writeInt(self.date);
        if (self.rank) |v| {
            try w.writeString(v);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.user_id = try r.readLong();
        self.inviter_id = try r.readLong();
        self.date = try r.readInt();
        if ((flags >> 0) & 1 != 0) {
            self.rank = try r.readString();
        } else {
            self.rank = null;
        }
        _ = allocator;
        return self;
    }
};

pub const chatParticipantsForbidden = struct {
    pub const constructor_id: u32 = 0x8763d3e1;
    chat_id: i64,
    self_participant: ?api.ChatParticipant = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.self_participant != null) flags |= 1 << 0;
        try w.writeUInt(flags);
        try w.writeLong(self.chat_id);
        if (self.self_participant) |v| {
            try v.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.chat_id = try r.readLong();
        if ((flags >> 0) & 1 != 0) {
            self.self_participant = try api.ChatParticipant.deserialize(allocator, r);
        } else {
            self.self_participant = null;
        }
        return self;
    }
};

pub const chatParticipants = struct {
    pub const constructor_id: u32 = 0x3cbc93f8;
    chat_id: i64,
    participants: []api.ChatParticipant,
    version: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeLong(self.chat_id);
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.participants.len);
        for (self.participants) |elem_948| {
            try elem_948.serialize(w);
        }
        try w.writeInt(self.version);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.chat_id = try r.readLong();
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_949 = try r.readVectorLength();
        const vec_950 = allocator.alloc(api.ChatParticipant, len_949) catch return error.OutOfMemory;
        for (vec_950) |*elem_951| {
            elem_951.* = try api.ChatParticipant.deserialize(allocator, r);
        }
        self.participants = vec_950;
        self.version = try r.readInt();
        return self;
    }
};

pub const chatPhotoEmpty = struct {
    pub const constructor_id: u32 = 0x37c1011c;

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        _ = self;
        _ = w;
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        _ = allocator;
        _ = r;
        return .{};
    }
};

pub const chatPhoto = struct {
    pub const constructor_id: u32 = 0x1c6e1c11;
    has_video: bool = false,
    photo_id: i64,
    stripped_thumb: ?[]const u8 = null,
    dc_id: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.has_video) flags |= 1 << 0;
        if (self.stripped_thumb != null) flags |= 1 << 1;
        try w.writeUInt(flags);
        try w.writeLong(self.photo_id);
        if (self.stripped_thumb) |v| {
            try w.writeBytes(v);
        }
        try w.writeInt(self.dc_id);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.has_video = (flags >> 0) & 1 != 0;
        self.photo_id = try r.readLong();
        if ((flags >> 1) & 1 != 0) {
            self.stripped_thumb = try r.readBytes();
        } else {
            self.stripped_thumb = null;
        }
        self.dc_id = try r.readInt();
        _ = allocator;
        return self;
    }
};

pub const dialog = struct {
    pub const constructor_id: u32 = 0xfc89f7f3;
    pinned: bool = false,
    unread_mark: bool = false,
    view_forum_as_messages: bool = false,
    peer: api.Peer,
    top_message: i32,
    read_inbox_max_id: i32,
    read_outbox_max_id: i32,
    unread_count: i32,
    unread_mentions_count: i32,
    unread_reactions_count: i32,
    unread_poll_votes_count: i32,
    notify_settings: api.PeerNotifySettings,
    pts: ?i32 = null,
    draft: ?api.DraftMessage = null,
    folder_id: ?i32 = null,
    ttl_period: ?i32 = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.pinned) flags |= 1 << 2;
        if (self.unread_mark) flags |= 1 << 3;
        if (self.view_forum_as_messages) flags |= 1 << 6;
        if (self.pts != null) flags |= 1 << 0;
        if (self.draft != null) flags |= 1 << 1;
        if (self.folder_id != null) flags |= 1 << 4;
        if (self.ttl_period != null) flags |= 1 << 5;
        try w.writeUInt(flags);
        try self.peer.serialize(w);
        try w.writeInt(self.top_message);
        try w.writeInt(self.read_inbox_max_id);
        try w.writeInt(self.read_outbox_max_id);
        try w.writeInt(self.unread_count);
        try w.writeInt(self.unread_mentions_count);
        try w.writeInt(self.unread_reactions_count);
        try w.writeInt(self.unread_poll_votes_count);
        try self.notify_settings.serialize(w);
        if (self.pts) |v| {
            try w.writeInt(v);
        }
        if (self.draft) |v| {
            try v.serialize(w);
        }
        if (self.folder_id) |v| {
            try w.writeInt(v);
        }
        if (self.ttl_period) |v| {
            try w.writeInt(v);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.pinned = (flags >> 2) & 1 != 0;
        self.unread_mark = (flags >> 3) & 1 != 0;
        self.view_forum_as_messages = (flags >> 6) & 1 != 0;
        self.peer = try api.Peer.deserialize(allocator, r);
        self.top_message = try r.readInt();
        self.read_inbox_max_id = try r.readInt();
        self.read_outbox_max_id = try r.readInt();
        self.unread_count = try r.readInt();
        self.unread_mentions_count = try r.readInt();
        self.unread_reactions_count = try r.readInt();
        self.unread_poll_votes_count = try r.readInt();
        self.notify_settings = try api.PeerNotifySettings.deserialize(allocator, r);
        if ((flags >> 0) & 1 != 0) {
            self.pts = try r.readInt();
        } else {
            self.pts = null;
        }
        if ((flags >> 1) & 1 != 0) {
            self.draft = try api.DraftMessage.deserialize(allocator, r);
        } else {
            self.draft = null;
        }
        if ((flags >> 4) & 1 != 0) {
            self.folder_id = try r.readInt();
        } else {
            self.folder_id = null;
        }
        if ((flags >> 5) & 1 != 0) {
            self.ttl_period = try r.readInt();
        } else {
            self.ttl_period = null;
        }
        return self;
    }
};

pub const dialogFolder = struct {
    pub const constructor_id: u32 = 0x71bd134c;
    pinned: bool = false,
    folder: api.Folder,
    peer: api.Peer,
    top_message: i32,
    unread_muted_peers_count: i32,
    unread_unmuted_peers_count: i32,
    unread_muted_messages_count: i32,
    unread_unmuted_messages_count: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.pinned) flags |= 1 << 2;
        try w.writeUInt(flags);
        try self.folder.serialize(w);
        try self.peer.serialize(w);
        try w.writeInt(self.top_message);
        try w.writeInt(self.unread_muted_peers_count);
        try w.writeInt(self.unread_unmuted_peers_count);
        try w.writeInt(self.unread_muted_messages_count);
        try w.writeInt(self.unread_unmuted_messages_count);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.pinned = (flags >> 2) & 1 != 0;
        self.folder = try api.Folder.deserialize(allocator, r);
        self.peer = try api.Peer.deserialize(allocator, r);
        self.top_message = try r.readInt();
        self.unread_muted_peers_count = try r.readInt();
        self.unread_unmuted_peers_count = try r.readInt();
        self.unread_muted_messages_count = try r.readInt();
        self.unread_unmuted_messages_count = try r.readInt();
        return self;
    }
};

pub const dialogCommunity = struct {
    pub const constructor_id: u32 = 0xf78a0973;
    pinned: bool = false,
    community_id: i64,
    notify_settings: api.PeerNotifySettings,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.pinned) flags |= 1 << 2;
        try w.writeUInt(flags);
        try w.writeLong(self.community_id);
        try self.notify_settings.serialize(w);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.pinned = (flags >> 2) & 1 != 0;
        self.community_id = try r.readLong();
        self.notify_settings = try api.PeerNotifySettings.deserialize(allocator, r);
        return self;
    }
};

pub const encryptedChatEmpty = struct {
    pub const constructor_id: u32 = 0xab7ec0a0;
    id: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeInt(self.id);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.id = try r.readInt();
        _ = allocator;
        return self;
    }
};

pub const encryptedChatWaiting = struct {
    pub const constructor_id: u32 = 0x66b25953;
    id: i32,
    access_hash: i64,
    date: i32,
    admin_id: i64,
    participant_id: i64,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeInt(self.id);
        try w.writeLong(self.access_hash);
        try w.writeInt(self.date);
        try w.writeLong(self.admin_id);
        try w.writeLong(self.participant_id);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.id = try r.readInt();
        self.access_hash = try r.readLong();
        self.date = try r.readInt();
        self.admin_id = try r.readLong();
        self.participant_id = try r.readLong();
        _ = allocator;
        return self;
    }
};

pub const encryptedChatRequested = struct {
    pub const constructor_id: u32 = 0x48f1d94c;
    folder_id: ?i32 = null,
    id: i32,
    access_hash: i64,
    date: i32,
    admin_id: i64,
    participant_id: i64,
    g_a: []const u8,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.folder_id != null) flags |= 1 << 0;
        try w.writeUInt(flags);
        if (self.folder_id) |v| {
            try w.writeInt(v);
        }
        try w.writeInt(self.id);
        try w.writeLong(self.access_hash);
        try w.writeInt(self.date);
        try w.writeLong(self.admin_id);
        try w.writeLong(self.participant_id);
        try w.writeBytes(self.g_a);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        if ((flags >> 0) & 1 != 0) {
            self.folder_id = try r.readInt();
        } else {
            self.folder_id = null;
        }
        self.id = try r.readInt();
        self.access_hash = try r.readLong();
        self.date = try r.readInt();
        self.admin_id = try r.readLong();
        self.participant_id = try r.readLong();
        self.g_a = try r.readBytes();
        _ = allocator;
        return self;
    }
};

pub const encryptedChat = struct {
    pub const constructor_id: u32 = 0x61f0d4c7;
    id: i32,
    access_hash: i64,
    date: i32,
    admin_id: i64,
    participant_id: i64,
    g_a_or_b: []const u8,
    key_fingerprint: i64,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeInt(self.id);
        try w.writeLong(self.access_hash);
        try w.writeInt(self.date);
        try w.writeLong(self.admin_id);
        try w.writeLong(self.participant_id);
        try w.writeBytes(self.g_a_or_b);
        try w.writeLong(self.key_fingerprint);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.id = try r.readInt();
        self.access_hash = try r.readLong();
        self.date = try r.readInt();
        self.admin_id = try r.readLong();
        self.participant_id = try r.readLong();
        self.g_a_or_b = try r.readBytes();
        self.key_fingerprint = try r.readLong();
        _ = allocator;
        return self;
    }
};

pub const encryptedChatDiscarded = struct {
    pub const constructor_id: u32 = 0x1e1c7c45;
    history_deleted: bool = false,
    id: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.history_deleted) flags |= 1 << 0;
        try w.writeUInt(flags);
        try w.writeInt(self.id);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.history_deleted = (flags >> 0) & 1 != 0;
        self.id = try r.readInt();
        _ = allocator;
        return self;
    }
};

pub const inputEncryptedChat = struct {
    pub const constructor_id: u32 = 0xf141b5e1;
    chat_id: i32,
    access_hash: i64,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeInt(self.chat_id);
        try w.writeLong(self.access_hash);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.chat_id = try r.readInt();
        self.access_hash = try r.readLong();
        _ = allocator;
        return self;
    }
};

pub const encryptedMessage = struct {
    pub const constructor_id: u32 = 0xed18c118;
    random_id: i64,
    chat_id: i32,
    date: i32,
    bytes: []const u8,
    file: api.EncryptedFile,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeLong(self.random_id);
        try w.writeInt(self.chat_id);
        try w.writeInt(self.date);
        try w.writeBytes(self.bytes);
        try self.file.serialize(w);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.random_id = try r.readLong();
        self.chat_id = try r.readInt();
        self.date = try r.readInt();
        self.bytes = try r.readBytes();
        self.file = try api.EncryptedFile.deserialize(allocator, r);
        return self;
    }
};

pub const encryptedMessageService = struct {
    pub const constructor_id: u32 = 0x23734b06;
    random_id: i64,
    chat_id: i32,
    date: i32,
    bytes: []const u8,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeLong(self.random_id);
        try w.writeInt(self.chat_id);
        try w.writeInt(self.date);
        try w.writeBytes(self.bytes);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.random_id = try r.readLong();
        self.chat_id = try r.readInt();
        self.date = try r.readInt();
        self.bytes = try r.readBytes();
        _ = allocator;
        return self;
    }
};

pub const chatInviteExported = struct {
    pub const constructor_id: u32 = 0xa22cbd96;
    revoked: bool = false,
    permanent: bool = false,
    request_needed: bool = false,
    link: []const u8,
    admin_id: i64,
    date: i32,
    start_date: ?i32 = null,
    expire_date: ?i32 = null,
    usage_limit: ?i32 = null,
    usage: ?i32 = null,
    requested: ?i32 = null,
    subscription_expired: ?i32 = null,
    title: ?[]const u8 = null,
    subscription_pricing: ?api.StarsSubscriptionPricing = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.revoked) flags |= 1 << 0;
        if (self.permanent) flags |= 1 << 5;
        if (self.request_needed) flags |= 1 << 6;
        if (self.start_date != null) flags |= 1 << 4;
        if (self.expire_date != null) flags |= 1 << 1;
        if (self.usage_limit != null) flags |= 1 << 2;
        if (self.usage != null) flags |= 1 << 3;
        if (self.requested != null) flags |= 1 << 7;
        if (self.subscription_expired != null) flags |= 1 << 10;
        if (self.title != null) flags |= 1 << 8;
        if (self.subscription_pricing != null) flags |= 1 << 9;
        try w.writeUInt(flags);
        try w.writeString(self.link);
        try w.writeLong(self.admin_id);
        try w.writeInt(self.date);
        if (self.start_date) |v| {
            try w.writeInt(v);
        }
        if (self.expire_date) |v| {
            try w.writeInt(v);
        }
        if (self.usage_limit) |v| {
            try w.writeInt(v);
        }
        if (self.usage) |v| {
            try w.writeInt(v);
        }
        if (self.requested) |v| {
            try w.writeInt(v);
        }
        if (self.subscription_expired) |v| {
            try w.writeInt(v);
        }
        if (self.title) |v| {
            try w.writeString(v);
        }
        if (self.subscription_pricing) |v| {
            try v.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.revoked = (flags >> 0) & 1 != 0;
        self.permanent = (flags >> 5) & 1 != 0;
        self.request_needed = (flags >> 6) & 1 != 0;
        self.link = try r.readString();
        self.admin_id = try r.readLong();
        self.date = try r.readInt();
        if ((flags >> 4) & 1 != 0) {
            self.start_date = try r.readInt();
        } else {
            self.start_date = null;
        }
        if ((flags >> 1) & 1 != 0) {
            self.expire_date = try r.readInt();
        } else {
            self.expire_date = null;
        }
        if ((flags >> 2) & 1 != 0) {
            self.usage_limit = try r.readInt();
        } else {
            self.usage_limit = null;
        }
        if ((flags >> 3) & 1 != 0) {
            self.usage = try r.readInt();
        } else {
            self.usage = null;
        }
        if ((flags >> 7) & 1 != 0) {
            self.requested = try r.readInt();
        } else {
            self.requested = null;
        }
        if ((flags >> 10) & 1 != 0) {
            self.subscription_expired = try r.readInt();
        } else {
            self.subscription_expired = null;
        }
        if ((flags >> 8) & 1 != 0) {
            self.title = try r.readString();
        } else {
            self.title = null;
        }
        if ((flags >> 9) & 1 != 0) {
            self.subscription_pricing = try api.StarsSubscriptionPricing.deserialize(allocator, r);
        } else {
            self.subscription_pricing = null;
        }
        return self;
    }
};

pub const chatInvitePublicJoinRequests = struct {
    pub const constructor_id: u32 = 0xed107ab7;

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        _ = self;
        _ = w;
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        _ = allocator;
        _ = r;
        return .{};
    }
};

pub const chatInviteAlready = struct {
    pub const constructor_id: u32 = 0x5a686d7c;
    chat: api.Chat,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try self.chat.serialize(w);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.chat = try api.Chat.deserialize(allocator, r);
        return self;
    }
};

pub const chatInvite = struct {
    pub const constructor_id: u32 = 0x5c9d3702;
    channel: bool = false,
    broadcast: bool = false,
    public: bool = false,
    megagroup: bool = false,
    request_needed: bool = false,
    verified: bool = false,
    scam: bool = false,
    fake: bool = false,
    can_refulfill_subscription: bool = false,
    title: []const u8,
    about: ?[]const u8 = null,
    photo: api.Photo,
    participants_count: i32,
    participants: ?[]api.User = null,
    color: i32,
    subscription_pricing: ?api.StarsSubscriptionPricing = null,
    subscription_form_id: ?i64 = null,
    bot_verification: ?api.BotVerification = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.channel) flags |= 1 << 0;
        if (self.broadcast) flags |= 1 << 1;
        if (self.public) flags |= 1 << 2;
        if (self.megagroup) flags |= 1 << 3;
        if (self.request_needed) flags |= 1 << 6;
        if (self.verified) flags |= 1 << 7;
        if (self.scam) flags |= 1 << 8;
        if (self.fake) flags |= 1 << 9;
        if (self.can_refulfill_subscription) flags |= 1 << 11;
        if (self.about != null) flags |= 1 << 5;
        if (self.participants != null) flags |= 1 << 4;
        if (self.subscription_pricing != null) flags |= 1 << 10;
        if (self.subscription_form_id != null) flags |= 1 << 12;
        if (self.bot_verification != null) flags |= 1 << 13;
        try w.writeUInt(flags);
        try w.writeString(self.title);
        if (self.about) |v| {
            try w.writeString(v);
        }
        try self.photo.serialize(w);
        try w.writeInt(self.participants_count);
        if (self.participants) |v| {
            try w.writeUInt(vector_constructor_id);
            try w.writeVectorLength(v.len);
            for (v) |elem_952| {
                try elem_952.serialize(w);
            }
        }
        try w.writeInt(self.color);
        if (self.subscription_pricing) |v| {
            try v.serialize(w);
        }
        if (self.subscription_form_id) |v| {
            try w.writeLong(v);
        }
        if (self.bot_verification) |v| {
            try v.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.channel = (flags >> 0) & 1 != 0;
        self.broadcast = (flags >> 1) & 1 != 0;
        self.public = (flags >> 2) & 1 != 0;
        self.megagroup = (flags >> 3) & 1 != 0;
        self.request_needed = (flags >> 6) & 1 != 0;
        self.verified = (flags >> 7) & 1 != 0;
        self.scam = (flags >> 8) & 1 != 0;
        self.fake = (flags >> 9) & 1 != 0;
        self.can_refulfill_subscription = (flags >> 11) & 1 != 0;
        self.title = try r.readString();
        if ((flags >> 5) & 1 != 0) {
            self.about = try r.readString();
        } else {
            self.about = null;
        }
        self.photo = try api.Photo.deserialize(allocator, r);
        self.participants_count = try r.readInt();
        if ((flags >> 4) & 1 != 0) {
            if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
            const len_953 = try r.readVectorLength();
            const vec_954 = allocator.alloc(api.User, len_953) catch return error.OutOfMemory;
            for (vec_954) |*elem_955| {
                elem_955.* = try api.User.deserialize(allocator, r);
            }
            self.participants = vec_954;
        } else {
            self.participants = null;
        }
        self.color = try r.readInt();
        if ((flags >> 10) & 1 != 0) {
            self.subscription_pricing = try api.StarsSubscriptionPricing.deserialize(allocator, r);
        } else {
            self.subscription_pricing = null;
        }
        if ((flags >> 12) & 1 != 0) {
            self.subscription_form_id = try r.readLong();
        } else {
            self.subscription_form_id = null;
        }
        if ((flags >> 13) & 1 != 0) {
            self.bot_verification = try api.BotVerification.deserialize(allocator, r);
        } else {
            self.bot_verification = null;
        }
        return self;
    }
};

pub const chatInvitePeek = struct {
    pub const constructor_id: u32 = 0x61695cb0;
    chat: api.Chat,
    expires: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try self.chat.serialize(w);
        try w.writeInt(self.expires);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.chat = try api.Chat.deserialize(allocator, r);
        self.expires = try r.readInt();
        return self;
    }
};

pub const draftMessageEmpty = struct {
    pub const constructor_id: u32 = 0x1b0c841a;
    date: ?i32 = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.date != null) flags |= 1 << 0;
        try w.writeUInt(flags);
        if (self.date) |v| {
            try w.writeInt(v);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        if ((flags >> 0) & 1 != 0) {
            self.date = try r.readInt();
        } else {
            self.date = null;
        }
        _ = allocator;
        return self;
    }
};

pub const draftMessage = struct {
    pub const constructor_id: u32 = 0x60fe3294;
    no_webpage: bool = false,
    invert_media: bool = false,
    reply_to: ?api.InputReplyTo = null,
    message: []const u8,
    entities: ?[]api.MessageEntity = null,
    media: ?*api.InputMedia = null,
    date: i32,
    effect: ?i64 = null,
    suggested_post: ?api.SuggestedPost = null,
    rich_message: ?api.RichMessage = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.no_webpage) flags |= 1 << 1;
        if (self.invert_media) flags |= 1 << 6;
        if (self.reply_to != null) flags |= 1 << 4;
        if (self.entities != null) flags |= 1 << 3;
        if (self.media != null) flags |= 1 << 5;
        if (self.effect != null) flags |= 1 << 7;
        if (self.suggested_post != null) flags |= 1 << 8;
        if (self.rich_message != null) flags |= 1 << 9;
        try w.writeUInt(flags);
        if (self.reply_to) |v| {
            try v.serialize(w);
        }
        try w.writeString(self.message);
        if (self.entities) |v| {
            try w.writeUInt(vector_constructor_id);
            try w.writeVectorLength(v.len);
            for (v) |elem_956| {
                try elem_956.serialize(w);
            }
        }
        if (self.media) |v| {
            try v.serialize(w);
        }
        try w.writeInt(self.date);
        if (self.effect) |v| {
            try w.writeLong(v);
        }
        if (self.suggested_post) |v| {
            try v.serialize(w);
        }
        if (self.rich_message) |v| {
            try v.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.no_webpage = (flags >> 1) & 1 != 0;
        self.invert_media = (flags >> 6) & 1 != 0;
        if ((flags >> 4) & 1 != 0) {
            self.reply_to = try api.InputReplyTo.deserialize(allocator, r);
        } else {
            self.reply_to = null;
        }
        self.message = try r.readString();
        if ((flags >> 3) & 1 != 0) {
            if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
            const len_957 = try r.readVectorLength();
            const vec_958 = allocator.alloc(api.MessageEntity, len_957) catch return error.OutOfMemory;
            for (vec_958) |*elem_959| {
                elem_959.* = try api.MessageEntity.deserialize(allocator, r);
            }
            self.entities = vec_958;
        } else {
            self.entities = null;
        }
        if ((flags >> 5) & 1 != 0) {
                        const ptr_960 = allocator.create(api.InputMedia) catch return error.OutOfMemory;
            ptr_960.* = try api.InputMedia.deserialize(allocator, r);
            self.media = ptr_960;
        } else {
            self.media = null;
        }
        self.date = try r.readInt();
        if ((flags >> 7) & 1 != 0) {
            self.effect = try r.readLong();
        } else {
            self.effect = null;
        }
        if ((flags >> 8) & 1 != 0) {
            self.suggested_post = try api.SuggestedPost.deserialize(allocator, r);
        } else {
            self.suggested_post = null;
        }
        if ((flags >> 9) & 1 != 0) {
            self.rich_message = try api.RichMessage.deserialize(allocator, r);
        } else {
            self.rich_message = null;
        }
        return self;
    }
};

pub const dialogPeer = struct {
    pub const constructor_id: u32 = 0xe56dbf05;
    peer: api.Peer,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try self.peer.serialize(w);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.peer = try api.Peer.deserialize(allocator, r);
        return self;
    }
};

pub const dialogPeerFolder = struct {
    pub const constructor_id: u32 = 0x514519e2;
    folder_id: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeInt(self.folder_id);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.folder_id = try r.readInt();
        _ = allocator;
        return self;
    }
};

pub const dialogPeerCommunity = struct {
    pub const constructor_id: u32 = 0x2f65c8e4;
    community_id: i64,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeLong(self.community_id);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.community_id = try r.readLong();
        _ = allocator;
        return self;
    }
};

pub const chatOnlines = struct {
    pub const constructor_id: u32 = 0xf041e250;
    onlines: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeInt(self.onlines);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.onlines = try r.readInt();
        _ = allocator;
        return self;
    }
};

pub const chatAdminRights = struct {
    pub const constructor_id: u32 = 0x5fb224d5;
    change_info: bool = false,
    post_messages: bool = false,
    edit_messages: bool = false,
    delete_messages: bool = false,
    ban_users: bool = false,
    invite_users: bool = false,
    pin_messages: bool = false,
    add_admins: bool = false,
    anonymous: bool = false,
    manage_call: bool = false,
    other: bool = false,
    manage_topics: bool = false,
    post_stories: bool = false,
    edit_stories: bool = false,
    delete_stories: bool = false,
    manage_direct_messages: bool = false,
    manage_ranks: bool = false,
    manage_linked_peers: bool = false,
    manage_welcome_messages: bool = false,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.change_info) flags |= 1 << 0;
        if (self.post_messages) flags |= 1 << 1;
        if (self.edit_messages) flags |= 1 << 2;
        if (self.delete_messages) flags |= 1 << 3;
        if (self.ban_users) flags |= 1 << 4;
        if (self.invite_users) flags |= 1 << 5;
        if (self.pin_messages) flags |= 1 << 7;
        if (self.add_admins) flags |= 1 << 9;
        if (self.anonymous) flags |= 1 << 10;
        if (self.manage_call) flags |= 1 << 11;
        if (self.other) flags |= 1 << 12;
        if (self.manage_topics) flags |= 1 << 13;
        if (self.post_stories) flags |= 1 << 14;
        if (self.edit_stories) flags |= 1 << 15;
        if (self.delete_stories) flags |= 1 << 16;
        if (self.manage_direct_messages) flags |= 1 << 17;
        if (self.manage_ranks) flags |= 1 << 18;
        if (self.manage_linked_peers) flags |= 1 << 19;
        if (self.manage_welcome_messages) flags |= 1 << 20;
        try w.writeUInt(flags);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.change_info = (flags >> 0) & 1 != 0;
        self.post_messages = (flags >> 1) & 1 != 0;
        self.edit_messages = (flags >> 2) & 1 != 0;
        self.delete_messages = (flags >> 3) & 1 != 0;
        self.ban_users = (flags >> 4) & 1 != 0;
        self.invite_users = (flags >> 5) & 1 != 0;
        self.pin_messages = (flags >> 7) & 1 != 0;
        self.add_admins = (flags >> 9) & 1 != 0;
        self.anonymous = (flags >> 10) & 1 != 0;
        self.manage_call = (flags >> 11) & 1 != 0;
        self.other = (flags >> 12) & 1 != 0;
        self.manage_topics = (flags >> 13) & 1 != 0;
        self.post_stories = (flags >> 14) & 1 != 0;
        self.edit_stories = (flags >> 15) & 1 != 0;
        self.delete_stories = (flags >> 16) & 1 != 0;
        self.manage_direct_messages = (flags >> 17) & 1 != 0;
        self.manage_ranks = (flags >> 18) & 1 != 0;
        self.manage_linked_peers = (flags >> 19) & 1 != 0;
        self.manage_welcome_messages = (flags >> 20) & 1 != 0;
        _ = allocator;
        return self;
    }
};

pub const chatBannedRights = struct {
    pub const constructor_id: u32 = 0x9f120418;
    view_messages: bool = false,
    send_messages: bool = false,
    send_media: bool = false,
    send_stickers: bool = false,
    send_gifs: bool = false,
    send_games: bool = false,
    send_inline: bool = false,
    embed_links: bool = false,
    send_polls: bool = false,
    change_info: bool = false,
    invite_users: bool = false,
    pin_messages: bool = false,
    manage_topics: bool = false,
    send_photos: bool = false,
    send_videos: bool = false,
    send_roundvideos: bool = false,
    send_audios: bool = false,
    send_voices: bool = false,
    send_docs: bool = false,
    send_plain: bool = false,
    edit_rank: bool = false,
    send_reactions: bool = false,
    manage_linked_peers: bool = false,
    until_date: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.view_messages) flags |= 1 << 0;
        if (self.send_messages) flags |= 1 << 1;
        if (self.send_media) flags |= 1 << 2;
        if (self.send_stickers) flags |= 1 << 3;
        if (self.send_gifs) flags |= 1 << 4;
        if (self.send_games) flags |= 1 << 5;
        if (self.send_inline) flags |= 1 << 6;
        if (self.embed_links) flags |= 1 << 7;
        if (self.send_polls) flags |= 1 << 8;
        if (self.change_info) flags |= 1 << 10;
        if (self.invite_users) flags |= 1 << 15;
        if (self.pin_messages) flags |= 1 << 17;
        if (self.manage_topics) flags |= 1 << 18;
        if (self.send_photos) flags |= 1 << 19;
        if (self.send_videos) flags |= 1 << 20;
        if (self.send_roundvideos) flags |= 1 << 21;
        if (self.send_audios) flags |= 1 << 22;
        if (self.send_voices) flags |= 1 << 23;
        if (self.send_docs) flags |= 1 << 24;
        if (self.send_plain) flags |= 1 << 25;
        if (self.edit_rank) flags |= 1 << 26;
        if (self.send_reactions) flags |= 1 << 27;
        if (self.manage_linked_peers) flags |= 1 << 28;
        try w.writeUInt(flags);
        try w.writeInt(self.until_date);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.view_messages = (flags >> 0) & 1 != 0;
        self.send_messages = (flags >> 1) & 1 != 0;
        self.send_media = (flags >> 2) & 1 != 0;
        self.send_stickers = (flags >> 3) & 1 != 0;
        self.send_gifs = (flags >> 4) & 1 != 0;
        self.send_games = (flags >> 5) & 1 != 0;
        self.send_inline = (flags >> 6) & 1 != 0;
        self.embed_links = (flags >> 7) & 1 != 0;
        self.send_polls = (flags >> 8) & 1 != 0;
        self.change_info = (flags >> 10) & 1 != 0;
        self.invite_users = (flags >> 15) & 1 != 0;
        self.pin_messages = (flags >> 17) & 1 != 0;
        self.manage_topics = (flags >> 18) & 1 != 0;
        self.send_photos = (flags >> 19) & 1 != 0;
        self.send_videos = (flags >> 20) & 1 != 0;
        self.send_roundvideos = (flags >> 21) & 1 != 0;
        self.send_audios = (flags >> 22) & 1 != 0;
        self.send_voices = (flags >> 23) & 1 != 0;
        self.send_docs = (flags >> 24) & 1 != 0;
        self.send_plain = (flags >> 25) & 1 != 0;
        self.edit_rank = (flags >> 26) & 1 != 0;
        self.send_reactions = (flags >> 27) & 1 != 0;
        self.manage_linked_peers = (flags >> 28) & 1 != 0;
        self.until_date = try r.readInt();
        _ = allocator;
        return self;
    }
};

pub const folder = struct {
    pub const constructor_id: u32 = 0xff544e65;
    autofill_new_broadcasts: bool = false,
    autofill_public_groups: bool = false,
    autofill_new_correspondents: bool = false,
    id: i32,
    title: []const u8,
    photo: ?api.ChatPhoto = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.autofill_new_broadcasts) flags |= 1 << 0;
        if (self.autofill_public_groups) flags |= 1 << 1;
        if (self.autofill_new_correspondents) flags |= 1 << 2;
        if (self.photo != null) flags |= 1 << 3;
        try w.writeUInt(flags);
        try w.writeInt(self.id);
        try w.writeString(self.title);
        if (self.photo) |v| {
            try v.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.autofill_new_broadcasts = (flags >> 0) & 1 != 0;
        self.autofill_public_groups = (flags >> 1) & 1 != 0;
        self.autofill_new_correspondents = (flags >> 2) & 1 != 0;
        self.id = try r.readInt();
        self.title = try r.readString();
        if ((flags >> 3) & 1 != 0) {
            self.photo = try api.ChatPhoto.deserialize(allocator, r);
        } else {
            self.photo = null;
        }
        return self;
    }
};

pub const inputFolderPeer = struct {
    pub const constructor_id: u32 = 0xfbd2c296;
    peer: *api.InputPeer,
    folder_id: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try self.peer.serialize(w);
        try w.writeInt(self.folder_id);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
                const ptr_961 = allocator.create(api.InputPeer) catch return error.OutOfMemory;
        ptr_961.* = try api.InputPeer.deserialize(allocator, r);
        self.peer = ptr_961;
        self.folder_id = try r.readInt();
        return self;
    }
};

pub const folderPeer = struct {
    pub const constructor_id: u32 = 0xe9baa668;
    peer: api.Peer,
    folder_id: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try self.peer.serialize(w);
        try w.writeInt(self.folder_id);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.peer = try api.Peer.deserialize(allocator, r);
        self.folder_id = try r.readInt();
        return self;
    }
};

pub const dialogFilter = struct {
    pub const constructor_id: u32 = 0xaa472651;
    contacts: bool = false,
    non_contacts: bool = false,
    groups: bool = false,
    broadcasts: bool = false,
    bots: bool = false,
    exclude_muted: bool = false,
    exclude_read: bool = false,
    exclude_archived: bool = false,
    title_noanimate: bool = false,
    id: i32,
    title: api.TextWithEntities,
    emoticon: ?[]const u8 = null,
    color: ?i32 = null,
    pinned_peers: []*api.InputPeer,
    include_peers: []*api.InputPeer,
    exclude_peers: []*api.InputPeer,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.contacts) flags |= 1 << 0;
        if (self.non_contacts) flags |= 1 << 1;
        if (self.groups) flags |= 1 << 2;
        if (self.broadcasts) flags |= 1 << 3;
        if (self.bots) flags |= 1 << 4;
        if (self.exclude_muted) flags |= 1 << 11;
        if (self.exclude_read) flags |= 1 << 12;
        if (self.exclude_archived) flags |= 1 << 13;
        if (self.title_noanimate) flags |= 1 << 28;
        if (self.emoticon != null) flags |= 1 << 25;
        if (self.color != null) flags |= 1 << 27;
        try w.writeUInt(flags);
        try w.writeInt(self.id);
        try self.title.serialize(w);
        if (self.emoticon) |v| {
            try w.writeString(v);
        }
        if (self.color) |v| {
            try w.writeInt(v);
        }
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.pinned_peers.len);
        for (self.pinned_peers) |elem_962| {
            try elem_962.serialize(w);
        }
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.include_peers.len);
        for (self.include_peers) |elem_963| {
            try elem_963.serialize(w);
        }
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.exclude_peers.len);
        for (self.exclude_peers) |elem_964| {
            try elem_964.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.contacts = (flags >> 0) & 1 != 0;
        self.non_contacts = (flags >> 1) & 1 != 0;
        self.groups = (flags >> 2) & 1 != 0;
        self.broadcasts = (flags >> 3) & 1 != 0;
        self.bots = (flags >> 4) & 1 != 0;
        self.exclude_muted = (flags >> 11) & 1 != 0;
        self.exclude_read = (flags >> 12) & 1 != 0;
        self.exclude_archived = (flags >> 13) & 1 != 0;
        self.title_noanimate = (flags >> 28) & 1 != 0;
        self.id = try r.readInt();
        self.title = try api.TextWithEntities.deserialize(allocator, r);
        if ((flags >> 25) & 1 != 0) {
            self.emoticon = try r.readString();
        } else {
            self.emoticon = null;
        }
        if ((flags >> 27) & 1 != 0) {
            self.color = try r.readInt();
        } else {
            self.color = null;
        }
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_965 = try r.readVectorLength();
        const vec_966 = allocator.alloc(*api.InputPeer, len_965) catch return error.OutOfMemory;
        for (vec_966) |*elem_967| {
                        const ptr_968 = allocator.create(api.InputPeer) catch return error.OutOfMemory;
            ptr_968.* = try api.InputPeer.deserialize(allocator, r);
            elem_967.* = ptr_968;
        }
        self.pinned_peers = vec_966;
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_969 = try r.readVectorLength();
        const vec_970 = allocator.alloc(*api.InputPeer, len_969) catch return error.OutOfMemory;
        for (vec_970) |*elem_971| {
                        const ptr_972 = allocator.create(api.InputPeer) catch return error.OutOfMemory;
            ptr_972.* = try api.InputPeer.deserialize(allocator, r);
            elem_971.* = ptr_972;
        }
        self.include_peers = vec_970;
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_973 = try r.readVectorLength();
        const vec_974 = allocator.alloc(*api.InputPeer, len_973) catch return error.OutOfMemory;
        for (vec_974) |*elem_975| {
                        const ptr_976 = allocator.create(api.InputPeer) catch return error.OutOfMemory;
            ptr_976.* = try api.InputPeer.deserialize(allocator, r);
            elem_975.* = ptr_976;
        }
        self.exclude_peers = vec_974;
        return self;
    }
};

pub const dialogFilterDefault = struct {
    pub const constructor_id: u32 = 0x363293ae;

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        _ = self;
        _ = w;
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        _ = allocator;
        _ = r;
        return .{};
    }
};

pub const dialogFilterChatlist = struct {
    pub const constructor_id: u32 = 0x96537bd7;
    has_my_invites: bool = false,
    title_noanimate: bool = false,
    id: i32,
    title: api.TextWithEntities,
    emoticon: ?[]const u8 = null,
    color: ?i32 = null,
    pinned_peers: []*api.InputPeer,
    include_peers: []*api.InputPeer,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.has_my_invites) flags |= 1 << 26;
        if (self.title_noanimate) flags |= 1 << 28;
        if (self.emoticon != null) flags |= 1 << 25;
        if (self.color != null) flags |= 1 << 27;
        try w.writeUInt(flags);
        try w.writeInt(self.id);
        try self.title.serialize(w);
        if (self.emoticon) |v| {
            try w.writeString(v);
        }
        if (self.color) |v| {
            try w.writeInt(v);
        }
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.pinned_peers.len);
        for (self.pinned_peers) |elem_977| {
            try elem_977.serialize(w);
        }
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.include_peers.len);
        for (self.include_peers) |elem_978| {
            try elem_978.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.has_my_invites = (flags >> 26) & 1 != 0;
        self.title_noanimate = (flags >> 28) & 1 != 0;
        self.id = try r.readInt();
        self.title = try api.TextWithEntities.deserialize(allocator, r);
        if ((flags >> 25) & 1 != 0) {
            self.emoticon = try r.readString();
        } else {
            self.emoticon = null;
        }
        if ((flags >> 27) & 1 != 0) {
            self.color = try r.readInt();
        } else {
            self.color = null;
        }
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_979 = try r.readVectorLength();
        const vec_980 = allocator.alloc(*api.InputPeer, len_979) catch return error.OutOfMemory;
        for (vec_980) |*elem_981| {
                        const ptr_982 = allocator.create(api.InputPeer) catch return error.OutOfMemory;
            ptr_982.* = try api.InputPeer.deserialize(allocator, r);
            elem_981.* = ptr_982;
        }
        self.pinned_peers = vec_980;
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_983 = try r.readVectorLength();
        const vec_984 = allocator.alloc(*api.InputPeer, len_983) catch return error.OutOfMemory;
        for (vec_984) |*elem_985| {
                        const ptr_986 = allocator.create(api.InputPeer) catch return error.OutOfMemory;
            ptr_986.* = try api.InputPeer.deserialize(allocator, r);
            elem_985.* = ptr_986;
        }
        self.include_peers = vec_984;
        return self;
    }
};

pub const dialogFilterSuggested = struct {
    pub const constructor_id: u32 = 0x77744d4a;
    filter: api.DialogFilter,
    description: []const u8,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try self.filter.serialize(w);
        try w.writeString(self.description);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.filter = try api.DialogFilter.deserialize(allocator, r);
        self.description = try r.readString();
        return self;
    }
};

pub const chatInviteImporter = struct {
    pub const constructor_id: u32 = 0x8c5adfd9;
    requested: bool = false,
    via_chatlist: bool = false,
    user_id: i64,
    date: i32,
    about: ?[]const u8 = null,
    approved_by: ?i64 = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.requested) flags |= 1 << 0;
        if (self.via_chatlist) flags |= 1 << 3;
        if (self.about != null) flags |= 1 << 2;
        if (self.approved_by != null) flags |= 1 << 1;
        try w.writeUInt(flags);
        try w.writeLong(self.user_id);
        try w.writeInt(self.date);
        if (self.about) |v| {
            try w.writeString(v);
        }
        if (self.approved_by) |v| {
            try w.writeLong(v);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.requested = (flags >> 0) & 1 != 0;
        self.via_chatlist = (flags >> 3) & 1 != 0;
        self.user_id = try r.readLong();
        self.date = try r.readInt();
        if ((flags >> 2) & 1 != 0) {
            self.about = try r.readString();
        } else {
            self.about = null;
        }
        if ((flags >> 1) & 1 != 0) {
            self.approved_by = try r.readLong();
        } else {
            self.approved_by = null;
        }
        _ = allocator;
        return self;
    }
};

pub const chatAdminWithInvites = struct {
    pub const constructor_id: u32 = 0xf2ecef23;
    admin_id: i64,
    invites_count: i32,
    revoked_invites_count: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeLong(self.admin_id);
        try w.writeInt(self.invites_count);
        try w.writeInt(self.revoked_invites_count);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.admin_id = try r.readLong();
        self.invites_count = try r.readInt();
        self.revoked_invites_count = try r.readInt();
        _ = allocator;
        return self;
    }
};

pub const forumTopicDeleted = struct {
    pub const constructor_id: u32 = 0x023f109b;
    id: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeInt(self.id);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.id = try r.readInt();
        _ = allocator;
        return self;
    }
};

pub const forumTopic = struct {
    pub const constructor_id: u32 = 0xfcdad815;
    my: bool = false,
    closed: bool = false,
    pinned: bool = false,
    short: bool = false,
    hidden: bool = false,
    title_missing: bool = false,
    id: i32,
    date: i32,
    peer: api.Peer,
    title: []const u8,
    icon_color: i32,
    icon_emoji_id: ?i64 = null,
    top_message: i32,
    read_inbox_max_id: i32,
    read_outbox_max_id: i32,
    unread_count: i32,
    unread_mentions_count: i32,
    unread_reactions_count: i32,
    unread_poll_votes_count: i32,
    from_id: api.Peer,
    notify_settings: api.PeerNotifySettings,
    draft: ?api.DraftMessage = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.my) flags |= 1 << 1;
        if (self.closed) flags |= 1 << 2;
        if (self.pinned) flags |= 1 << 3;
        if (self.short) flags |= 1 << 5;
        if (self.hidden) flags |= 1 << 6;
        if (self.title_missing) flags |= 1 << 7;
        if (self.icon_emoji_id != null) flags |= 1 << 0;
        if (self.draft != null) flags |= 1 << 4;
        try w.writeUInt(flags);
        try w.writeInt(self.id);
        try w.writeInt(self.date);
        try self.peer.serialize(w);
        try w.writeString(self.title);
        try w.writeInt(self.icon_color);
        if (self.icon_emoji_id) |v| {
            try w.writeLong(v);
        }
        try w.writeInt(self.top_message);
        try w.writeInt(self.read_inbox_max_id);
        try w.writeInt(self.read_outbox_max_id);
        try w.writeInt(self.unread_count);
        try w.writeInt(self.unread_mentions_count);
        try w.writeInt(self.unread_reactions_count);
        try w.writeInt(self.unread_poll_votes_count);
        try self.from_id.serialize(w);
        try self.notify_settings.serialize(w);
        if (self.draft) |v| {
            try v.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.my = (flags >> 1) & 1 != 0;
        self.closed = (flags >> 2) & 1 != 0;
        self.pinned = (flags >> 3) & 1 != 0;
        self.short = (flags >> 5) & 1 != 0;
        self.hidden = (flags >> 6) & 1 != 0;
        self.title_missing = (flags >> 7) & 1 != 0;
        self.id = try r.readInt();
        self.date = try r.readInt();
        self.peer = try api.Peer.deserialize(allocator, r);
        self.title = try r.readString();
        self.icon_color = try r.readInt();
        if ((flags >> 0) & 1 != 0) {
            self.icon_emoji_id = try r.readLong();
        } else {
            self.icon_emoji_id = null;
        }
        self.top_message = try r.readInt();
        self.read_inbox_max_id = try r.readInt();
        self.read_outbox_max_id = try r.readInt();
        self.unread_count = try r.readInt();
        self.unread_mentions_count = try r.readInt();
        self.unread_reactions_count = try r.readInt();
        self.unread_poll_votes_count = try r.readInt();
        self.from_id = try api.Peer.deserialize(allocator, r);
        self.notify_settings = try api.PeerNotifySettings.deserialize(allocator, r);
        if ((flags >> 4) & 1 != 0) {
            self.draft = try api.DraftMessage.deserialize(allocator, r);
        } else {
            self.draft = null;
        }
        return self;
    }
};

pub const defaultHistoryTTL = struct {
    pub const constructor_id: u32 = 0x43b46b20;
    period: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeInt(self.period);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.period = try r.readInt();
        _ = allocator;
        return self;
    }
};

pub const inputChatlistDialogFilter = struct {
    pub const constructor_id: u32 = 0xf3e0da33;
    filter_id: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeInt(self.filter_id);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        self.filter_id = try r.readInt();
        _ = allocator;
        return self;
    }
};

pub const exportedChatlistInvite = struct {
    pub const constructor_id: u32 = 0x0c5181ac;
    title: []const u8,
    url: []const u8,
    peers: []api.Peer,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        const flags: u32 = 0;
        try w.writeUInt(flags);
        try w.writeString(self.title);
        try w.writeString(self.url);
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.peers.len);
        for (self.peers) |elem_987| {
            try elem_987.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        _ = flags;
        self.title = try r.readString();
        self.url = try r.readString();
        if ((try r.readUInt()) != vector_constructor_id) return error.InvalidValue;
        const len_988 = try r.readVectorLength();
        const vec_989 = allocator.alloc(api.Peer, len_988) catch return error.OutOfMemory;
        for (vec_989) |*elem_990| {
            elem_990.* = try api.Peer.deserialize(allocator, r);
        }
        self.peers = vec_989;
        return self;
    }
};

pub const savedDialog = struct {
    pub const constructor_id: u32 = 0xbd87cb6c;
    pinned: bool = false,
    peer: api.Peer,
    top_message: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.pinned) flags |= 1 << 2;
        try w.writeUInt(flags);
        try self.peer.serialize(w);
        try w.writeInt(self.top_message);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.pinned = (flags >> 2) & 1 != 0;
        self.peer = try api.Peer.deserialize(allocator, r);
        self.top_message = try r.readInt();
        return self;
    }
};

pub const monoForumDialog = struct {
    pub const constructor_id: u32 = 0x64407ea7;
    unread_mark: bool = false,
    nopaid_messages_exception: bool = false,
    peer: api.Peer,
    top_message: i32,
    read_inbox_max_id: i32,
    read_outbox_max_id: i32,
    unread_count: i32,
    unread_reactions_count: i32,
    draft: ?api.DraftMessage = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.unread_mark) flags |= 1 << 3;
        if (self.nopaid_messages_exception) flags |= 1 << 4;
        if (self.draft != null) flags |= 1 << 1;
        try w.writeUInt(flags);
        try self.peer.serialize(w);
        try w.writeInt(self.top_message);
        try w.writeInt(self.read_inbox_max_id);
        try w.writeInt(self.read_outbox_max_id);
        try w.writeInt(self.unread_count);
        try w.writeInt(self.unread_reactions_count);
        if (self.draft) |v| {
            try v.serialize(w);
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.unread_mark = (flags >> 3) & 1 != 0;
        self.nopaid_messages_exception = (flags >> 4) & 1 != 0;
        self.peer = try api.Peer.deserialize(allocator, r);
        self.top_message = try r.readInt();
        self.read_inbox_max_id = try r.readInt();
        self.read_outbox_max_id = try r.readInt();
        self.unread_count = try r.readInt();
        self.unread_reactions_count = try r.readInt();
        if ((flags >> 1) & 1 != 0) {
            self.draft = try api.DraftMessage.deserialize(allocator, r);
        } else {
            self.draft = null;
        }
        return self;
    }
};

pub const missingInvitee = struct {
    pub const constructor_id: u32 = 0x628c9224;
    premium_would_allow_invite: bool = false,
    premium_required_for_pm: bool = false,
    user_id: i64,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        var flags: u32 = 0;
        if (self.premium_would_allow_invite) flags |= 1 << 0;
        if (self.premium_required_for_pm) flags |= 1 << 1;
        try w.writeUInt(flags);
        try w.writeLong(self.user_id);
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        var self: @This() = undefined;
        const flags = try r.readUInt();
        self.premium_would_allow_invite = (flags >> 0) & 1 != 0;
        self.premium_required_for_pm = (flags >> 1) & 1 != 0;
        self.user_id = try r.readLong();
        _ = allocator;
        return self;
    }
};

pub const Chat = union(enum) {
    chatEmpty: api.chatEmpty,
    chat: api.chat,
    chatForbidden: api.chatForbidden,
    channel: api.channel,
    channelForbidden: api.channelForbidden,
    communityForbidden: api.communityForbidden,
    community: api.community,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.chatEmpty.constructor_id => return .{ .chatEmpty = try api.chatEmpty.deserialize(allocator, r) },
            api.chat.constructor_id => return .{ .chat = try api.chat.deserialize(allocator, r) },
            api.chatForbidden.constructor_id => return .{ .chatForbidden = try api.chatForbidden.deserialize(allocator, r) },
            api.channel.constructor_id => return .{ .channel = try api.channel.deserialize(allocator, r) },
            api.channelForbidden.constructor_id => return .{ .channelForbidden = try api.channelForbidden.deserialize(allocator, r) },
            api.communityForbidden.constructor_id => return .{ .communityForbidden = try api.communityForbidden.deserialize(allocator, r) },
            api.community.constructor_id => return .{ .community = try api.community.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const ChatFull = union(enum) {
    chatFull: api.chatFull,
    channelFull: api.channelFull,
    communityFull: api.communityFull,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.chatFull.constructor_id => return .{ .chatFull = try api.chatFull.deserialize(allocator, r) },
            api.channelFull.constructor_id => return .{ .channelFull = try api.channelFull.deserialize(allocator, r) },
            api.communityFull.constructor_id => return .{ .communityFull = try api.communityFull.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const ChatParticipant = union(enum) {
    chatParticipant: api.chatParticipant,
    chatParticipantCreator: api.chatParticipantCreator,
    chatParticipantAdmin: api.chatParticipantAdmin,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.chatParticipant.constructor_id => return .{ .chatParticipant = try api.chatParticipant.deserialize(allocator, r) },
            api.chatParticipantCreator.constructor_id => return .{ .chatParticipantCreator = try api.chatParticipantCreator.deserialize(allocator, r) },
            api.chatParticipantAdmin.constructor_id => return .{ .chatParticipantAdmin = try api.chatParticipantAdmin.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const ChatParticipants = union(enum) {
    chatParticipantsForbidden: api.chatParticipantsForbidden,
    chatParticipants: api.chatParticipants,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.chatParticipantsForbidden.constructor_id => return .{ .chatParticipantsForbidden = try api.chatParticipantsForbidden.deserialize(allocator, r) },
            api.chatParticipants.constructor_id => return .{ .chatParticipants = try api.chatParticipants.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const ChatPhoto = union(enum) {
    chatPhotoEmpty: api.chatPhotoEmpty,
    chatPhoto: api.chatPhoto,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.chatPhotoEmpty.constructor_id => return .{ .chatPhotoEmpty = try api.chatPhotoEmpty.deserialize(allocator, r) },
            api.chatPhoto.constructor_id => return .{ .chatPhoto = try api.chatPhoto.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const Dialog = union(enum) {
    dialog: api.dialog,
    dialogFolder: api.dialogFolder,
    dialogCommunity: api.dialogCommunity,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.dialog.constructor_id => return .{ .dialog = try api.dialog.deserialize(allocator, r) },
            api.dialogFolder.constructor_id => return .{ .dialogFolder = try api.dialogFolder.deserialize(allocator, r) },
            api.dialogCommunity.constructor_id => return .{ .dialogCommunity = try api.dialogCommunity.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const EncryptedChat = union(enum) {
    encryptedChatEmpty: api.encryptedChatEmpty,
    encryptedChatWaiting: api.encryptedChatWaiting,
    encryptedChatRequested: api.encryptedChatRequested,
    encryptedChat: api.encryptedChat,
    encryptedChatDiscarded: api.encryptedChatDiscarded,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.encryptedChatEmpty.constructor_id => return .{ .encryptedChatEmpty = try api.encryptedChatEmpty.deserialize(allocator, r) },
            api.encryptedChatWaiting.constructor_id => return .{ .encryptedChatWaiting = try api.encryptedChatWaiting.deserialize(allocator, r) },
            api.encryptedChatRequested.constructor_id => return .{ .encryptedChatRequested = try api.encryptedChatRequested.deserialize(allocator, r) },
            api.encryptedChat.constructor_id => return .{ .encryptedChat = try api.encryptedChat.deserialize(allocator, r) },
            api.encryptedChatDiscarded.constructor_id => return .{ .encryptedChatDiscarded = try api.encryptedChatDiscarded.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const InputEncryptedChat = union(enum) {
    inputEncryptedChat: api.inputEncryptedChat,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.inputEncryptedChat.constructor_id => return .{ .inputEncryptedChat = try api.inputEncryptedChat.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const EncryptedMessage = union(enum) {
    encryptedMessage: api.encryptedMessage,
    encryptedMessageService: api.encryptedMessageService,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.encryptedMessage.constructor_id => return .{ .encryptedMessage = try api.encryptedMessage.deserialize(allocator, r) },
            api.encryptedMessageService.constructor_id => return .{ .encryptedMessageService = try api.encryptedMessageService.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const ExportedChatInvite = union(enum) {
    chatInviteExported: api.chatInviteExported,
    chatInvitePublicJoinRequests: api.chatInvitePublicJoinRequests,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.chatInviteExported.constructor_id => return .{ .chatInviteExported = try api.chatInviteExported.deserialize(allocator, r) },
            api.chatInvitePublicJoinRequests.constructor_id => return .{ .chatInvitePublicJoinRequests = try api.chatInvitePublicJoinRequests.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const ChatInvite = union(enum) {
    chatInviteAlready: api.chatInviteAlready,
    chatInvite: api.chatInvite,
    chatInvitePeek: api.chatInvitePeek,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.chatInviteAlready.constructor_id => return .{ .chatInviteAlready = try api.chatInviteAlready.deserialize(allocator, r) },
            api.chatInvite.constructor_id => return .{ .chatInvite = try api.chatInvite.deserialize(allocator, r) },
            api.chatInvitePeek.constructor_id => return .{ .chatInvitePeek = try api.chatInvitePeek.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const DraftMessage = union(enum) {
    draftMessageEmpty: api.draftMessageEmpty,
    draftMessage: api.draftMessage,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.draftMessageEmpty.constructor_id => return .{ .draftMessageEmpty = try api.draftMessageEmpty.deserialize(allocator, r) },
            api.draftMessage.constructor_id => return .{ .draftMessage = try api.draftMessage.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const DialogPeer = union(enum) {
    dialogPeer: api.dialogPeer,
    dialogPeerFolder: api.dialogPeerFolder,
    dialogPeerCommunity: api.dialogPeerCommunity,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.dialogPeer.constructor_id => return .{ .dialogPeer = try api.dialogPeer.deserialize(allocator, r) },
            api.dialogPeerFolder.constructor_id => return .{ .dialogPeerFolder = try api.dialogPeerFolder.deserialize(allocator, r) },
            api.dialogPeerCommunity.constructor_id => return .{ .dialogPeerCommunity = try api.dialogPeerCommunity.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const ChatOnlines = union(enum) {
    chatOnlines: api.chatOnlines,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.chatOnlines.constructor_id => return .{ .chatOnlines = try api.chatOnlines.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const ChatAdminRights = union(enum) {
    chatAdminRights: api.chatAdminRights,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.chatAdminRights.constructor_id => return .{ .chatAdminRights = try api.chatAdminRights.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const ChatBannedRights = union(enum) {
    chatBannedRights: api.chatBannedRights,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.chatBannedRights.constructor_id => return .{ .chatBannedRights = try api.chatBannedRights.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const Folder = union(enum) {
    folder: api.folder,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.folder.constructor_id => return .{ .folder = try api.folder.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const InputFolderPeer = union(enum) {
    inputFolderPeer: api.inputFolderPeer,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.inputFolderPeer.constructor_id => return .{ .inputFolderPeer = try api.inputFolderPeer.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const FolderPeer = union(enum) {
    folderPeer: api.folderPeer,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.folderPeer.constructor_id => return .{ .folderPeer = try api.folderPeer.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const DialogFilter = union(enum) {
    dialogFilter: api.dialogFilter,
    dialogFilterDefault: api.dialogFilterDefault,
    dialogFilterChatlist: api.dialogFilterChatlist,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.dialogFilter.constructor_id => return .{ .dialogFilter = try api.dialogFilter.deserialize(allocator, r) },
            api.dialogFilterDefault.constructor_id => return .{ .dialogFilterDefault = try api.dialogFilterDefault.deserialize(allocator, r) },
            api.dialogFilterChatlist.constructor_id => return .{ .dialogFilterChatlist = try api.dialogFilterChatlist.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const DialogFilterSuggested = union(enum) {
    dialogFilterSuggested: api.dialogFilterSuggested,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.dialogFilterSuggested.constructor_id => return .{ .dialogFilterSuggested = try api.dialogFilterSuggested.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const ChatInviteImporter = union(enum) {
    chatInviteImporter: api.chatInviteImporter,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.chatInviteImporter.constructor_id => return .{ .chatInviteImporter = try api.chatInviteImporter.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const ChatAdminWithInvites = union(enum) {
    chatAdminWithInvites: api.chatAdminWithInvites,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.chatAdminWithInvites.constructor_id => return .{ .chatAdminWithInvites = try api.chatAdminWithInvites.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const ForumTopic = union(enum) {
    forumTopicDeleted: api.forumTopicDeleted,
    forumTopic: api.forumTopic,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.forumTopicDeleted.constructor_id => return .{ .forumTopicDeleted = try api.forumTopicDeleted.deserialize(allocator, r) },
            api.forumTopic.constructor_id => return .{ .forumTopic = try api.forumTopic.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const DefaultHistoryTTL = union(enum) {
    defaultHistoryTTL: api.defaultHistoryTTL,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.defaultHistoryTTL.constructor_id => return .{ .defaultHistoryTTL = try api.defaultHistoryTTL.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const InputChatlist = union(enum) {
    inputChatlistDialogFilter: api.inputChatlistDialogFilter,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.inputChatlistDialogFilter.constructor_id => return .{ .inputChatlistDialogFilter = try api.inputChatlistDialogFilter.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const ExportedChatlistInvite = union(enum) {
    exportedChatlistInvite: api.exportedChatlistInvite,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.exportedChatlistInvite.constructor_id => return .{ .exportedChatlistInvite = try api.exportedChatlistInvite.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const SavedDialog = union(enum) {
    savedDialog: api.savedDialog,
    monoForumDialog: api.monoForumDialog,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.savedDialog.constructor_id => return .{ .savedDialog = try api.savedDialog.deserialize(allocator, r) },
            api.monoForumDialog.constructor_id => return .{ .monoForumDialog = try api.monoForumDialog.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};

pub const MissingInvitee = union(enum) {
    missingInvitee: api.missingInvitee,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        switch (self.*) {
            inline else => |v| {
                try w.writeConstructorId(@TypeOf(v).constructor_id);
                try v.serialize(w);
            },
        }
    }

    pub fn deserialize(allocator: std.mem.Allocator, r: *Reader) TlError!@This() {
        const id = try r.readConstructorId();
        switch (id) {
            api.missingInvitee.constructor_id => return .{ .missingInvitee = try api.missingInvitee.deserialize(allocator, r) },
            else => return error.InvalidValue,
        }
    }
};


//! GENERATED by td TL code generator from "api.tl".
//! Do not edit by hand; regenerate with td-gen.

const std = @import("std");
const td = @import("../../root.zig");
const Writer = td.tl.Writer;
const Reader = td.tl.Reader;
const TlError = td.TlError;
const vector_constructor_id = td.tl.vector_constructor_id;

const api = @import("../mod.zig");

pub const getBoostsList = struct {
    pub const constructor_id: u32 = 0x60f67660;
    pub const Result = api.premium.BoostsList;
    gifts: bool = false,
    peer: *api.InputPeer,
    offset: []const u8,
    limit: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeConstructorId(constructor_id);
        var flags: u32 = 0;
        if (self.gifts) flags |= 1 << 0;
        try w.writeUInt(flags);
        try self.peer.serialize(w);
        try w.writeString(self.offset);
        try w.writeInt(self.limit);
    }
};

pub const getMyBoosts = struct {
    pub const constructor_id: u32 = 0x0be77b4a;
    pub const Result = api.premium.MyBoosts;

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeConstructorId(constructor_id);
        _ = self;
    }
};

pub const applyBoost = struct {
    pub const constructor_id: u32 = 0x6b7da746;
    pub const Result = api.premium.MyBoosts;
    slots: ?[]i32 = null,
    peer: *api.InputPeer,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeConstructorId(constructor_id);
        var flags: u32 = 0;
        if (self.slots != null) flags |= 1 << 0;
        try w.writeUInt(flags);
        if (self.slots) |v| {
            try w.writeUInt(vector_constructor_id);
            try w.writeVectorLength(v.len);
            for (v) |elem_2745| {
                try w.writeInt(elem_2745);
            }
        }
        try self.peer.serialize(w);
    }
};

pub const getBoostsStatus = struct {
    pub const constructor_id: u32 = 0x042f1f61;
    pub const Result = api.premium.BoostsStatus;
    peer: *api.InputPeer,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeConstructorId(constructor_id);
        try self.peer.serialize(w);
    }
};

pub const getUserBoosts = struct {
    pub const constructor_id: u32 = 0x39854d1f;
    pub const Result = api.premium.BoostsList;
    peer: *api.InputPeer,
    user_id: api.InputUser,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeConstructorId(constructor_id);
        try self.peer.serialize(w);
        try self.user_id.serialize(w);
    }
};


//! GENERATED by td TL code generator from "api.tl".
//! Do not edit by hand; regenerate with td-gen.

const std = @import("std");
const td = @import("../../root.zig");
const Writer = td.tl.Writer;
const Reader = td.tl.Reader;
const TlError = td.TlError;
const vector_constructor_id = td.tl.vector_constructor_id;

const api = @import("../mod.zig");

pub const getState = struct {
    pub const constructor_id: u32 = 0xedd4882a;
    pub const Result = api.updates.State;

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeConstructorId(constructor_id);
        _ = self;
    }
};

pub const getDifference = struct {
    pub const constructor_id: u32 = 0x19c2f763;
    pub const Result = api.updates.Difference;
    pts: i32,
    pts_limit: ?i32 = null,
    pts_total_limit: ?i32 = null,
    date: i32,
    qts: i32,
    qts_limit: ?i32 = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeConstructorId(constructor_id);
        var flags: u32 = 0;
        if (self.pts_limit != null) flags |= 1 << 1;
        if (self.pts_total_limit != null) flags |= 1 << 0;
        if (self.qts_limit != null) flags |= 1 << 2;
        try w.writeUInt(flags);
        try w.writeInt(self.pts);
        if (self.pts_limit) |v| {
            try w.writeInt(v);
        }
        if (self.pts_total_limit) |v| {
            try w.writeInt(v);
        }
        try w.writeInt(self.date);
        try w.writeInt(self.qts);
        if (self.qts_limit) |v| {
            try w.writeInt(v);
        }
    }
};

pub const getChannelDifference = struct {
    pub const constructor_id: u32 = 0x03173d78;
    pub const Result = api.updates.ChannelDifference;
    force: bool = false,
    channel: api.InputChannel,
    filter: api.ChannelMessagesFilter,
    pts: i32,
    limit: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeConstructorId(constructor_id);
        var flags: u32 = 0;
        if (self.force) flags |= 1 << 0;
        try w.writeUInt(flags);
        try self.channel.serialize(w);
        try self.filter.serialize(w);
        try w.writeInt(self.pts);
        try w.writeInt(self.limit);
    }
};


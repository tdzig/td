//! GENERATED by td TL code generator from "api.tl".
//! Do not edit by hand; regenerate with td-gen.

const std = @import("std");
const td = @import("../../root.zig");
const Writer = td.tl.Writer;
const Reader = td.tl.Reader;
const TlError = td.TlError;
const vector_constructor_id = td.tl.vector_constructor_id;

const api = @import("../mod.zig");

pub const updateProfilePhoto = struct {
    pub const constructor_id: u32 = 0x09e82039;
    pub const Result = api.photos.Photo_;
    fallback: bool = false,
    bot: ?api.InputUser = null,
    id: api.InputPhoto,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeConstructorId(constructor_id);
        var flags: u32 = 0;
        if (self.fallback) flags |= 1 << 0;
        if (self.bot != null) flags |= 1 << 1;
        try w.writeUInt(flags);
        if (self.bot) |v| {
            try v.serialize(w);
        }
        try self.id.serialize(w);
    }
};

pub const uploadProfilePhoto = struct {
    pub const constructor_id: u32 = 0x0388a3b5;
    pub const Result = api.photos.Photo_;
    fallback: bool = false,
    bot: ?api.InputUser = null,
    file: ?api.InputFile = null,
    video: ?api.InputFile = null,
    video_start_ts: ?f64 = null,
    video_emoji_markup: ?api.VideoSize = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeConstructorId(constructor_id);
        var flags: u32 = 0;
        if (self.fallback) flags |= 1 << 3;
        if (self.bot != null) flags |= 1 << 5;
        if (self.file != null) flags |= 1 << 0;
        if (self.video != null) flags |= 1 << 1;
        if (self.video_start_ts != null) flags |= 1 << 2;
        if (self.video_emoji_markup != null) flags |= 1 << 4;
        try w.writeUInt(flags);
        if (self.bot) |v| {
            try v.serialize(w);
        }
        if (self.file) |v| {
            try v.serialize(w);
        }
        if (self.video) |v| {
            try v.serialize(w);
        }
        if (self.video_start_ts) |v| {
            try w.writeDouble(v);
        }
        if (self.video_emoji_markup) |v| {
            try v.serialize(w);
        }
    }
};

pub const deletePhotos = struct {
    pub const constructor_id: u32 = 0x87cf7f2f;
    pub const Result = []i64;
    id: []api.InputPhoto,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeConstructorId(constructor_id);
        try w.writeUInt(vector_constructor_id);
        try w.writeVectorLength(self.id.len);
        for (self.id) |elem_2683| {
            try elem_2683.serialize(w);
        }
    }
};

pub const getUserPhotos = struct {
    pub const constructor_id: u32 = 0x91cd32a8;
    pub const Result = api.photos.Photos;
    user_id: api.InputUser,
    offset: i32,
    max_id: i64,
    limit: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeConstructorId(constructor_id);
        try self.user_id.serialize(w);
        try w.writeInt(self.offset);
        try w.writeLong(self.max_id);
        try w.writeInt(self.limit);
    }
};

pub const uploadContactProfilePhoto = struct {
    pub const constructor_id: u32 = 0xe14c4a71;
    pub const Result = api.photos.Photo_;
    suggest: bool = false,
    save: bool = false,
    user_id: api.InputUser,
    file: ?api.InputFile = null,
    video: ?api.InputFile = null,
    video_start_ts: ?f64 = null,
    video_emoji_markup: ?api.VideoSize = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeConstructorId(constructor_id);
        var flags: u32 = 0;
        if (self.suggest) flags |= 1 << 3;
        if (self.save) flags |= 1 << 4;
        if (self.file != null) flags |= 1 << 0;
        if (self.video != null) flags |= 1 << 1;
        if (self.video_start_ts != null) flags |= 1 << 2;
        if (self.video_emoji_markup != null) flags |= 1 << 5;
        try w.writeUInt(flags);
        try self.user_id.serialize(w);
        if (self.file) |v| {
            try v.serialize(w);
        }
        if (self.video) |v| {
            try v.serialize(w);
        }
        if (self.video_start_ts) |v| {
            try w.writeDouble(v);
        }
        if (self.video_emoji_markup) |v| {
            try v.serialize(w);
        }
    }
};


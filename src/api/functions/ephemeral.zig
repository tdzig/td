//! GENERATED by td TL code generator from "api.tl".
//! Do not edit by hand; regenerate with td-gen.

const std = @import("std");
const td = @import("../../root.zig");
const Writer = td.tl.Writer;
const Reader = td.tl.Reader;
const TlError = td.TlError;
const vector_constructor_id = td.tl.vector_constructor_id;

const api = @import("../mod.zig");

pub const sendMessage = struct {
    pub const constructor_id: u32 = 0xba8d5f35;
    pub const Result = api.Updates;
    invert_media: bool = false,
    welcome: bool = false,
    anchor: bool = false,
    noforwards: bool = false,
    peer: ?*api.InputPeer = null,
    receiver_id: api.InputUser,
    query_id: ?i64 = null,
    message: []const u8,
    entities: ?[]api.MessageEntity = null,
    media: ?*api.InputMedia = null,
    reply_markup: ?api.ReplyMarkup = null,
    rich_message: ?api.InputRichMessage = null,
    random_id: i64,
    reply_to: ?api.InputReplyTo = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeConstructorId(constructor_id);
        var flags: u32 = 0;
        if (self.invert_media) flags |= 1 << 6;
        if (self.welcome) flags |= 1 << 7;
        if (self.anchor) flags |= 1 << 9;
        if (self.noforwards) flags |= 1 << 10;
        if (self.peer != null) flags |= 1 << 8;
        if (self.query_id != null) flags |= 1 << 0;
        if (self.entities != null) flags |= 1 << 1;
        if (self.media != null) flags |= 1 << 2;
        if (self.reply_markup != null) flags |= 1 << 3;
        if (self.rich_message != null) flags |= 1 << 4;
        if (self.reply_to != null) flags |= 1 << 5;
        try w.writeUInt(flags);
        if (self.peer) |v| {
            try v.serialize(w);
        }
        try self.receiver_id.serialize(w);
        if (self.query_id) |v| {
            try w.writeLong(v);
        }
        try w.writeString(self.message);
        if (self.entities) |v| {
            try w.writeUInt(vector_constructor_id);
            try w.writeVectorLength(v.len);
            for (v) |elem_2746| {
                try elem_2746.serialize(w);
            }
        }
        if (self.media) |v| {
            try v.serialize(w);
        }
        if (self.reply_markup) |v| {
            try v.serialize(w);
        }
        if (self.rich_message) |v| {
            try v.serialize(w);
        }
        try w.writeLong(self.random_id);
        if (self.reply_to) |v| {
            try v.serialize(w);
        }
    }
};

pub const deleteMessage = struct {
    pub const constructor_id: u32 = 0x92f6e797;
    pub const Result = bool;
    peer: ?*api.InputPeer = null,
    receiver_id: api.InputUser,
    id: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeConstructorId(constructor_id);
        var flags: u32 = 0;
        if (self.peer != null) flags |= 1 << 0;
        try w.writeUInt(flags);
        if (self.peer) |v| {
            try v.serialize(w);
        }
        try self.receiver_id.serialize(w);
        try w.writeInt(self.id);
    }
};

pub const reportMessage = struct {
    pub const constructor_id: u32 = 0x8704f2bf;
    pub const Result = api.ReportResult;
    peer: *api.InputPeer,
    id: i32,
    option: []const u8,
    message: []const u8,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeConstructorId(constructor_id);
        try self.peer.serialize(w);
        try w.writeInt(self.id);
        try w.writeBytes(self.option);
        try w.writeString(self.message);
    }
};

pub const getCallbackAnswer = struct {
    pub const constructor_id: u32 = 0x3fa464c8;
    pub const Result = api.messages.BotCallbackAnswer;
    peer: *api.InputPeer,
    id: i32,
    data: ?[]const u8 = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeConstructorId(constructor_id);
        var flags: u32 = 0;
        if (self.data != null) flags |= 1 << 1;
        try w.writeUInt(flags);
        try self.peer.serialize(w);
        try w.writeInt(self.id);
        if (self.data) |v| {
            try w.writeBytes(v);
        }
    }
};

pub const editMessage = struct {
    pub const constructor_id: u32 = 0xcf9c725b;
    pub const Result = api.Updates;
    invert_media: bool = false,
    welcome: bool = false,
    peer: ?*api.InputPeer = null,
    receiver_id: api.InputUser,
    id: i32,
    message: ?[]const u8 = null,
    media: ?*api.InputMedia = null,
    entities: ?[]api.MessageEntity = null,
    reply_markup: ?api.ReplyMarkup = null,
    rich_message: ?api.InputRichMessage = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeConstructorId(constructor_id);
        var flags: u32 = 0;
        if (self.invert_media) flags |= 1 << 5;
        if (self.welcome) flags |= 1 << 6;
        if (self.peer != null) flags |= 1 << 7;
        if (self.message != null) flags |= 1 << 0;
        if (self.media != null) flags |= 1 << 3;
        if (self.entities != null) flags |= 1 << 1;
        if (self.reply_markup != null) flags |= 1 << 2;
        if (self.rich_message != null) flags |= 1 << 4;
        try w.writeUInt(flags);
        if (self.peer) |v| {
            try v.serialize(w);
        }
        try self.receiver_id.serialize(w);
        try w.writeInt(self.id);
        if (self.message) |v| {
            try w.writeString(v);
        }
        if (self.media) |v| {
            try v.serialize(w);
        }
        if (self.entities) |v| {
            try w.writeUInt(vector_constructor_id);
            try w.writeVectorLength(v.len);
            for (v) |elem_2747| {
                try elem_2747.serialize(w);
            }
        }
        if (self.reply_markup) |v| {
            try v.serialize(w);
        }
        if (self.rich_message) |v| {
            try v.serialize(w);
        }
    }
};

pub const deleteWelcomeMessage = struct {
    pub const constructor_id: u32 = 0xe882a9e1;
    pub const Result = bool;
    peer: *api.InputPeer,
    id: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeConstructorId(constructor_id);
        try self.peer.serialize(w);
        try w.writeInt(self.id);
    }
};

pub const deleteAllWelcomeMessages = struct {
    pub const constructor_id: u32 = 0x734f9721;
    pub const Result = bool;
    peer: *api.InputPeer,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeConstructorId(constructor_id);
        try self.peer.serialize(w);
    }
};

pub const getWelcomeMessages = struct {
    pub const constructor_id: u32 = 0xdb9ac18d;
    pub const Result = api.ephemeral.WelcomeMessages;
    peer: *api.InputPeer,
    hash: i64,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeConstructorId(constructor_id);
        try self.peer.serialize(w);
        try w.writeLong(self.hash);
    }
};


//! GENERATED by td TL code generator from "api.tl".
//! Do not edit by hand; regenerate with td-gen.

const std = @import("std");
const td = @import("../../root.zig");
const Writer = td.tl.Writer;
const Reader = td.tl.Reader;
const TlError = td.TlError;
const vector_constructor_id = td.tl.vector_constructor_id;

const api = @import("../mod.zig");

pub const getBroadcastStats = struct {
    pub const constructor_id: u32 = 0xab42441a;
    pub const Result = api.stats.BroadcastStats;
    dark: bool = false,
    channel: api.InputChannel,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeConstructorId(constructor_id);
        var flags: u32 = 0;
        if (self.dark) flags |= 1 << 0;
        try w.writeUInt(flags);
        try self.channel.serialize(w);
    }
};

pub const loadAsyncGraph = struct {
    pub const constructor_id: u32 = 0x621d5fa0;
    pub const Result = api.StatsGraph;
    token: []const u8,
    x: ?i64 = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeConstructorId(constructor_id);
        var flags: u32 = 0;
        if (self.x != null) flags |= 1 << 0;
        try w.writeUInt(flags);
        try w.writeString(self.token);
        if (self.x) |v| {
            try w.writeLong(v);
        }
    }
};

pub const getMegagroupStats = struct {
    pub const constructor_id: u32 = 0xdcdf8607;
    pub const Result = api.stats.MegagroupStats;
    dark: bool = false,
    channel: api.InputChannel,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeConstructorId(constructor_id);
        var flags: u32 = 0;
        if (self.dark) flags |= 1 << 0;
        try w.writeUInt(flags);
        try self.channel.serialize(w);
    }
};

pub const getMessagePublicForwards = struct {
    pub const constructor_id: u32 = 0x5f150144;
    pub const Result = api.stats.PublicForwards;
    channel: api.InputChannel,
    msg_id: i32,
    offset: []const u8,
    limit: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeConstructorId(constructor_id);
        try self.channel.serialize(w);
        try w.writeInt(self.msg_id);
        try w.writeString(self.offset);
        try w.writeInt(self.limit);
    }
};

pub const getMessageStats = struct {
    pub const constructor_id: u32 = 0xb6e0a3f5;
    pub const Result = api.stats.MessageStats;
    dark: bool = false,
    channel: api.InputChannel,
    msg_id: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeConstructorId(constructor_id);
        var flags: u32 = 0;
        if (self.dark) flags |= 1 << 0;
        try w.writeUInt(flags);
        try self.channel.serialize(w);
        try w.writeInt(self.msg_id);
    }
};

pub const getStoryStats = struct {
    pub const constructor_id: u32 = 0x374fef40;
    pub const Result = api.stats.StoryStats;
    dark: bool = false,
    peer: *api.InputPeer,
    id: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeConstructorId(constructor_id);
        var flags: u32 = 0;
        if (self.dark) flags |= 1 << 0;
        try w.writeUInt(flags);
        try self.peer.serialize(w);
        try w.writeInt(self.id);
    }
};

pub const getStoryPublicForwards = struct {
    pub const constructor_id: u32 = 0xa6437ef6;
    pub const Result = api.stats.PublicForwards;
    peer: *api.InputPeer,
    id: i32,
    offset: []const u8,
    limit: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeConstructorId(constructor_id);
        try self.peer.serialize(w);
        try w.writeInt(self.id);
        try w.writeString(self.offset);
        try w.writeInt(self.limit);
    }
};

pub const getPollStats = struct {
    pub const constructor_id: u32 = 0xc27dfa68;
    pub const Result = api.stats.PollStats;
    dark: bool = false,
    peer: *api.InputPeer,
    msg_id: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeConstructorId(constructor_id);
        var flags: u32 = 0;
        if (self.dark) flags |= 1 << 0;
        try w.writeUInt(flags);
        try self.peer.serialize(w);
        try w.writeInt(self.msg_id);
    }
};


//! GENERATED by td TL code generator from "api.tl".
//! Do not edit by hand; regenerate with td-gen.

const std = @import("std");
const td = @import("../../root.zig");
const Writer = td.tl.Writer;
const Reader = td.tl.Reader;
const TlError = td.TlError;
const vector_constructor_id = td.tl.vector_constructor_id;

const api = @import("../mod.zig");

pub const createTone = struct {
    pub const constructor_id: u32 = 0x4aa83913;
    pub const Result = api.AiComposeTone;
    display_author: bool = false,
    emoji_id: i64,
    title: []const u8,
    prompt: []const u8,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeConstructorId(constructor_id);
        var flags: u32 = 0;
        if (self.display_author) flags |= 1 << 0;
        try w.writeUInt(flags);
        try w.writeLong(self.emoji_id);
        try w.writeString(self.title);
        try w.writeString(self.prompt);
    }
};

pub const updateTone = struct {
    pub const constructor_id: u32 = 0x903bcf59;
    pub const Result = api.AiComposeTone;
    tone: api.InputAiComposeTone,
    display_author: ?bool = null,
    emoji_id: ?i64 = null,
    title: ?[]const u8 = null,
    prompt: ?[]const u8 = null,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeConstructorId(constructor_id);
        var flags: u32 = 0;
        if (self.display_author != null) flags |= 1 << 0;
        if (self.emoji_id != null) flags |= 1 << 1;
        if (self.title != null) flags |= 1 << 2;
        if (self.prompt != null) flags |= 1 << 3;
        try w.writeUInt(flags);
        try self.tone.serialize(w);
        if (self.display_author) |v| {
            try w.writeBool(v);
        }
        if (self.emoji_id) |v| {
            try w.writeLong(v);
        }
        if (self.title) |v| {
            try w.writeString(v);
        }
        if (self.prompt) |v| {
            try w.writeString(v);
        }
    }
};

pub const saveTone = struct {
    pub const constructor_id: u32 = 0x1782cbb1;
    pub const Result = bool;
    tone: api.InputAiComposeTone,
    unsave: bool,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeConstructorId(constructor_id);
        try self.tone.serialize(w);
        try w.writeBool(self.unsave);
    }
};

pub const deleteTone = struct {
    pub const constructor_id: u32 = 0xdd39316a;
    pub const Result = bool;
    tone: api.InputAiComposeTone,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeConstructorId(constructor_id);
        try self.tone.serialize(w);
    }
};

pub const getTone = struct {
    pub const constructor_id: u32 = 0xb2e8ba03;
    pub const Result = api.aicompose.Tones;
    tone: api.InputAiComposeTone,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeConstructorId(constructor_id);
        try self.tone.serialize(w);
    }
};

pub const getTones = struct {
    pub const constructor_id: u32 = 0xabd59201;
    pub const Result = api.aicompose.Tones;
    hash: i64,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeConstructorId(constructor_id);
        try w.writeLong(self.hash);
    }
};

pub const getToneExample = struct {
    pub const constructor_id: u32 = 0xd1b4ab14;
    pub const Result = api.AiComposeToneExample;
    tone: api.InputAiComposeTone,
    num: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeConstructorId(constructor_id);
        try self.tone.serialize(w);
        try w.writeInt(self.num);
    }
};


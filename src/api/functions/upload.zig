//! GENERATED by td TL code generator from "api.tl".
//! Do not edit by hand; regenerate with td-gen.

const std = @import("std");
const td = @import("../../root.zig");
const Writer = td.tl.Writer;
const Reader = td.tl.Reader;
const TlError = td.TlError;
const vector_constructor_id = td.tl.vector_constructor_id;

const api = @import("../mod.zig");

pub const saveFilePart = struct {
    pub const constructor_id: u32 = 0xb304a621;
    pub const Result = bool;
    file_id: i64,
    file_part: i32,
    bytes: []const u8,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeConstructorId(constructor_id);
        try w.writeLong(self.file_id);
        try w.writeInt(self.file_part);
        try w.writeBytes(self.bytes);
    }
};

pub const getFile = struct {
    pub const constructor_id: u32 = 0xbe5335be;
    pub const Result = api.upload.File;
    precise: bool = false,
    cdn_supported: bool = false,
    location: api.InputFileLocation,
    offset: i64,
    limit: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeConstructorId(constructor_id);
        var flags: u32 = 0;
        if (self.precise) flags |= 1 << 0;
        if (self.cdn_supported) flags |= 1 << 1;
        try w.writeUInt(flags);
        try self.location.serialize(w);
        try w.writeLong(self.offset);
        try w.writeInt(self.limit);
    }
};

pub const saveBigFilePart = struct {
    pub const constructor_id: u32 = 0xde7b673d;
    pub const Result = bool;
    file_id: i64,
    file_part: i32,
    file_total_parts: i32,
    bytes: []const u8,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeConstructorId(constructor_id);
        try w.writeLong(self.file_id);
        try w.writeInt(self.file_part);
        try w.writeInt(self.file_total_parts);
        try w.writeBytes(self.bytes);
    }
};

pub const getWebFile = struct {
    pub const constructor_id: u32 = 0x24e6818d;
    pub const Result = api.upload.WebFile;
    location: api.InputWebFileLocation,
    offset: i32,
    limit: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeConstructorId(constructor_id);
        try self.location.serialize(w);
        try w.writeInt(self.offset);
        try w.writeInt(self.limit);
    }
};

pub const getCdnFile = struct {
    pub const constructor_id: u32 = 0x395f69da;
    pub const Result = api.upload.CdnFile;
    file_token: []const u8,
    offset: i64,
    limit: i32,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeConstructorId(constructor_id);
        try w.writeBytes(self.file_token);
        try w.writeLong(self.offset);
        try w.writeInt(self.limit);
    }
};

pub const reuploadCdnFile = struct {
    pub const constructor_id: u32 = 0x9b2754a8;
    pub const Result = []api.FileHash;
    file_token: []const u8,
    request_token: []const u8,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeConstructorId(constructor_id);
        try w.writeBytes(self.file_token);
        try w.writeBytes(self.request_token);
    }
};

pub const getCdnFileHashes = struct {
    pub const constructor_id: u32 = 0x91dc3f31;
    pub const Result = []api.FileHash;
    file_token: []const u8,
    offset: i64,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeConstructorId(constructor_id);
        try w.writeBytes(self.file_token);
        try w.writeLong(self.offset);
    }
};

pub const getFileHashes = struct {
    pub const constructor_id: u32 = 0x9156982a;
    pub const Result = []api.FileHash;
    location: api.InputFileLocation,
    offset: i64,

    pub fn serialize(self: *const @This(), w: *Writer) TlError!void {
        try w.writeConstructorId(constructor_id);
        try self.location.serialize(w);
        try w.writeLong(self.offset);
    }
};

